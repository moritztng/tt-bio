# Run outputs and the append lock: the evidence that ships is results.jsonl and sweep.log.
runs/
.results.lock
# Operational marker written by ladder.sh when a rung breaks the card. Transient, not evidence.
.card*.needs_reset
