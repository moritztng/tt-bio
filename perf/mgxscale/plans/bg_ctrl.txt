# The batch-1 boltzgen control, on the same fixture as the 8-design job. Without it, the
# 4-of-8 geometry flags at batch 8 cannot be told apart from a property of this target crop.
--model boltzgen --target-res 512 --designs 1 --bg-steps design --host-threads 2 --timeout 14400 --tag ctrl
