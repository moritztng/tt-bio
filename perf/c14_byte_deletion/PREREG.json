{
 "row": "c14-byte-deletion",
 "written_before": "any device arm of lnbind.py ran",
 "question": "is layer_norm_w s 843.2 Mc above the traffic roof (239.0 GB/s of a measured 442.9) deletable bytes, or a non-byte term the byte roof cannot see?",
 "instrument_control": "copy_ctl: ttnn.clone on the same [1,512,512,128] bf16 DRAM->DRAM tensor moves the SAME 2 Z = 134.218 MB nominal. A known-answer case: it must land inside 380-460 GB/s, the neighbourhood of the 442.9 GB/s same-part DRAM roof c10-fold-census measured. If it does not, the instrument is wrong and no arm is believed.",
 "arms": ["ln_shipped", "ln_shipped_AA", "copy_ctl", "ln_noaffine", "ln_l1_out", "mean_ctl"],
 "predicted": {
  "ln_over_copy_ratio": {"my_call": "1.55-1.95", "reasoning": "442.9/239.0 = 1.85 if the whole gap is non-byte",
   "traffic_bound_branch": "<= 1.15 -- the gap IS traffic, byte deletion on layer_norm_w is viable and this row should build it",
   "not_traffic_branch": ">= 1.40 -- the gap is NOT traffic; layer_norm_w s 843.2 Mc / 0.6246 s is a reduce/affine term and no byte deletion can collect it"},
  "ln_l1_out_saving": {"my_call": "under 0.15 of the copy arm s 2 Z time", "lever_branch": ">= 0.25 of the copy arm s time means an L1 destination at the norm boundary is a real byte lever worth pricing per site"}
 },
 "what_would_make_it_negative": "copy_ctl outside 380-460 GB/s (instrument), or an A/A twin above 1.0 % (no arm separable at the sizes in question)",
 "fold_conversion": {"Z_MB": 67.108864, "one_deleted_pass_per_trunk_pairformer_call": {"calls": 264, "GB": 17.717, "s_at_423.6": 0.0418, "s_at_442.9": 0.0400},
  "note": "so the 0.3 s mass filter is 7.2 deleted passes over the pair tensor per PairformerLayer call"}
}
