# The xplane trace is 88 MB a run and the HLO dump carries one object file per kernel.
# Both are inputs to attrib.py, regenerated by re-running the arm; the three JSON
# artifacts it produces are what the row publishes.
out/*/trace/
out/*/hlo/
out/*/1_Trajectories/
out/*.log
