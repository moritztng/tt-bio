CAPACITY GATE -- 1536 tokens -- p300c / blackhole on tt-quietbox2:3
  DRAM 8 banks x 4278190016 B = 31.875 GiB   L1 110 banks x 1461760 B   grid (x=11,y=10)
  CAPACITY ONLY: allocates and completes. It does not and cannot check correctness -- that is scripts/full_parity_gate.py.

  model          verdict      tok   pad   rows    peak DRAM     wall  by/mech
  ---------------------------------------------------------------------------

  Coverage reductions applied by this run:
    - diffusion_samples=1. Samples are independent and add no peak, so this costs no coverage.
    - The MSA is a committed alignment rather than a fresh search. Device memory does not care how the alignment was found; this costs no coverage.
    - Target-first: 1536 runs FIRST and a pass ends the cell. Only a failure pays for a downward bisect. Costs no coverage.
    - The fixture is polymer-only, so the LIGAND-TOKEN path is uncovered: no cell here sets ligand_tokens, and a model whose production input carries ligands is tested at 1536 polymer tokens only.
    - Not driven by this gate at all: boltzgen, nesso1, pxdesign, rfd3, saprot-1.3b (see EXEMPT for each reason). Reported SKIPPED, never PASS.

  0 pass, 0 fail, 0 inconclusive, 0 skipped, 0 no-weights, 0 unmeasured of 0 cells
