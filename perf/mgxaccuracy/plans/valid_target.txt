# JOB 0 IS THE MERGE CONFIRMATION (orchestrator note 2026-09-23 14:30 UTC).
# The pxdesign refusal-memo fix is on main as 0c34f135a + d6ddfa5f9 and NOBODY has run pxdesign
# on a device since. So the first chip goes to ONE design at the 1224-token production case
# (1024 target residues + a 200-residue binder, the largest a user can submit today), whose only
# job is to report completion, wall time and DURING-sampled AICLK. One design rather than eight
# because confirmation should not wait on a batch.
--model pxdesign --target perf/mgxaccuracy/targets/gpb_dimer_1646.cif --target-res 1024 --binder 200 --designs 1 --steps 400 --host-threads 2 --timeout 7200 --tag mergeconf
# Quality at 512 against 1536 on a target that IS a target, ordered by what each job decides.
# Predictions for jobs 1-3 are pre-registered in perf/mgxaccuracy/PREDICTION.md.
#
# perf/bhdesign/targets/big_1831.cif cannot answer this: its 1536 crop is two chains 246.9 A
# apart whose sub-22 A conditioning graph has two components and ZERO edges between them.
# gpb_dimer_1646.cif is the 1GPB biological assembly, one component at every crop.
#
#   1  THE LIVE-REACHABLE RUNG. aiand-bio caps a user at 1024 target residues and pxdesign has
#      no lower override, so 1024 + a 200-residue binder = 1224 tokens ships today, and the
#      highest token count any pxdesign fit_rmsd was ever measured at is 1024. If this fails it
#      is a customer bug, not a campaign blocker. That is why it is first.
#   2  the mechanism test at 1616 tokens: predicted sub-2 A, refuted by tens of angstrom.
#   3  the 512 control on the new target, so 1 and 2 have a same-structure baseline.
#   4-5 boltzgen designability at both sizes, full pipeline, the row's headline metric.
#   6  a SECOND 512 target from the same structure (offset 823 = chain B 1-512), so the
#      fixed-size across-target spread is measured rather than assumed.
#   7  boltzgen scRMSD at n=8 at 1536 on the ORIGINAL fixture, which completes the withdrawal
#      of the opening claim on the fixture the claim was made on.
--model pxdesign --target perf/mgxaccuracy/targets/gpb_dimer_1646.cif --target-res 1024 --binder 200 --designs 8 --steps 400 --host-threads 2 --timeout 10800 --tag gpbmax
--model pxdesign --target perf/mgxaccuracy/targets/gpb_dimer_1646.cif --target-res 1536 --designs 8 --steps 400 --host-threads 2 --timeout 10800 --tag gpb
--model pxdesign --target perf/mgxaccuracy/targets/gpb_dimer_1646.cif --target-res 512  --designs 8 --steps 400 --host-threads 2 --timeout 7200  --tag gpb
--model boltzgen --target perf/mgxaccuracy/targets/gpb_dimer_1646.cif --target-res 1536 --designs 8 --budget 8 --score-dsg --host-threads 2 --timeout 32400 --tag gpb
--model boltzgen --target perf/mgxaccuracy/targets/gpb_dimer_1646.cif --target-res 512  --designs 8 --budget 8 --score-dsg --host-threads 2 --timeout 14400 --tag gpb
--model pxdesign --target perf/mgxaccuracy/targets/gpb_dimer_1646.cif --target-res 512 --crop-offset 823 --designs 8 --steps 400 --host-threads 2 --timeout 7200 --tag gpb
--model boltzgen --target-res 1536 --designs 8 --budget 8 --score-dsg --host-threads 2 --timeout 32400 --tag q
