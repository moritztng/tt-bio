# THE ENGINE DELTA BETWEEN THE 2x2's TREES IS 0.117 A, AND THE TARGET EFFECT IS 13.04 A.
#
# The 2x2's four cells did not all run the same tree (720b43a5d, d1133c67e, 07bd40614,
# d0c4f33e4), and `git diff 07bd40614 d0c4f33e4 -- tt_bio/` is 422 insertions across 14 files
# including 128 changed lines of tenstorrent.py, which every model rides. The 13.04 A target
# effect is in docs/design-throughput.md, so that had to be measured rather than argued away.
#
# The control is the SAME CROP on two trees, n=8 each, everything else identical:
#
#   512 offset 0, tree 720b43a5d   n=8   median  4.748   range 0.693-12.925
#   512 offset 0, current tree     n=8   median  4.631   range 0.823-13.493
#                                        shift   0.117 A
#
#   Mann-Whitney does NOT reject: U = 28 of 64, where 32 is no difference at all. The two
#   cells are statistically indistinguishable.
#
#   target effect, as published (cross-tree)        13.04 A
#   target effect against the new same-tree cell    13.15 A
#   engine delta is 111x smaller than the effect it could have contaminated.
#
# So the cross-tree construction of the 2x2 does not threaten the target effect. The offset-100
# re-run then landed and completed the same-tree pair:
#
#   512 offset 0 / offset 100, BOTH on the current tree   4.631 -> 16.021   gap 11.39 A
#   the same pair cross-tree                              4.748 -> 17.785   gap 13.04 A
#
# The effect reproduces. What did NOT survive is the ORDERING claim the docs carried: the size
# gap at offset 0 on the after-fix engine is 19.398 - 4.631 = 14.77 A, which EXCEEDS the target
# gap either way. The doc now says the two are the same order rather than that target beats
# size (results/token_distance_fix_refuted.txt has the 1536 cell).
#
# A SECOND THING THIS BUYS, and it is worth more than the control. These two cells are also a
# REPEAT of one configuration: same crop, same n, same everything the row controls. So they
# measure the run-to-run reproducibility of an n=8 MEDIAN.
#
# CORRECTED 2026-09-24 03:0xZ: an earlier version of this file read 0.117 A off the offset-0
# cell alone and stated it as THE reproducibility of an n=8 median. That over-generalised from
# one cell. The offset-100 cell was then repeated too and moved 1.764 A:
#
#   cell             first     repeat    spread
#   512 offset   0   4.748     4.631     0.117 A
#   512 offset 100  17.785    16.021     1.764 A
#
# So the honest figure is 0.1 to 1.8 A over two cells, and it is cell-dependent rather than a
# constant. The qualitative point survives and is what matters: the n=8 median moves by ~1 A
# or less between runs while individual designs inside one cell span 0.7 to 13.5 A, so
# eight-design cells carry quality claims and catchers and single draws do not.
#
#   per-design spread within a cell    0.7 - 13.5 A
#   run-to-run spread of the n=8 median  0.1 - 1.8 A  (two cells)
#   the effect being claimed            11.4 - 13.0 A (same-tree / cross-tree)
#
# Reproduce: perf/mgxaccuracy/plans/floor512_onetree.txt entry 1, against the banked
# offset-0 cell in results/size_1536.jsonl (engine "after-fix", crop_offset 0).


# THE SAME CONTROL AT 1536, 2026-09-24 07:58Z: ENGINE-INERT THERE TOO, AT U = 32 OF 64.
#
# Everything above is the 512 crop. The 1536 cells had no such control, and the row's headline
# interaction depends on them, so 1536/offset-100 was re-run on a THIRD tree (72ad980ee: 20
# tt_bio files and 525 insertions against the cell's original engine, tenstorrent.py +145 and
# boltzgen's own model/models/boltz.py among them). Reading pre-registered before it landed.
#
#   1536 offset 100, original (tag gpbo100)   n=8   median 13.273   range  7.951-15.153
#   1536 offset 100, repeat   (tag o100r2)    n=8   median 12.950   range  9.101-16.733
#                                                   shift  0.323 A
#
#   Mann-Whitney U = 32.0 of 64, where 32 is no difference at all; exact two-sided
#   p = 1.0000 (all 12870 splits of C(16,8) are at least as extreme as the observed one).
#   This is not "fails to reject" -- it is the statistic's exact centre.
#   0 of 8 under 4 A both times. rc 0, wall 11923.9 s, card 2, AICLK median 1000 over
#   n=4990 samples taken DURING the fold.
#
# So the cell is reproducible AND engine-inert, which by the pre-registered bands (inside
# 11.5-15.1 A) leaves every sign in the 2x2 untouched. The more expensive branch -- the 2x2
# not being engine-stable, forcing all four cells to be re-collected on one tree -- is closed.
#
# REPEATABILITY BOUND, which the closing rule depends on. This is the row's third n=8 median
# repeat: 0.117 A (512/off0), 0.323 A (here) and the 1.8 A reading. The worst case is still
# 1.8 A, so plans/closing_rule.txt keeps 4 x 1.8 = 7.2 A as GroEL's conservative threshold; it
# is not recomputed, because this repeat did not move the bound.
#
# WHAT THIS DOES NOT SETTLE. Pairing the repeat with 19.398 gives a 1536 crop contrast of
# -6.448 A against the published -6.125, same sign and nearly the same size -- but that pairing
# is STILL cross-tree, because 19.398 was measured on the pre-revert engine and the repeat on a
# third one. Only plans/onetree_1536.txt, the 1536/off0 cell running on 72ad980ee since
# 08:04Z, makes the 1536 contrast within-tree the way the 512 one already is.
