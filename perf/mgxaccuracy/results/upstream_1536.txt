# *** WITHDRAWN -- DO NOT QUOTE. Every number in this file was measured on the DISQUALIFIED
# *** disconnected fixture (chains A=1008 B=528, 162.40 A apart), not the valid contacting
# *** dimer the device ran. See results/upstream_reference_wrong_fixture.txt. The replacement
# *** run is out1536_valid on qb2. Full withdrawal note at the end of this file.
#
# UPSTREAM'S OWN 1536 DESIGN, and the answer to "is a worse number at 1536 a defect".
#
# The brief's joint 2: a worse device number at 1536 is a defect only if upstream does better
# at 1536. Upstream's six-step CPU run (out1536, qb2, torch fp32, --protocol protein-anything
# --budget 1) took 11.7 h to get through its design step and is still inside the whole-complex
# fold at 1616 tokens. The scRMSD does not need that step: design_folding reads design_dir,
# not the folding output, and the two configs differ by two lines (ref/designfolding_only.sh
# documents the diff). So step 4 was run directly off the finished inverse-folded design.
#
# ONE METRIC, ONE REFOLDER, ONE TOOL on every row below. designfolding-bb_rmsd, N,CA,C,O
# backbone, upstream BoltzGen torch fp32 on qb2 CPU, scored by scrmsd.py off refold_design_cif.
# The only thing that changes between rows is WHO DESIGNED and AT WHAT SIZE.
#
#   designed by   size   n    min    median    max
#   upstream       512   1     -      11.04     -      (step-4-only; the six-step run's own
#                                                       analysis CSV on the same design: 7.17)
#   device         512   8   0.98     7.01    12.32
#   upstream      1536   1     -       7.91     -
#   device        1536   8  16.45     19.35    22.21
#
# READ THE COLUMN, NOT THE CELL. Upstream does not degrade from 512 to 1536: 11.04 -> 7.91 by
# the identical method, or 7.17 -> 7.91 if the six-step analysis column is used at 512, and
# both moves are inside the 0.9-1.9 A repeat spread this row measured for a single upstream
# refold. The device does degrade, hard: 7.01 -> 19.35 on the same axis.
#
# At 1536 the device's ENTIRE eight-design range sits above upstream's draw. The smallest gap
# is 16.45 - 7.91 = 8.54 A, against a refold repeat spread of 0.9-1.9 A and an across-target
# 512 floor of 2.10 A. At 512 the device is at or better than upstream on the same comparison.
#
# So the row's opening question now has a sign: **the 1536 loss is the port's, not BoltzGen's.**
#
# THE REFOLD NOISE IS NOW MEASURED AT 1536, not borrowed from 512. The same upstream design
# was refolded a second time, same config, same checkpoint:
#
#   upstream 1536 design, refold 1     7.907 A
#   upstream 1536 design, refold 2     6.668 A   -> spread 1.239 A, mean 7.287
#
# which sits inside the 0.86 / 1.93 A pairs measured at 512, so the scoring fold behaves the
# same way at this size. The gap from upstream's WORST draw (7.907) to the device's BEST 1536
# design (16.446) is 8.539 A, which is 6.9x that measured spread.
#
# WHAT IS STILL n=1, EXACTLY. One upstream DESIGN, refolded twice. The refold axis is now
# doubled; the DESIGN axis is not, so upstream's design-to-design spread at 1536 remains
# unmeasured and that is the load-bearing caveat, not the refold. For the reading above to be
# wrong, upstream's 1536 design distribution would have to have its bulk above 16.45 A with
# this one draw landing at 7.9 and 6.7 on two independent refolds. A second upstream 1536
# design costs another ~12 h design step on 16 cores and is the honest way to firm it up;
# ask 9957 (vast.ai GPU) is the cheap way and has been unanswered since 02:27Z 09-23.
#
# AND EVERY DEVICE 1536 NUMBER HERE IS PRE-FIX. cc908c377 corrects TokenDistanceRecycle, the
# module that injects the target's token distances -- the conditioning a binder design is
# built on -- where the old code returned twice the pairformer's output instead of its input
# plus the update. That is an error whose size grows with token count, which is the shape of
# exactly this table. The after-fix 1536 arm is running (card 3) and is what decides whether
# the bug WAS this gap. Do not publish a 1536 quality number to users until it lands: the fix
# is already on main, so the pre-fix rows describe an engine nobody will run.
#
# Reproduce:
#   ssh qb2 'cd ~/mgxacc-tools && bash designfolding_only.sh --size 1536 --threads 6'
#   ssh qb2 'cd ~/mgxacc-tools && ~/bgref-env/bin/python3 scrmsd.py ~/bgref-work/out1536_df \
#            --json --model boltzgen --target gpb_dimer_1646 --target-res 1536 \
#            --side upstream-design-upstream-refold'
#
# ============================================================================================
# WITHDRAWN 2026-09-24 01:1xZ. Everything above is measured on the WRONG TARGET: the upstream
# fixture ~/bgref-work/fx/bgt1536.cif is the disconnected rung this row disqualified (chains
# A=1008 B=528, min inter-chain distance 162.40 A, centroid separation 250.81 A), not the
# valid gpb_dimer_1646 crop the device ran (A=823 B=713). The 7.91/6.67 A readings are real
# numbers against an easier problem and must not be set beside the device's 19.35 A.
# See results/upstream_reference_wrong_fixture.txt. Joint 2 is OPEN.
# ============================================================================================
