# The size claim with the SCORING FOLD held constant, n=8 per size, one valid target.
#
# Both rows below are the DEVICE's own designs. The only thing that folds them is upstream
# BoltzGen, torch fp32 on qb2 CPU, same checkpoint, same protocol, no card opened. So a
# difference between the rows cannot be the refolder: it is the designs.
#
#   scRMSD = designfolding-bb_rmsd, N,CA,C,O backbone, recomputed from refold_design_cif on
#   disk (scrmsd.py --check agrees with the pipeline's own column to 5 dp on the 512 run).
#
# side = upstream-refold-of-device-design, target gpb_dimer_1646 A1-<res> + 80-residue binder
#
#   res    n   min     median   max     strict  permissive   out_dir
#   512    8   0.976    7.012   12.321   0.125    0.375      out_devrefold512_all8
#  1536    8  16.446   19.347   22.209   0.000    0.000      out_dev1536
#
# ZERO OVERLAP: the best 1536 design (16.446) is worse than the worst 512 design (12.321).
#   Mann-Whitney U = 0 of 64, the maximal separation the test can report.
#   Exact two-sided p = 2/C(16,8) = 1.554e-4.
#
# That is the whole size question with every shared-instrument objection removed at once:
# same target, same crop, same binder length, same designer, same checkpoint, same refolder,
# same metric, same backbone atom set, n=8 on each side. The device refolder reads the same
# way on the identical designs (21.52 A at 1536, 8.46 A at 512 pre-fix), so two independent
# refolders agree and neither is the cause.
#
# BOTH SIDES ARE PRE-FIX, checked rather than assumed. A 512 side taken from the after-fix
# run would have confounded the token-distance fix with size, since only 512 has an after-fix
# cell so far. The designs staged into out_devrefold512_all8 are timestamped 09-23 23:21Z and
# the after-fix 512 run did not finish until 23:49:50Z, so they cannot be its output; they are
# the pre-fix eight-design cell whose device-refolder median is 8.455 A.
#
# The two refolders track each other at both sizes, same direction, same order of magnitude:
#   res    device refolder   upstream refolder   upstream - device
#   512        8.455              7.012              -1.44
#  1536       21.516             19.347              -2.17
# Against the measured upstream refold repeat spread of 0.9-1.9 A on one design
# (results/refolder_pair_512.txt) neither residual is a signal, which is the point: the
# instrument reads the same on both sides, so the 12.33 A it reports between them is not it.
#
# What this does NOT establish, and neither does anything else in this row:
#   * that TT is worse than upstream AT 1536. That needs upstream's own 1536 DESIGN, which
#     is the qb2 out1536 run -- 11.7 h in and now past its design step into folding.
#   * that the size effect is not a target effect. One valid target per size; the 512
#     across-target floor is 2.10 A and the gap here is 12.33 A, so the floor does not cover
#     it, but plans/bg_floor_1536.txt is the honest second target and it has not run.
#
# Reproduce:
#   ssh qb2 'cd ~/mgxacc-tools && ~/bgref-env/bin/python3 scrmsd.py ~/bgref-work/<out> \
#            --json --model boltzgen --target gpb_dimer_1646 --target-res <res> \
#            --side upstream-refold-of-device-design'
