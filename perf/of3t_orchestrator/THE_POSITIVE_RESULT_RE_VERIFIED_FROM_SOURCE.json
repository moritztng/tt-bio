{
 "as_of": "2026-09-20, of3t-orchestrator pass 183 -- an INDEPENDENT re-check of pass 176's inertness audit, read from the source trees on pc, CPU only",
 "why_I_re-checked_something_that_already_passed": "36.9462 % of the campaign's gradient mass -- the bulk of its entire positive result -- rests on ONE claim: that 0.5.0's change to diffusion_conditioning is cosmetic, so our agreement there is not an artefact of scoring against the wrong revision. Pass 176 verified it and I had been repeating it. This pass I caught two CHARACTERISATIONS I had repeated without checking (an inference cost quoted as the price of training parity, and 'fused-qkv' for 240 unplaced leaves), so the load-bearing one deserved the same treatment. It survives.",
 "what_pass_176_claimed": "0.5.0 constructs linear_s with linear_init_params.linear_s where 0.4.3 passed linear_init_params.linear_z -- a call site reading the right values from the wrong key -- and under diffusion_cond_init both keys are the BYTE-IDENTICAL {'bias': False, 'init': 'default'} in BOTH trees, so same state_dict and same init, cosmetic.",
 "what_I_read_myself": {
  "files": [
   "~/.coworker/scratch/of3t-reference/upstream/openfold3/projects/of3_all_atom/config/linear_init_config.py",
   "~/.coworker/scratch/of3t-reference/upstream050/openfold3/projects/of3_all_atom/config/linear_init_config.py",
   "the same pair's openfold3/core/model/layers/diffusion_conditioning.py"
  ],
  "diffusion_cond_init_0_4_3": {
   "linear_s": "{\"bias\": False, \"init\": \"default\"}",
   "linear_z": "{\"bias\": False, \"init\": \"default\"}"
  },
  "diffusion_cond_init_0_5_0": {
   "linear_s": "{\"bias\": False, \"init\": \"default\"}",
   "linear_z": "{\"bias\": False, \"init\": \"default\"}"
  },
  "the_call_sites": {
   "line_92": "linear_init_params.linear_z in BOTH trees -- unchanged",
   "line_108": "linear_init_params.linear_z in 0.4.3, linear_init_params.linear_s in 0.5.0 -- the one change"
  },
  "verdict": "CONFIRMED. One call site switches which key it reads; both keys hold the identical dict in both trees. `bias: False` means the same state_dict keys and shapes, and `init: default` is overwritten by a loaded checkpoint. Functionally inert."
 },
 "what_this_re-check_does_and_does_not_extend": "it re-verifies BOTH modules behind the 38.1862 % from source: diffusion_conditioning (36.9462 %) at pass 183 and msa_module (1.24 %) at pass 184. It does NOT cover the diffusion transformer carrying the 51.1358 %, which pass 184 read and found NOT inert -- 0.4.3 has a learned layer_norm_z per DiT block where 0.5.0 has one shared (D108). That difference changes no measurement, because every figure on that scope is against 0.4.3 and our port selects the variant from the state dict, but it is revision-DIFFERENT-BUT-HANDLED and must not be called inert.",
 "the_general_point_this_pass_keeps_producing": "the campaign's numbers are audited on every compose -- 162 checks, 0 drifted. Its CHARACTERISATIONS are not, and they are where the errors have been: a cost figure from the wrong execution, a category ('fused-qkv') with nothing establishing it, and this one, which happened to be right. A number has a check; a word has an author. Re-read the words that carry the most mass.",
 "provenance": "of3t-orchestrator pass 183 on pc, CPU only, no device. Both release trees read directly; no figure taken from an artifact.",
 "msa_module_now_re_verified_too_pass_184": {
  "was": "the 1.24 % module was left as 'pass 176's reading -- 20 lines, all cache plumbing', explicitly NOT re-checked, and this file said so.",
  "read": "~/.coworker/scratch/of3t-reference/upstream{,050}/openfold3/core/model/latent/msa_module.py, full diff, 20 changed lines.",
  "what_they_are": [
   "a copyright line",
   "`from openfold3.core.utils.device_utils import empty_device_cache`",
   "a new parameter `clear_cache_between_blocks: bool = False` plus its docstring, stored and threaded down to the stack",
   "`torch.cuda.empty_cache()` replaced by `empty_device_cache(accel_device)` -- device-portability, not arithmetic",
   "three further `empty_device_cache(...)` calls, each guarded by `if self.clear_cache_between_blocks`"
  ],
  "verdict": "CONFIRMED inert. Every changed line is accelerator memory-cache management, the new flag DEFAULTS FALSE so nothing changes unless it is switched on, and no line touches the math. 'Cache plumbing' was exactly right.",
  "so": "both modules behind the 38.1862 % are now independently re-verified from source rather than inherited -- diffusion_conditioning at pass 183 and msa_module here. The campaign's positive result no longer rests on any characterisation this row has not read itself."
 }
}
