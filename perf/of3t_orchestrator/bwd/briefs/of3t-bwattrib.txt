# Workstream: of3t-bwattrib
#DISPATCH: host=pc card=0 repo=tt-bio maxit=24 tier=opus5
DONE_CHECK: python3 /home/moritz/.coworker/workstreams/_of3t_donecheck.py of3t-bwattrib

**CONSOLIDATED 2026-09-25 pass 451, replacing five stacked amendments.** Your brief had grown to
23 KB and the fleet injected 137 diff lines on your restart, on top of a turn that hit the 3000 s
cap. A brief is re-read every pass exactly like a state doc, so that churn was my cost to you and
this is the fix: everything below is CURRENT. Nothing is superseded, nothing contradicts anything,
and the pre-consolidation version is at
`perf/of3t_orchestrator/bwd/briefs/of3t-bwattrib.pre-consolidation.txt` if you want the history.

**READ FIRST**: the **STALE BASELINE banner** at the top of `state/of3t/BACKWARD.md`, and **§A46
of `state/of3t/PROTOCOL.md`** (how a backward result is graded). Do not re-derive either.

## What this row is

**J0: attribute where the backward's time actually goes.** It is the highest-value row in the
sprint and everything else is ranked behind it. Your work so far is pushed at
`origin/wk/of3t-bwattrib` `d4eb1f0cf` (three commits — the histogram instrument, the forward
profiled on the same instrument, and the aborted crop-384 attempt kept for its lease refusal), so
the timeout cost you a turn and not your results.

## What has changed under you, in one place

1. **The baseline is void (R209).** The 466.702 s step was banked at `451ed56f4`, two days before
   `502ed112e` put a **HOST float64** softmax and layer norm inside every `tape()` — and inside
   `autograd.backward`, which opens `with _training_exact("backward")` for itself. A taped forward
   is **251.66 s** today against **3.415 s** banked, and **247.8 s of that is the exactness**.
   **Do not use 356.00 s, 168,922 calls, 2.107 ms, 44.3x, the 97.7 % or the 100.668 s residual as
   a denominator.** Report your own totals and derive your own shares. Your RUN is unaffected —
   you profile today's tree, so you measure what actually happens.
2. **Name the commit at the top of CAUSE.** Two correct suspect lists exist and which one is right
   depends entirely on the tree you profiled. On `451ed56f4`: `Tensor.evict`, `add_grad`'s fp32
   typecast, `to_layout` at fan-in, allocation churn. On main, `of3t-intensity` adds a fifth that
   dominates — **the exact LayerNorm backward does two `ttnn.to_torch(...).double()` per node over
   1,296 of 2,473 tape nodes**. A histogram that does not say which tree it ran on cannot be read.
3. **Grade with `exact_training(False)`, stated in every arm.** Decided pass 450 for the whole
   sprint: every kernel row branching from main runs an 8.4x step, so an uncontrolled A/B is 8.4x
   contaminated by a host term and two real levers become indistinguishable. The shipped-config
   baseline is `of3t-restep`'s job, not yours. This is a measurement convention and **not** a
   proposal to ship `exact_training(False)`.
4. **`Tensor.evict` is already re-priced by your own work** — a device-local L1-to-DRAM copy, not
   a PCIe round trip — so it belongs against the bandwidth roof. Carry that into CAUSE with the
   roof named (A46 clause 2).

## Deliverables

- **HISTOGRAM:** the per-verb timing DISTRIBUTION — name, shape, dtype, count, total. Not a mean:
  the question is whether the per-call cost is uniform overhead or a few verb classes carrying it,
  and those are different defects with different fixes.
- **CAUSE:** suspects separated by break controls that MOVE the number, commit named first. A
  suspect you cannot make worse on purpose is a suspect you have not tested.
- **RESIDUAL:** the backward's time minus the sum of your histogram. **This is the most valuable
  unclaimed number in the sprint** and its likely cause is now named rather than open: the
  backward carries the same host float64 softmax and layer norm the forward does. Time inside
  `_exact_softmax_raw` / `_exact_layer_norm_raw` against the residual settles it in one line. If
  the residual IS the exactness, say so plainly — that is a more valuable result than the
  histogram, and it means J1/J2/J4 are second-order until the exactness question is resolved.
  Your gate does not require this field, so it cannot block your conclusion.
- **BRACKET:** already delivered and already banked a decision — your 2-blocks-per-call reading
  retired J3 (R208). What is still owed is the runtime `sdpa_shapes` confirmation, which you said
  comes out of the FORWARD leg inside the first ~15 s of device time. **Report it even if the
  backward profile is still running or fails**: it is the falsifiable reopen condition on a
  retirement, and a count near 40 rather than 2 puts J3 back at the top of the list. Settle the
  `fp32_softmax=True` question beside it (`tenstorrent.py:10395`) — if `_v_sdpa` never fires in
  OF3's trunk, that is its own line and it may be the larger finding.
- **FIX:** land only what is unambiguously yours and cheap. `tt_bio/autograd.py` and
  `tt_bio/taped_ttnn.py` are CONTESTED — BCX is rewriting the head verbs in both at
  `wk/bcx-heads` `237f53064` — so keep any diff there minimal, list the line ranges, and expect to
  re-run BCX's bar. You were right not to land a wiring fix off an unmeasured attribution.

## The card, and the one thing not to do

You are pinned to **pc card 0**, the only reachable Blackhole card (whglx is Wormhole; qb1 is
unreachable with all cards blocked; qb2 card 0 reads 800 MHz so a timing there is an artifact, and
1/2/3 are held by BCX). It is contended — `of3t-wheelbw` holds it as I write this and
`of3t-restep` is queued — so **push every pass**, which you have been doing and which is why the
timeout cost nothing.

**Do not treat the host float64 path as a bug and do not propose switching it off to buy speed.**
It is a fidelity feature Moritz approved personally, training-only, inference untouched, and it is
why this campaign's gradients are correct. Price it precisely, with the axis and the arms. What to
do about the price is a product decision on a trade he already ruled on once, and it goes to him
with a number — not to a row with a flag.

State doc `state/of3t-bwattrib.md`: HISTOGRAM:, CAUSE:, BRACKET:, FIX:, VERDICT: (and RESIDUAL:).
