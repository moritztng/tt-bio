<!-- PUBLISHED COPY, regenerated by compose_verify.sh on every compose.
     AUTHORITATIVE SOURCE: ~/.coworker/state/of3t/DEFECTS.md on pc.
     Edit that one. An edit here is overwritten on the next compose, and two live
     copies of one document is the defect this campaign spent four passes fixing.
     Published so that wk/of3t carries the reasoning and not only the artifacts:
     the source is gitignored, on one machine, with no backup. -->

# What the OF3T campaign found in shipped code

The campaign was chartered to prove that our OpenFold3 training step is upstream's. It has not
reached that claim yet. What it *has* produced is a list of real defects, several of them in code
that runs today and several affecting models other than OpenFold3.

**Everything below is fixed on `wk/of3t` and unmerged, or unfixed.** Nothing here has reached
`main`. `wk/of3t` is the single reviewable branch and it goes to Moritz's gate, not past it.

Maintained by `of3t-orchestrator`. Every entry names the row that found it and how it was
measured, because an unattributed defect list is a rumour.

---

## Affects inference that users get today

### MECHANISM MAP — three UNFIXED defects sit in the same pair track and no entry named the others

Added pass 83 because the record described them separately for forty passes and a reader (me,
twice) cannot assemble them from three entries that do not cross-reference. **Co-location is
stated as fact; a common cause is NOT asserted** — it is the open question, and this campaign
has been wrong twice recently by promoting a plausible shape to a finding.

| defect | what was measured | where |
|---|---|---|
| **D19** | forward masked-z error **7.811e-03** per pairformer block, composing **near-linearly** to **2.792e-01** over 48 blocks — five times the `sqrt(48)` rounding predicts, so **correlated**, i.e. systematic | pairformer block, forward |
| **D8** | assembled block's pair-track **gradients** 4.3e-01 to 1.4e+00 while every sub-module passes alone (0.0092–0.0172); graded by attention/pair involvement — `tri_att_end` 0.3838, `attn_pair_bias` 0.1470, `tri_att_start` 0.0865, and `single_transition`, the one sub-module with no attention and no pair coupling, is the **only passer** at 0.0212 | pairformer block, gradient |
| **D9** | `fp32_softmax` alone moves the triangle-attention weight gradient **3.2x** while the forward moves **12 %** | triangle attention, gradient-only |

**PASS 89 — D23 SUPERSEDES THE CAUSE OF D19 IN THIS MAP.** The reference bundle is upstream
0.5.0 loading the preview2 checkpoint, a combination upstream's own registry marks unsupported.
Two code changes separate the revisions: `transpose_bias` on the trunk's ending node, and a
`layer_norm_z` that 0.5.0's new `DiffusionAttentionPairBias` no longer has. D19 is the first of
those measured against a reference that cannot run these weights. **D8/D9 are NOT explained by
it** — they are gradient-scope and the trunk change is a forward one, so this map's co-location
claim stands for them. See D23.

**What follows, and it is load-bearing for the verdict.** D9 proves a class of error here that a
forward comparison **structurally cannot see**. Therefore **closing D19 would not make instrument
A pass**: a forward fix cannot reach a gradient-only defect, and D8's grading by attention
involvement says the gradient side has its own contribution. The two must be closed
**separately**, and any plan that treats the forward gap as the single blocker is mis-scoped.

**What is NOT established.** That D19 and D8 share a cause. They are different quantities
(forward activation vs weight gradient) measured at different scopes, and pass 47 explicitly
bounded D9 out as the explanation for D8 — projected onto block 0 at D9's own factors the block
median moves only 0.07813 → 0.07024 with 32 of 52 tensors still over bar. Three defects in one
pair track is a lead, not a mechanism.

### D1. OpenFold3's trunk pair bias arrives at 20 % of its intended value. Fix WRITTEN and MEASURED; must not ship alone (blocked by D10). Release-gated.

`openfold3_trunk.py:139` builds all 48 pairformer blocks with `scale_pair_bias=False`. The
token-level `AttentionPairBias` computes `(q@k^T + z) * head_dim**-0.5` **inline**, so the bias
is scaled along with the scores and must arrive pre-baked by `sqrt(head_dim)`. It does not, so
it lands at **1/sqrt(24) = 0.204** of the reference value in every OF3 fold we serve.

Found by `of3t-confidence`, by sweeping the coefficient `c` in `softmax(qk/sqrt(24) + c*bias)`
per block against the device output with real weights: `c = 1/sqrt(24)` fits all four blocks at
**6.9e-03 to 1.7e-02** while the reference `c = 1` is off by 9.1e-02 to 3.3e-01. `compute_bias`
itself is exact, so the z-projection was never the cause. On block 0 against the real golden the
update reads **2.5873e-02** with the current setting against **1.5725e-02** with the bias
correct.

**Why it is not simply flipped, established by `of3t-orchestrator`:** one `scale_pair_bias` flag
is forwarded to all three attentions in `PairformerLayer`, and the two classes need **opposite**
values for OF3. For `TriangleAttention` `False` is *correct* and was established by measurement
(the folded form makes the OF3 softmax ~5.7x too peaky, 0.892 against 1.000000 versus the
reference golden); for `AttentionPairBias` it is *wrong*. **Flipping the single flag fixes one
track and breaks the other.** The fix is to split the flag.

**PASS 92: the default was flipped ON in the composition anyway, and is now held at False with a
guard.** `of3t-pairbias`'s commit `bb59a9730` landed the split **and** the flip, against its own
verdict ("Land the MECHANISM. Do NOT flip the OF3 trunk default on this evidence"), so
`wk/of3t` — the single branch that goes to Moritz's merge gate — carried `scale_pair_bias=True`
while `origin/main` carries `False`. Merged as it stood it would have served the 1.245 A rank-0
regression to every OpenFold3 user. Found by `of3t-confhead` reading the branch rather than the
verdict, verified against git by the orchestrator, and reverted on `wk/of3t-orchestrator`, which
the compose merges last. **The split stays** — it is correct, unified, byte-identical on the other
four models, and `of3t-confhead` needs it to drive its arms. `compose_verify.sh` now asserts the
composed tree carries `scale_pair_bias=False, tri_att_scale_pair_bias=False` and exits 1 naming
the line otherwise; shown failing on the pairbias tree. When D1+D10 are approved, the assertion
changes in the same commit as the flip.

**Status: MEASURED end to end, and the answer is that the fix must NOT ship alone.**
`of3t-pairbias` split the flag (`tri_att_scale_pair_bias`, default-preserving) and folded 1UBQ
through the production CLI with a searched MSA, 5 diffusion samples, **nine seeds per arm**,
shipped arm from a detached `origin/wk/of3t` checkout **on the same p300c at AICLK 1350 sampled
during the fold**:

| | shipped | split |
|---|---|---|
| rank 0 vs experimental | **0.782 A** | **1.245 A** |
| best of five vs experimental | **0.679 A** | **0.629 A** |
| lever, arm vs arm, matched seed | **1.204 A** mean (0.439-1.626) | |
| **seed floor**, this target, 36 pairs | **0.324 A** mean (0.011-0.734) | |

**The lever is 3.7x the seed floor, so it is real.** And it splits two ways that must not be
averaged: **the corrected trunk samples better and serves worse**. Best-of-five improves
0.679 -> 0.629 while rank 0 degrades 0.782 -> 1.245, because the confidence head mis-ranks —
see **D10**. Shipping this fix on its own would make served structures worse even though the
model got better.

**One half of the original diagnosis is withdrawn.** The token-side claim holds:
`AttentionPairBias` at `False` reads 5.7577e-02 / PCC 0.998522 against a float64 transcription
of OF3's own formula, at `True` 1.7795e-02 / PCC 0.999977 — a 3.2x error reduction, `True` is
right. But `TriangleAttention` reads **3.4222e-02 at False against 3.4287e-02 at True — the flag
is inert there**, because `bias_scale_inv = 1.0/self._bias_scale` (`tenstorrent.py:7658`) and
`ttnn.multiply(bias, self.scale/self._bias_scale)` (7647, 7683) divide out what the load-time
bake multiplied in, leaving only bf16 rounding. Reverting the confidence head's tri override
moves the served structure by **0.000 A on five seeds**. The claim that the two classes needed
*opposite* values was half wrong; the split is still right engineering, because it is what let
the token side take `True` alone.

Other models unaffected and the control proves it can fail: Boltz-2, BoltzGen, Protenix-v2 and
AF2 each from its own checkpoint are **byte-identical**, while `--tri 0` changes all three
Pairformer digests.

**PASS 90: D1's convention survives the D23 audit, and it needed checking.** D23 showed the
campaign's reference ran the preview2 checkpoint on 0.5.0 and that two conventions differ between
the revisions, so a fix whose reference transcription came from the wrong one would have been the
same mistake in the other direction — and D1 changes every OF3 fold users get. It is clean: the
computational core of `primitives/attention.py::_attention` is identical in 0.4.3 and 0.5.0.
`q` is pre-divided by `sqrt(c_hidden)` by the caller (`attention.py:314` in 0.4.3, `:321` in
0.5.0), then `scores += b` adds the biases **unscaled to already-scaled scores**, then softmax.
The only difference between the revisions is where a dtype cast sits, inert in float64. **So the
bias must arrive pre-baked by `sqrt(head_dim)` under both revisions and D1's diagnosis is
revision-stable.** Owner `of3t-confhead`, dispatched pass 90 — D1 and D10 ship together or not at
all.

### D25. Replaying upstream against upstream across boxes, at one revision, reads median 6.224e-02 with 151 of 171 tensors over the bar. It is NOT a floor on our arms — it shares a term with them. MEASURED.

Raised by `of3t-orchestrator` pass 93 and **substantially corrected by `of3t-rebase` the same
night**; the correction is the useful part and is recorded first.

**The measurement.** `perf/of3t_gradients/replay_vs_r0.json`: a qb2-CPU r = 0 replay against the
republished r = 0 tape produced on a rented A100 — **upstream against upstream**, same revision,
same batch `batch_step003.pt`, same replayed `draws_recycles0.pt`, dropout disabled both sides.
Median **6.224e-02** over 171 tensors, **151 over the 5.0e-02 bar**; per block 5.707e-02 /
6.198e-02 / 7.379e-02. **No model of ours is in it.** That much stands.

**What does NOT follow, and pass 93 asserted it anyway.** I wrote that this "bounds every
comparison taken against it" and that the `tb-off` arm reading 1.148e-02 — below 5.665e-02 — was
an inconsistency needing explanation. Both are wrong. `of3t-rebase` tested the two explanations I
offered and refuted each, with arithmetic over artifacts already in the branch
(`perf/of3t_rebase/box_floor_a14.py`, `box_floor_a14.json`):

| block | floor as published | floor, A14 applied | floor on the arm's own 52 | `tb-off` arm |
|---|---|---|---|---|
| 0 | 5.707e-02 (50/57) | 5.698e-02 (49/56) | 5.665e-02 (45/52) | **1.148e-02** (7/52) |
| 23 | 6.198e-02 (52/57) | 6.190e-02 (51/56) | 6.182e-02 (47/52) | **1.743e-02** (12/52) |
| 47 | 7.379e-02 (49/57) | 7.323e-02 (48/56) | 7.323e-02 (44/52) | **4.004e-01** (52/52) |

A14 is not it: it removes exactly three tensors, one per block, all
`attn_pair_bias.layer_norm_z.bias` at reference norms 1.089e-19 / 4.181e-19 / 1.932e-17. That
collapses the **worst** case — 2.2439 to 0.1744 at block 0 — and moves the **median** by nothing,
6.2244e-02 to 6.2147e-02 over all 171. The tensor set is not it either: matched 52 to 52, none
absent either side, the gap stands.

**The reason is that the two contrasts share a subtrahend.** The floor is
`upstream_CPU − reference_A100`; the arm is `ours − reference_A100`. Two differences against the
same reference do not order each other — the triangle inequality bounds `|arm − floor|` by the
distance between our gradient and upstream's, and says nothing about `arm ≥ floor`. The intuition
that "a comparison cannot be tighter than the floor under it" holds for an *independent* noise
floor and not for this. **So the published box figure is not a floor on these arms at all, and
D25's original framing is withdrawn.**

**And it inverts what the rebuild does.** Building BUNDLE-MIN-043 on qb2 CPU — the host the
capture runs on — does not *add* a box confound, it *removes* one: the published arms were
ours against upstream-on-A100, the corrected arms are ours against upstream-on-qb2. The
0.5.0-on-qb2-CPU control still earns its time, because it is what quantifies the A100-to-CPU term
at fixed revision so the revision effect can be stated alone.

**What survives, and it is the part that matters for D19.** D19 records a diffusion-scope figure
of **6.444e-02** from *"their model against their own bundle entries"* — the same kind of
measurement as this one, upstream against upstream — and attributes it to our forward gap, then
concludes *"until D19 closes nothing compared against the bundle at this scope can read below
6.4e-02."* **That is wrong twice.** A replay of upstream against upstream cannot contain our
forward gap, so the attribution is wrong; and a shared-subtrahend contrast is not a bound, so the
conclusion does not follow even if the attribution had been right. D19's floor claim should be
read as a measurement of cross-box reproducibility and not as a limit on anything.

**Provenance note, the reusable part.** `of3t-rebase` first sized its box term from
`capture_trunk_boundary_nodropout.json`, quoting worst 2.256 / 1.971 / 1.563 and forward
1.686e-02. That file carries `reference_standing.standing = HISTORICAL` and a `superseded_by`
field: its `capture_vs_bundle` block scores against the **D18-withdrawn** train-mode tape, whose
median of 1.096 is inside noise of the zero-model answer of 1.0. The current figures are 6.224e-02
and 6.735e-03. *The artifact said it was superseded and was read past* — which is why superseded
fields should be nulled or moved rather than labelled.

### D24. On a single chain, OpenFold3 ranks its samples with a rule two of whose four terms are identically zero — and it is the only model of five that leaves it unhandled. UNFIXED. Affects every monomer fold shipped today.

Found by `of3t-confhead` pass 1, machine-checked in `perf/of3t_confhead/rank_rule.py`. Independent
of D1: it is the shipped selector, on the shipped default.

**PASS 95 CORRECTION, by `of3t-confhead` against its own hypothesis: on ubiquitin the rule reduces
one step FURTHER, to `0.2*pTM` and nothing else.** `disorder` reads **0.0 on all five samples** —
a compact 76-residue fold never pushes a 25-residue smoothed RASA window past the 0.581 threshold
— so every `rank_score` in the captured fold is `0.2*ptm` to the last digit. **The disorder
mechanism below is therefore not what moves these picks**, and a fix aimed at the RASA term would
have measured nothing on this target.

Scope that correction precisely rather than over-applying it: `disorder = 0` is a property of a
compact 76-residue monomer, not of monomers. On a larger or genuinely disordered single chain the
term is non-zero and its 2.5x leverage over the only confidence term applies as written. What is
refuted is *disorder as the explanation for D10 on 1UBQ*, not the weighting analysis.

**And it makes the obvious fix provably inert.** With `iptm = 0` and `disorder = 0`, the
`shipped`, `family`, `no_disorder` and `ptm` rules all reduce to a positive multiple of pTM, and a
positive multiple cannot change an ordering — `rules.py` puts all four on **identical served
RMSDs, sample for sample**, which needs no further seeds to believe. **So giving OpenFold3 the
ipTM→pTM fallback the other four models carry changes nothing a single-chain user is served.** It
remains the right consistency change for complexes; it is not the D10 fix.

**Which relocates the fix.** The selector rests entirely on pTM, whose spread across the five
samples is **0.010953** while their Cα-RMSD spreads **0.58 A** — it is ranking a 0.58 A structural
difference with a signal that moves by one part in a hundred. The head also emits **pLDDT, PAE,
PDE and experimentally-resolved, and the rule reads none of them**. On the three `fix`-arm folds
captured so far `gpde`, `plddt`, `pae` and `boltz` each avoid the 1.60 A sample entirely and serve
0.803–0.830 A where pTM serves 0.966 A. The row states plainly that three runs is not a result and
the nine-seed table decides.

**The reproduction control under all of this is unusually strong and worth recording on its own.**
One fold, arm `fix`, seed 1, qb2 p300c card 0, **AICLK 1350 MHz with 34 reads during the fold,
min = max = median = 1350**, 371 s wall: its five ranked Cα-RMSDs reproduce `of3t-pairbias`'s
published `fix_s1` row to a **worst difference of 3.4e-08 A** — on a different card of the same
class, through a different driver, with the D1 arm applied by patching the trunk `Pairformer`
rather than by a second checkout. One comparison confirming the lever, the RMSD computation, the
ranked order and card-class reproducibility together.

`openfold3_fold.py:277` selects the returned sample with AF3 SI 5.9.3's full-complex metric,

```
0.8*iptm + 0.2*ptm + 0.5*disorder - 100*has_clash
```

which is faithfully upstream's (`openfold3 0.5.0 core/metrics/sample_ranking.py`). **On a single
chain two of those four terms are identically zero by construction**: ipTM averages over
cross-chain pairs that do not exist, and `has_clash` is an inter-chain indicator. Verified against
a verbatim transcription of upstream's own `compute_ptm` rather than asserted — one chain gives
ipTM **0.000000** in both implementations, two chains give **0.154491** in both, so the collapse is
the rule and not a broken call. (Our `_ptm_iptm` and theirs also agree on pTM to six decimals,
0.150017, which is a free parity reading.)

**So 0.8 of the weight budget is identically zero and what actually ranks a monomer's samples is
`0.2*ptm + 0.5*disorder`** — in which the RASA disorder term carries **2.5x the weight of the only
confidence term, with a positive sign**. A rival sample needs pTM higher by 0.125 to outrank a
sample 0.05 more disordered. That is a concrete reason for a selector to prefer the looser mode and
it predicts the sign D10 observes.

**PASS 103 CORRECTION, from reading the four sites in source: OpenFold3 is alone in the CODE and
is NOT alone in the resulting ORDER, and the actual outlier is Boltz-2.** Monomer ordering implied
by each site with `iptm = 0` or `None`:

| site | monomer score reduces to | orders by |
|---|---|---|
| `openfold3_fold.py:277` | `0.2*ptm + 0.5*disorder`; disorder = 0 on 1UBQ → `0.2*ptm` | **pTM** |
| `rf3/confidence.py:108` | `iptm_v := ptm_v`, so `1.0*ptm − 100*clash` | **pTM** |
| `worker.py:1065` | `iptm ≤ 0 → ptm` (pLDDT only if `ptm == 0`) | **pTM** |
| `boltz2.py:6238` | `4*complex_plddt + ptm` | **a pLDDT blend** |

**Three of the four order monomers identically, and OpenFold3 is one of the three** — `0.2*ptm`,
`1.0*ptm` and `ptm` are positive multiples of one another. That is the same algebra `of3t-confhead`
used to prove the family fallback inert, carried one step further: it is inert across rf3 and
protenix too, so adopting it would move OpenFold3 from one pTM ordering to another.

**Boltz-2 is the outlier and it is the only site that reads pLDDT at all** — which matters because
pLDDT is the better signal on this evidence, serving **0.709 A** against `of3_fix`'s 0.755 A and
shipped's 0.775 A on the same samples. So the chosen fix is **adopting Boltz-2's shape and
departing from rf3 and protenix**, which is the right direction on the measurement and is not
"bringing OpenFold3 into line with the family". Consistency with the family is the one
justification the source does not support.

**And UNIFIED-NEVER-PER-MODEL is not satisfied by making a fourth copy agree with three others
when the four are four different formulas.** If pLDDT ranks better, the unified answer is one
shared ranking function all four call, and rf3, protenix and of3 are all currently on the worse
side of the campaign's own measurement. That is a larger change than `of3t-confhead` should make
unasked; it is recorded here as the recommendation the evidence supports and as the thing that row
is deliberately not doing.

**OpenFold3 is alone in the family in leaving the degeneracy unhandled**, checked against the four
source sites:

| model | monomer handling | site |
|---|---|---|
| **openfold3 / openbind** | **none — 0.8 of the weight budget is identically zero** | `openfold3_fold.py:277` |
| boltz-2 / boltzgen | substitutes pTM for ipTM | `boltz2.py:6238` |
| protenix-v2 / opendde | substitutes pTM, then pLDDT | `worker.py:1065` |
| rf3 | substitutes pTM | `rf3/confidence.py:108` |

**What is NOT established**, and the row says so: that disorder is what flips these particular
picks. The previous row stored only per-run aggregates, never per-sample ptm/disorder/pLDDT, so the
term that moves each decision is unmeasured. `perf/of3t_confhead/rank_fold.py` is written for it
and needs a card.

**The obvious fix is not sufficient, and that is arithmetic rather than opinion.** With the family
ipTM→pTM fallback the monomer rule becomes `1.0*ptm + 0.5*disorder`, so the pTM edge a better
sample needs falls from **2.5x** the disorder gap to **0.5x** — a 5x reduction, not an elimination.
`rules.py --selftest` constructs a 0.05 pTM gap against a 0.30 disorder gap where **both** rules
still serve the 1.6 A sample over the 0.5 A one, while the seven candidate rules that do not reward
RASA serve the 0.5 A one. Nine candidate rules are committed **before** any per-sample number
exists, because a rule chosen after seeing which one wins on nine seeds of one target is a rule
fitted to nine seeds of one target.

Scope if a selection-rule change lands: `openfold3_fold.py:277` is **openfold3 and openbind**, so
it reaches 2 of the 5 named models by construction and the other 3 must be shown byte-identical
with a control that moves them. Owner `of3t-confhead`.

### D10. The confidence head mis-ranks diffusion samples, and it is what makes D1's fix serve worse. UNFIXED.

**PASS 101 — MEASURED END TO END BY `of3t-confhead`. D1 DOES NOT SHIP; D10 SHIPS AS A CORRECTNESS
FIX WITH NO ACCURACY CLAIM.** 1UBQ, production CLI, the same searched MSA `of3t-pairbias` used, 5
samples, 200 sampling steps, arms interleaved, p300c, AICLK 1350 sampled during. Four arms from
two fold campaigns, because a selection-rule change does not move the diffusion samples — six
seeds with both arms complete, three pairs still folding:

**FINAL, superseding the six-seed interim first recorded here** — nine ship-arm seeds, eight
fix-arm seeds, seed floor over **28 pairs**, read from `perf/of3t_confhead/analyze.json` on
`wk/of3t-confhead` at `5588d889a` rather than from the row's prose:

| arm | rank 0 (served) | best of 5 | seed floor, 28 pairs | picks best |
|---|---|---|---|---|
| shipped | **0.775 A** | 0.679 A | **0.226 A** | 1 |
| D1 alone | **1.201 A** | 0.616 A | 1.133 A | 3 |
| D10 alone | **0.760 A** | 0.679 A | 0.282 A | 1 |
| D1 + D10 | **0.924 A** | 0.616 A | 0.671 A | **0** |

The D1+D10 gap is **0.92369 − 0.77507 = 0.149 A**, not the 0.170 A of the six-seed interim. Every
number here moved and the sign did not.

**A nuance the `picks best` column carries and the means hide:** the repaired rule picks the
single best sample **0 times** on the D1 arm where the shipped rule picks it 3 times — and still
serves **0.28 A better on average**. It trades *picking the best* for *avoiding the worst mode*,
which is the right trade on a bimodal distribution and is why mean-served improves while
picks-best falls.

**D1 does not ship, and fixing the selector did not rescue it.** D1+D10 serves **0.149 A worse**
than shipped; the best rule in the whole candidate set, `gpde`, still serves 0.861 A, 0.086 A
worse. The corrected trunk continues to **sample better and serve worse** — best-of-5 0.634 A
against 0.663 A. The bar is "the served structure, rank 0, at or better than shipped", and it is
not met.

**The row states, correctly and unprompted, that both gaps are smaller than the shipped arm's own
0.275 A seed floor**, so neither the regression nor D10's 0.020–0.066 A improvement is separable
from re-running with another seed. D10 therefore ships as what it is — a rule that gave 0.8 of its
weight to a term that is identically zero (D24), now fixed, with the served structure no worse —
and **not** as a measured accuracy win. `plddt` alone would have served 0.709 A, better than the
chosen `of3_fix` at 0.755 A, and was not picked: a rule chosen for the best number on one target
is a rule fitted to one target, which is why the nine-rule candidate set was committed before any
number existed.

**Sign stability is reported rather than a single number**: the D1+D10 gap reads 0.062 A at four
seeds and 0.170 A at six, same sign throughout. Six seeds, one target, one checkpoint.

**Provenance, recorded rather than buried**: folds ran on qb2 outside the fleet lease system for
that host (chip 0 seeds 1–5, chip 1 seeds 7–9 plus a `fix` seed-3 re-run, chip 2 seed 6 after
`c14-land-tail` released the 2/3 board pair). One fold was refused when `of3t-rebase` opened chip
0 inside tt_bio's 120 s device-lease wait and was re-run; one wedged at 0 % CPU before building
the trunk and was killed by explicit pid and restarted. Load 39–43 on 16 cores throughout, both
campaign shells and all descendants at **nice 15 in one sweep so both arms carry equal priority**,
and no wall-clock trend is claimed. The seed-1 control is what makes cross-chip reporting safe:
the same seed on a different chip of the same class reproduces the published structure to
**3.4e-08 A**.


**PASS 92 CORRECTION TO THE PASS-90 REFRAME BELOW, by `of3t-confhead`, and it is right.** A
five-sample ordering has two one-sided marginals and they disagree here. Pass 90 quoted only the
first and overstated it.

| | ship | D1 fix | random |
|---|---|---|---|
| picks the best sample | 1/9 | 3/9 | 1.8/9 |
| **predicted** rank of the truly best sample | 3.56 ± 0.44 | 2.22 ± 0.32 | 3.00 |
| **true** rank of the sample the head picked | 2.67 ± 0.41 | 3.33 ± 0.60 | 3.00 |
| Spearman rho, predicted vs true | 0.178 ± 0.173 | 0.267 ± 0.155 | 0.000 |

Quote the first marginal and the head looks better with the fix; quote the second and it looks
worse. The symmetric statistic settles it: **fix minus ship is +0.089 ± 0.232 Spearman**, which
separates neither arm from the other nor convincingly from chance. **Ordering quality is
statistically unchanged**, so pass 90's "the head ranks better with the fix" is withdrawn — the
half it got right, and which stands, is that this is *not* a calibration regression to undo.

**What the row established instead is sharper and is a decision, not a distribution.** The
corrected trunk's samples go bimodal: pooled over 45 samples the fix arm spans 0.560–1.670 A with
its largest gap at 1.362 → 1.592 A, and the mode above that gap holds **10 of 45 samples (22 %)**.
The head serves rank 0 **from that worst mode on 5 of 9 seeds**, where random selection would do
it 22 % of the time — **P = 0.030**. The shipped arm's equivalent mode holds 4 of 45 and the head
serves from it **0 of 9** times. So the head is not ordering randomly and is not broadly
mis-calibrated; **it is actively preferring the worst mode.**

| policy | ship | D1 fix |
|---|---|---|
| what the head picks (rank 0) | 0.782 A | **1.245 A** |
| picking at random | 0.815 A | **1.079 A** |
| a perfect selector | 0.679 A | 0.629 A |

**In the corrected-trunk arm the head selects worse than a coin flip**, 1.245 A against 1.079 A,
against this target's own **0.324 A** seed floor. The mechanism candidate is D24.

**PASS 90 REFRAME, from data already in `perf/of3t_pairbias/fold_rmsd.json`: the head's ORDERING
IMPROVES under D1's fix. What changes is the sample distribution.** That file carries, per seed
and arm, the five per-sample RMSDs in the model's own ranked order, which measures the ordering
directly (`perf/of3t_orchestrator/revision/d10_ordering.py`, `d10_ordering.json`). Five samples
per seed, so a uniformly random selector picks the best 1 time in 5 and sits at mean rank 2.0:

| arm | picks best | mean rank of best | mean regret | within-seed spread | mean best | mean rank 0 | mean pLDDT |
|---|---|---|---|---|---|---|---|
| shipped | **1/9** | **2.56** (worse than random) | 0.102 A | 0.284 A | 0.679 A | 0.782 A | 0.7163 |
| D1 fix | **3/9** | **1.22** (better than random) | 0.616 A | **0.971 A** | 0.629 A | 1.245 A | 0.8011 |

**So "the corrected trunk changed its calibration" is the wrong half of the story.** The head
ranks *better* with the fix — mean rank of best 2.56 to 1.22, picks-best 1/9 to 3/9 — and the
shipped arm is **worse than random**. What the fix changes is **variance**: the within-seed
spread more than triples, 0.284 A to 0.971 A, and the samples go bimodal. Seeds 4, 5 and 6 each
produce a ~0.56 A structure and a ~1.60 A structure, with best values 0.566 / 0.566 / 0.560 and
rank-0 values 1.597 / 1.603 / 1.592 — near-identical across seeds, so these are two discrete
basins rather than a continuum (compare `b2z2-seed-basin-flips-under-any-bf16-perturbation`).

**The defect is therefore not a regression to undo.** The head has always been a weak ranker; it
was invisible because shipped samples are interchangeable, a 0.284 A spread against a 0.324 A
seed floor, so picking randomly cost 0.102 A. D1's fix produces a genuinely better mode — 0.560
to 0.566 A, better than anything the shipped arm reaches, whose best over nine seeds is 0.646 A
— and a worse one, and the weak ranker now costs 0.616 A. **D1 made the model better and made
the selector matter.** D10's own sentence "a worse sample distribution masked a bad selector" was
right; the clause about calibration next to it was not, and the record said both.

This narrows the remedy. Restoring the old calibration is not the goal and would throw away the
better mode. What is needed is a selector that separates two well-separated basins — which is an
easier problem than general calibration, and the head's ordering signal is already better in the
arm that needs it. **What the artifact cannot answer, and needs a card:** per-sample confidence
values were not stored, only per-run aggregates, so ordering-vs-calibration-vs-selection-rule
cannot be fully separated without re-folding. That is `of3t-confhead` deliverable 1.


On **6 of 9 seeds** the confidence head selects a **1.59 A** sample while a **0.56 A** sample
sits in the same batch of five. pLDDT rises over the same runs (**0.7162 -> 0.8014**), so the
head is more confident and no better at ranking — the corrected trunk changed its calibration.

This was invisible while the trunk bias was wrong, because a worse sample distribution masked a
bad selector. It is now the blocker on D1: **fix the ranking, then re-measure the fold delta**,
rather than shipping a trunk fix that improves sampling and degrades what is served.

### D15. Instrument C gated the optimizer check on OUR schedule alignment, not upstream's. FIXED pass 37.

Found by `of3t-orchestrator` when `of3t-updaterule` landed D11 and the instrument's two arms
**swapped places**: the gated arm went from 7.586e-08 to **1.499e-03** and the "info" arm went
the other way.

`instrument_c_optim.py` ran both alignments and printed both numbers, so nothing was concealed
-- but the **pass criterion** was our own convention, `lr(k)` read after the counter moves. The
reference was driven with the lr sequence our code produces, so a §5 PASS asserted that Adam's
arithmetic matches given a matching rate, and could not have caught a one-step schedule offset.
It did not catch D11; D11 was found by reading upstream.

Upstream's order verified on their own code rather than inferred: a real `torch.optim.Adam`
driven by a real `AlphaFoldLRScheduler` (built `last_epoch=-1`, so `_LRScheduler.__init__`
steps it once to 0 before training) reads `param_groups` lr **0.000e+00, 1.800e-06, 3.600e-06,
5.400e-06, 7.200e-06** at updates 1..5. Their update k runs at **lr(k-1)** and their first
update runs at exactly **0**.

The gated arm is now upstream's alignment; the other is kept, renamed
`of3_schedule_offset_by_one`, to size the offset. On a branch without D11's fix the corrected
instrument reads **FAIL at 1.499e-03**, which is the truth about that branch.

---

### D18. BUNDLE-MIN was taped in train mode, so the reference carries a dropout mask nothing can reproduce. FIX IN FLIGHT — the rebuild reproduces bit-identically; publication pending.

Found by `of3t-gradients` (pass 40) with the one probe that identifies it: the same captured
block boundary, run twice with dropout disabled.

The Pairformer block carries one `Dropout` at rate **0.25**, and its mask comes from generators
an RNG-state snapshot does not reproduce across devices (`torch.randn` and `random.random`, not
the ones the snapshot holds). Two seeds on the identical boundary: worst **1.167**, median
**0.550**, **47 of 57** over the 0.05 bar; a second pair worst 2.419, median 0.481. **Dropout
off, twice: 0.000e+00 worst and median over 57 of 57, bit-identical** — which is what makes
dropout the whole of the explanation rather than a candidate for it.

**So the artifact's own draw-to-draw floor sits an order of magnitude above the bar it would be
judged at**, and a per-parameter comparison against it measures a mask. Their replay of the
bundle's own block differs from the bundle by median **1.096** in train mode and **1.065** in
eval — the published gradient is one draw, and no mode reproduces it.

**No tolerance was touched.** A bar is not wrong because a reference is noisier than it
(PROTOCOL A13).

Their FD validation stands: it restores the same state on the same device per evaluation, so
the mask is fixed inside the run. **A reference can be internally consistent and still be
unreproducible from the outside, and only the second property makes it a reference.**

Fix: `Dropout` in eval when the bundle is taped — one line in `bundle_min.py`, `of3t-reference`'s
file — then republish and re-hash.

**Pass 55, observed live on the rented A100 (not yet committed).** Dropout is pinned off inside
`bundle_min.build` and the rebuild now passes **A13's own detector**: two independent float64
tapings of the same step at `num_recycles = 0`, with determinism pinned
(`CUBLAS_WORKSPACE_CONFIG=:4096:8` and self-drawn draws), are **4,147 of 4,147 bit-identical** —
worst rel L2 **0.0**, median **0.0**, 0 over the 0.05 bar.

**And the same pair without determinism pinned is the measurement of what that pinning is
worth**: only 9 tensors bit-identical, **55 of 4,147 over the 0.05 bar**, worst **1.985** on
`blocks.26.attn_pair_bias.layer_norm_z.bias` — with the **median at 2.63e-14**. Not broad
drift: a handful of near-zero-gradient tensors where cuBLAS reduction order is the whole
signal. **Any comparison against this reference must pin determinism or it will see 55
spurious failures**, and they will land on exactly the tensors A14's denominator floor is
about. That row had concluded; it is **reopened** for this.
`tt_bio/train/lora.py:46` records that our tape has no dropout op, so our side already computes
the r = 0 function, which is why the comparison becomes legitimate the moment theirs does too.

---

### D20. The diffusion path is now 100 % taped, forward AND backward, and all 870 reachable weights carry a gradient. COVERAGE CLOSED (`of3t-diffusion`, pass 71). Instrument A at model scope is now a measurement nobody has run, not a port nobody has written.

Found by `of3t-gradients` (pass 59) after D14 closed and the memory explanation ran out.

**Instrument A at model scope is not blocked by L1, by memory, or by the reference.** A taped
trunk cycle fits crop 384 on one p300c. The obstacle is that **tt-bio wires no OF3 training
forward past the trunk**: the diffusion module (**89.2106 %** of the reference's squared
gradient norm, 761 tensors) and `aux_heads` (**2.8431 %**) are not on a taped path, so a
model-scope gradient *has nothing to come from on our side*. Together that is **92.05 %** of
the magnitude the claim is about.

**PASS 71 — CLOSED on coverage.** Strict forward **and** backward end to end:
**2,456 of 2,457 = 100.0 %, 0 missing**, and **all 870 reachable device weights carry a
gradient** where the survey arm had landed 2. Before this, strict mode stopped on the 7th call
of the forward.

**Both fixes are in the tape, as classes, not as OF3 cases** — the standing UNIFIED rule, and
here it pays immediately: **25 `ttnn.embedding` call sites across 6 models** were blocked by
the missing entry; **5 of 25 are now proven to route** (the OF3 diffusion module, decoder and
atom transformer) and the other **20 are unblocked rather than verified**, which is the honest
way to say it.

1. **`ttnn.embedding` has a tape entry.** A gather by row index whose gradient is a
   **scatter-add**, because many rows read the same entry — *"a closure that assigned would be
   exactly right on a permutation and silently wrong on the real one"*, so the test drives
   duplicates and asserts the **accumulated** value (8.0 on 8 rows per entry) rather than a
   tolerance around it. `ttnn.embedding_bw` carries two constraints its signature does not
   advertise: **rank-4 cotangent with both leading dims 1**, and **bf16 only** (fp32 refused
   outright) — neither costing accuracy, because every shipped call site already downcasts the
   table.
2. **A parent receives its cotangent in ITS OWN layout**, enforced in `add_grad`. Most of the
   tape is tiled end to end so this never surfaced; a row-major activation sends a row-major
   gradient up a chain of tiled ops and **the throw lands several closures from the cause** —
   it cost two debugging rounds exactly that way.

**`pad_dim`'s host round-trip closes additively**, and it is additive *because the
unconditional version was measured and rejected*: it moves the fold.

**The accuracy this buys, and its named limit.** Against float64 `index_add` at the shapes OF3
runs: **1.03e-2** at [96,384] from 608 rows and **1.35e-2** at [9216,128] from 77,824 rows,
both inside the 5.0e-2 per-tensor bar. The error is **the kernel's own bf16 accumulation, not
input rounding** — a two-term bf16 split of the cotangent moves 1.354e-2 to 1.344e-2 — and it
**grows with rows per entry: 6.2e-2 at 43, which is over the bar.** The one-hot-matmul escape
is recorded in the docstring. *So the backward is inside the bar at the shapes OF3 runs and
would not be at denser ones, which is a scope statement rather than a caveat.*

**PASS 70 — the census that made this a two-verb problem.** `of3t-diffusion`
ran the strict-mode coverage census the brief demanded before any port, and *the verbs are
covered*: **2,367 of 2,457 tensor-carrying calls route, across 18 verbs**, and the whole
shortfall is **14 calls in two verbs**:

| verb | calls | where |
|---|---|---|
| `ttnn.embedding` | 11, at 5 sites | the atom←token and pair-block broadcasts. The **table** is the taped activation and the indices are raw, so the backward is a **scatter-add**, not a no-op |
| `ttnn.to_torch` | 3 | all `tenstorrent.py:1042` — `pad_dim`'s host fallback, round-tripping a taped activation through torch |

**And it fails LOUDLY.** Strict mode stops on the 7th call of the forward at
`openfold3_diffusion_module.py:105` and the tape **raises rather than unwrapping** — so there
is no silent gap on this path, which is the D6/R11 failure mode and the thing worth checking
first. §6's other half is clean too: **0 function-local `import ttnn`** across the six
diffusion modules.

**Measured on the configuration production actually runs**, which took finding: the fold
builds the diffusion module under `device_dtype_override(float32)`
(`openfold3_fold.py:205`, `OF3_DIFFUSION_FP32_DEVICE` default-on), so a bf16 census would have
measured a path we do not ship. Both arms agree on the gap — **fp32 2,367/2,457 = 96.3 %**,
bf16 2,345/2,421 = 96.9 %, **same 14 calls, same two verbs**.

**Two numbers for whoever measures leaves here.** 870 device weights are reachable from the
module and **only 272 came from the loader** — `_w_tt` transposes every weight on host and
caches it, so 598 of the tensors the forward multiplies never pass through
`Module.torch_to_tt` and a leaf count taken there is **69 % short**. And the survey-backward
arm lands **2 of 870** weights with a gradient: the lower bound a severed chain predicts,
which confirms the gap is load-bearing rather than cosmetic.

*So the ceiling is real but the remedy is small and named.* The claim "none of it is on a
taped path" is withdrawn; what is true is that **two verbs** stand between this campaign and a
model-scope gradient.

*(Figures restated pass 69 against the republished r = 0 reference, global norm **3.707776**.
Against the withdrawn train-mode tape they read 88.54 / 3.57 / 91.9 %. **Every share in this
campaign is a share of a particular gradient**, and when the gradient was replaced they all
moved — which is why the shares are recomputed from `reach_by_norm.json` rather than carried
forward.)*

**This reframes the whole endgame** — and my first wording of it, one pass old, was wrong and
is corrected here. I wrote "code that does not exist yet". **The modules exist and run**:
`openfold3_diffusion_module.py`, `openfold3_diffusion_transformer.py`,
`openfold3_atom_transformer.py`, `openfold3_sample_diffusion.py` and
`openfold3_diffusion_decoder.py` are all on device and are what produces a fold today,
bit-exact against upstream's `diffusion_module`.

What does not exist is a **taped TRAINING path** through them. Upstream's training step
differentiates 48 noised structures per step through `diffusion_module`; our device code
*samples* through it. Two different call patterns, and only the sampling one is wired.

And the tape's coverage of those verbs **has never been measured**: `of3t-tape`'s censuses
cover trunk (11003/11003), MSA (1095) and template (338/346) and stop there, and
`of3t-gradients`' §6 census lists `diffusion_rollout` as NOT COVERED with the reason *"the
rollout is a differentiated path through the diffusion module and needs a model forward, which
this census does not have"*.

So the remaining work is **not** a port from nothing: it is (i) a coverage census of the
existing device modules under the tape, then (ii) a taped training forward in the noised-batch
call pattern. The trunk needed R11-class fixes before it could be differentiated at all — a
`MUL_UNARY_SFPU` score scale no other model took — so (i) is the honest first deliverable and
it is cheap.

**The next runnable rung, named by the row**: the whole 48-block pairformer stack driven by a
captured trunk-output cotangent — one CPU float64 replay to bank the boundary, then one ~870 s
device run. That rung is **5.8282 %** of the squared norm. It is a real advance on block 0's
**0.12631 %** and it is still not model scope. **It has since been run** — see the stack-scope
ceiling in EVIDENCE: 2,496 of 2,736 tensors, both arms above the zero model's 1.0.

---

### D19. Our forward and the reference's disagree at 6.735e-03, and that costs a 6.4e-02 median on the gradient. UNFIXED — it is a FLOOR, measured, not a footnote.

**PASS 91: D19 is the trunk half of D23 and its corrected arms are in flight.** The diffusion half
of the same cause is now measured and closed at block 0 — 2.07e-02 to 3.448e-03 per block, 1.592e-01
to 2.168e-02 over 24 — so the mechanism is demonstrated, not merely argued. D19's own corrected
arms need the 0.4.3 block boundary, which is captured from the float64 gradient rebuild `of3t-rebase`
has running. The prediction is pre-registered and unchanged: the **shipped** arm falls from
2.792e-01 toward the bf16 composition floor while the **`tb-off`** arm, at 4.965e-02 against 0.5.0,
gets worse. **Both arms moving the same way refutes D23's trunk half**, and the row is instructed to
say so rather than reconcile it.


Found by `of3t-gradients` (pass 59), and it is a **floor under instrument A at any scope
against this reference**.

Loss, same step, `num_recycles = 0`, every recorded draw replayed:

| side | loss |
|---|---|
| ours | **1.6311432393241485** |
| theirs | **1.6422035029890711** |
| relative | **6.73e-03** |

A gradient comparison cannot be tighter than the forward it is taken at, so this bounds every
§3d number measured against this reference — and the median the row measured, **0.06224**, is
still over §3d's 2.0e-02 bar with it.

**PASS 74 — what the gap is worth, measured instead of assumed.** `of3t-diffusion` compared
**their** model against **their own** bundle entries at diffusion scope: median **6.444e-02**,
worst **4.900e-01** on
`diffusion_module.atom_attn_enc.atom_transformer.blocks.2.attention_pair_bias.layer_norm_a_q.linear_g.bias`,
with **491 of 738 tensors over the 5.0e-02 bar holding 61.54 %** of the compared squared norm.

**A 6.7e-03 forward gap is roughly a tenfold larger gradient gap.** So:

> **Until D19 closes, nothing compared against the bundle at this scope can read below a
> 6.4e-02 median** — and the §3d per-tensor bar is 5.0e-02. The floor is above the bar.

That is not a caveat on a result, it is a statement that the measurement cannot be made that
way at all. The row's response is the right one: because `diffusion_module` parameters appear
nowhere else in the graph, the loss gradient with respect to them **is** the module-local
function driven by the cotangent at its output — so capturing `(kwargs, xl_out, dL/dxl_out)`
turns **89.2106 %** of the squared norm into a **self-contained float64 reference with D19
entirely upstream of it**.

**It has already halved once, which is evidence the dropout fix was real.** Against the
**published train-mode** bundle the same capture read `their_loss` **1.6591175475821072** and
a relative of **1.686e-02** (`capture_trunk_boundary_nodropout.json`). Against the **rebuilt
r = 0** reference it reads 1.6422035029890711 and **6.73e-03**. So eval-mode taping removed
more than half the gap and something else remains.

**A hypothesis with a decisive test, offered rather than asserted.** Our 1.6311 is a **device
bf16** forward (330.94 s on qb2); theirs is float64. The residual 6.73e-03 is the same order
as the block-0 forward agreement measured on real tokens — **s 8.061e-03 / z 7.979e-03** — so
"this is our device precision, not an unmatched input" is live. It is also exactly the kind of
plausible story this campaign has been wrong about before, so it gets a discriminator instead
of a conclusion:

> Replay the **same captured boundary** through **our own** stack in float64 on CPU and compare
> to our device bf16 number. If they differ by ~6e-03, the gap is our precision and D19 is a
> **floor** rather than a defect — the standing rule is that bit-exactness is not required and
> the accuracy bar is. If our own two precisions agree to ~1e-10, the gap is an input or
> implementation mismatch and stays with the reference recipe.

Neither answer is assumed here. `of3t-gradients` holds the captured boundary and can run both
arms; `of3t-reference` owns the recipe if the second answer wins.

**One candidate eliminated rather than left open**: the diffusion noise levels are *not* an
unreplayed input. `draws_recycles0.pt` records `noise_level` [1,48] separately, but the same
values arrive through the recorded `randn` draws at shapes (1,48) and (1,48,422,3), because
`model.py:481-483` derives `t` from `n`.

---

**RESOLVED AGAINST ITSELF, pass 82 — the hypothesis above is REFUTED, and this entry should
have said so twenty passes ago.** The discriminator it asks for had already run, at stack scope,
and the answer is recorded in A16 and on the scoreboard but was never brought back here:

> one block's masked-z error of **7.811e-03** composes to **2.792e-01 over 48 blocks** —
> **near-linearly**, not as the `sqrt(48)` that rounding alone predicts, and **39.7x and 95.2x
> above upstream's own replay floor.**

Random rounding error composes as `sqrt(depth)`; `sqrt(48) x 7.811e-03 = 5.4e-02`, and the
measurement is **five times that**. Near-linear composition means the per-block errors are
**correlated** — the signature of a systematic bias, not of a precision floor. Combined with the
disagreement sitting two orders above upstream's own replay of itself, **"this is our device
precision" is dead.** D19 is a **defect**, and the standing rule that bit-exactness is not
required does **not** absolve it: an accuracy bar is still a bar, and this one is missed by a
mechanism rather than by arithmetic.

**Why this correction is filed as a finding and not a tidy-up.** I read this entry during pass
82, believed its hypothesis was open, and spent most of a pass designing a control — their model
in bf16 against their float64 self — to settle a question the campaign had already settled. The
refuting number was in `PROTOCOL.md`, in `EVIDENCE.md`, and in my own state doc. It was not
here, in the entry a reader goes to when they want to know what is wrong. *A defect entry that
states a live hypothesis while the evidence file already refutes it is worse than one that says
nothing, because it actively spends the reader's time.* The audit now checks for exactly that
shape (see `audit_evidence.py`, `open hypothesis in an UNFIXED defect`).

### D17. The parameter bijection reached 6.5 % of the reference gradient's magnitude. CLOSED, passes 41-42 — 98.01 %.

Found by `of3t-updaterule` (pass 39) by profiling the published float64 reference per tensor
against the bijection, on the artifact, CPU only, sha256 verified first.

`of3t-equivalence`'s bijection maps **3,275 of 4,147 tensors — 79 % by count**. Those tensors
hold L2 **0.9997** of a model total **3.9083**, which is **6.54 % of the squared gradient
norm**. The count is the flattering denominator; the norm is the one a gradient claim owes
(K29, in the gradient's own currency).

The unmapped 872 are concentrated exactly where the magnitude is: `diffusion_module` L2
**3.6776** over 738 tensors, `input_embedder` 0.4361 over 98, `aux_heads` 0.7380 over 16. The
single largest tensor in the whole reference —
`diffusion_module.…blocks.5.attention_pair_bias.layer_norm_a.layer_norm_s.weight`, L2
**1.4108**, 36 % of the total on its own — is unmapped, and the largest MAPPED tensor is
**5.6x smaller** at 0.2518.

**Why this is a defect and not a scope note.** Instrument A at model scope is the campaign's
central claim, and as instrumented it would compare the tensors carrying **6.5 %** of the
gradient's magnitude while reading as "per-parameter gradient agreement". The bijection is
out of scope for those modules because they need a card to construct, so this is a task rather
than a wall. Routed to `of3t-gradients`, which holds the card and owns instrument A; its brief
is amended to report reach **in gradient norm** and to extend the bijection over
`diffusion_module` where a card makes it constructible.

**CLOSED to 94.44 % within one pass** (`of3t-gradients`), on the hash-verified artifact in
float64, global norm 3.908301894520 reproducing the manifest to every printed digit:

| bijection | tensors | by count | **by squared norm** |
|---|---|---|---|
| K22 tracer, through the CP | 3,275 / 4,147 | 79.0 % | **6.54 %** |
| this row's device bijection, pass 41 | 3,497 / 4,147 | 84.3 % | 94.44 % |
| **+ one call to the confidence head, pass 42** | **3,545 / 4,147** | **85.5 %** | **98.01 %** |

**Five points of tensor count, fourteen times the reach** — because OF3's gradient mass is not
spread over its tensors: `diffusion_module` **88.54 %** of the squared norm over 738 tensors,
`pairformer_stack` **5.27 %** over 2,736, `aux_heads` 3.57 %, `msa_module` 1.27 %,
`input_embedder` 1.25 %. One tensor is **13.03 %** on its own. Building the shipped model on a
card IS the extension the amendment asked for; the module was out of scope only because it
could not be constructed.

**What this puts a ceiling on.** Pairformer block 0 — the scope instrument A has actually run
at — is 57 of 4,170 tensors holding **0.12631 % of the squared norm** against the 0.4.3
reference, and the **whole 48-block trunk is 5.8282 %**. *No trunk-scope instrument can be a statement about the gradient's
magnitude, whatever its per-tensor result.* That is now the governing scope note on every §3d
number this campaign has produced.

**The last big gap was one tensor, and naming it was enough to close it.** Pass 41 reported
`aux_heads` at 3.57 % over 68 tensors, of which `aux_heads.distogram.linear.weight` was
**3.57 % by itself** — lazily materialised on the confidence head's first call, which the
64-token materialising forward never made. Pass 42 makes that call. `aux_heads` is now at
**0.0 % unreached**.

Still unreached, **1.99 % in total**: `input_embedder` 1.17 % over 93 tensors,
`diffusion_module` 0.74 % over 189, `pairformer_stack` 0.07 % over 240, `msa_module` and
`template_embedder` under 0.003 %.

**Two producer defects the row found on the way and fixed rather than noted**:
`instrument_full_model.py` truncated a list to 60 entries beside a count of 610, so the first
version of this measurement computed reach over 60 missing tensors and read **96.43 %** —
`reach_by_norm.py` now refuses any list whose length disagrees with its own count; and the
instrument was pinned to file names `of3t-reference` had renamed, so it now follows
`MANIFEST.json` and hashes the presence pattern of the **validated** gradient rather than the
withdrawn random-init one.

---

### D16. Three of five models still drop the entity weighting after D12's fix. CLOSED and verified (`of3t-entity`, pass 43).

Found by `of3t-updaterule` (pass 39) in its own MODELS census, which is why the census is a
deliverable rather than a formality.

D12's fix passes `is_dna` / `is_rna` / `is_ligand` through, and OpenFold3's vendored upstream
featuriser emits all three. **Protenix-v2, Boltz-2 and BoltzGen emit `mol_type` instead** — the
same fact under another name — so the adapter receives None for all three and the weighting is
still dropped on every nucleic-acid or ligand sample those three train on. `without` fires with
all three names on each, which is the fix's reporting half doing its job. On a 24-token
synthetic batch with 2 ligand tokens the weighting is worth **0.2721 on the value and 0.7926 on
the gradient seed**.

AF2 carries no molecule-type feature at all: it folds proteins, the weighting is vacuous, and
firing `without` there is correct.

The remedy is a `mol_type` → three-flag adapter in one place, not three featuriser edits
(**UNIFIED, NEVER PER-MODEL**).

**CLOSED: one derivation, one site, five models covered and each measured.**

**And my framing of the defect was wrong in a way the row corrected.** Upstream Protenix has no
integer `mol_type` feature at all — its featuriser emits `is_protein`/`is_ligand`/`is_dna`/
`is_rna` directly off a string annotation (`protenix/data/core/featurizer.py:542-552`). **The
integer column is our port's own encoding**, and it follows OpenFold3's enum rather than
ByteDance's. "Protenix-v2 emits `mol_type`" is a fact about us, not about them.

**The three stacks do not agree, and the adapter takes the disagreement as input.** Measured by
EXECUTING each stack's own parse/tokenise/featurise path on the same protein(10)+DNA(8)+RNA(8)+
ATP complex in the same chain order, so the columns compare elementwise — every stack matched
its own declared source:

| convention | protein | rna | dna | ligand | who |
|---|---|---|---|---|---|
| `af3` | 0 | **1** | **2** | 3 | OpenFold3 (`MoleculeType`), Protenix-v2 (our port) |
| `boltz` | 0 | **2** | **1** | 3 | Boltz-2, BoltzGen (same `const` module) |

Both put protein at 0 and ligand at 3; **they swap DNA and RNA**. The two columns differ on
**16 of 57 tokens, all nucleic acid** — and the loss is identical anyway, because upstream
weights DNA and RNA both at 5.0. *That equality is what makes an unnamed convention safe, and
it is checked rather than assumed*: `entity_flags` reads `losses.mse`'s signature and refuses,
naming both conventions, the moment `w_dna` and `w_rna` differ.

**Deltas, each labelled REAL or SYNTHETIC rather than blended.** OpenFold3 is the only real one
(frozen 5nw3 batch): dropping its native flags moves value 0.2334 / seed 0.7637 — D12's number
re-measured — and handing the same fact back as a `mol_type` column reproduces the native loss
and seed **bit-identically** under both conventions and under none. Protenix-v2 and Boltz-2,
real `mol_type` columns with synthetic coordinates, 57 tokens of which 31 ligand: value
**0.8697**, seed **0.9009**. BoltzGen: **0.8651** / **0.8981**. AF2: **vacuous, and measured** —
0 molecule-type keys among 83 distinct string keys across its four modules, and an all-protein
column is bit-identical to the bare batch, so `without` firing there is the correct report.

**The control predicted its own number before measuring it.** Ligand re-pointed at the RNA
class (1+5.0 instead of 1+10.0) — breaking the MAPPING, not the plumbing — on Boltz-2's own
column with 31 ligand tokens asserted non-zero. With the weighted Kabsch frozen the result is
closed form: **measured value ratio 0.628828190098 against predicted 0.628828190098, relative
error 1.77e-16**, per-token seed ratio equal to the weight ratio to 2.22e-16. Unfrozen, 0.3712
value / 0.4324 seed.

The convention table is **re-derived from all three live sources by `tests/test_entity_flags.py`
rather than pinned**, so an upstream reordering fails a test instead of silently retraining
nucleic acids at the wrong weight — and the table gate was watched failing (flipping the boltz
row breaks 2 of 6).

---

### D14. A taped OpenFold3 cycle now trains at crop 384, the smallest crop upstream's recipe uses. CLOSED (`of3t-l1`, pass 51, VERDICT GO). The ladder stops between 384 and 640.

Escalated 2026-09-19 from "an L1 clash at 384" once `of3t-tape`'s five fixes removed the
activation wall. Measured on the composition at `249cba371`, qb2 p300c, AICLK 1350 during:

| crop | result |
|---|---|
| 128 | **L1 clash** in `minimal_matmul`, L1 buffer 1114112 vs CB region end 1159680 |
| 256 | **L1 clash**, 262144 vs 307712 |
| **384** | **DRAM OOM** in `transition_z`'s swiglu — **25165824 B wanted, 1079616 B free** |
| 384, `--cycles 1` | identical OOM, same allocated byte count |
| 384, `--clear-caches` | identical OOM, same allocated byte count |

Their training crops are **384 / 640 / 768**. A taped backward runs at 76 tokens; **the L1 wall
appears between 76 and 128**, and the smallest crop that matters runs out of DRAM.

**Where it actually breaks, measured pass 32:** at 384 the taped cycle exhausts all **34.217 GB
across 4977 live DRAM allocations**, and **the peak is set inside the MSA module before the
template stack or the 48-block trunk has run**. Only one of the three block stacks had been
offered to `ops.checkpoint_segment`; the other two now are. Everyone including this document had
been saying "the taped trunk" — the trunk is not where it breaks.

**Two confounds killed by measurement, so they need not be re-tested.** Not the `no_grad`
prefix: `--cycles 1` removes it entirely and the failure byte count is identical. Not warmed
caches: `--clear-caches` freed 216 tensors from 324 dicts for 31.0 MB against a **~2.7 GB**
shortfall. And not a single placement flag — `TT_BIO_RESIDUAL_L1=0` reproduces an identical
clash at identical addresses, with three further flags changing nothing.

**Pass 35 progress:** `evict-on-read` **clears the circular-buffer clash at 384** — backward L1
holders drop to three, all non-evictable, none at a clash address — at a named cost of DRAM
21.003 GB / 4974 live allocations against 15.623 GB / 4653 before. **The wall moved rather than
fell**: the backward now fails on `ttnn.sum` in a broadcast-add backward raising
`num_shards <= num_cores`, **512 shards along height against 64 cores**. A sharding constraint,
not a capacity one, and DRAM is no longer binding at this rung. 128 and 256 have not been
re-measured against this lever.

**CLOSED, pass 51.** A taped OF3 trunk cycle completes forward **and** backward at crop 384 on
a single Blackhole p300c, with a **bit-identical gradient** and **byte-identical inference**.
Before this row, 384 exhausted all 34.22 GB of the card inside the MSA module and produced no
gradient at all.

| crop | forward | backward | backward DRAM high-water | live allocs |
|---|---|---|---|---|
| 128 | PASS | **PASS** | 2.522 GB | 5581 |
| 256 | PASS | **PASS** | 7.384 GB | 6249 |
| **384** | PASS | **PASS** | **17.983 GB** | 6514 |
| 640 | PASS | FAIL | 34.215 GB | 6016 |

384's backward went **32.370 GB → 17.983 GB**, which is what made it fit; the ladder now stops
between 384 and 640.

**PASS 97 — "stops between 384 and 640" understates it, and the arithmetic had never been done.**
640 failing at **34.215 GB of a 34.22 GB card** reads like a 5 MB miss. It is not: 34.215 GB is
the high-water **reached before it died**, which is the card ceiling, and 640's live allocation
count of **6016 is LOWER than 384's 6514** precisely because it never got to its own peak.
Fitting the three passing rungs (`perf/of3t_orchestrator/crop/crop_ladder_fit.py`):

| fit | crop 640 | crop 768 |
|---|---|---|
| `a + b·N²` through 256 and 384 | **51.9 GB, 1.52x the card**, short by **17.68 GB** | **75.2 GB, 2.2x**, short by 41.0 GB |
| `a + b·N²` through 128 and 384 | 48.9 GB | 70.2 GB |

**So crop 640 needs roughly half a card again, and 768 needs over two cards.** No modest memory
lever closes 15-18 GB. Stated as an estimate rather than a measurement: these are two-point
extrapolations under an `a + b·N²` model, and the pairwise exponents differ across the ladder
(**1.55** from 128→256, **2.20** from 256→384), so the constant is soft. What is robust is the
order of magnitude, and it says 640 is out of reach rather than nearly in it.

**What that leaves, honestly.** Splitting one model across two chips would fit 640 in their
combined 68.4 GB and is **forbidden on this fleet**, so more chips is not a path. The one legitimate lever is **deeper
activation checkpointing** — more recompute for less memory, which trades time and is not "doing
less of the model's own work". `ops.checkpoint_segment` already covers the three block stacks
after `of3t-l1`; whether finer segmentation can find 15-18 GB at 640 is **unmeasured and
unowned**. Until it is, the campaign's demonstrated training scope is **crop 384, one of
upstream's four stage configs**, and that is a scope bound to state rather than a pending item. **1,639 of 2,531** declared trunk weights carry a gradient at every
passing rung — the same count at all three, which is the consistency check.

**And the recorded clash was never the defect.** D14's original signature —
`triangle_multiplication_start`'s `minimal_matmul`, L1 buffer 884736 against a static CB region
ending at 1159680, reached from the template stack — **could not be reproduced on the
composition at all**, because `wk/of3t` carries R19's L1-residency fix landed after `of3t-perf`
took those probes. The taped trunk never reaches that site; it died earlier, on **DRAM
exhaustion inside the MSA module**, 34.217 GB of a 34.22 GB card across 4,977 live allocations.
Seven changes closed it, "each one hiding the next" — the first being that
`ops.checkpoint_segment` was offered at **one of the three** pairformer-shaped stacks, and the
MSA module's four blocks exhaust the card on their own.

*A defect recorded with a precise signature can be superseded by an unrelated fix and keep its
name.* The signature was current when it was written and stale when it was acted on.

**So the retention is the taped cycle's own activations**, which is the tape's retention policy
and the memory ladder jointly. Owner `of3t-l1`. Until it clears there is no OF3 training run at
any size upstream trains at, and the model-scope gradient comparison and every perf number sit
behind it.


**PASS 41 — the picture is now a ladder, and the wall is fragmentation.** One taped cycle per
rung on qb2 card 0, p300c, AICLK median 1350 sampled DURING:

| crop | forward | backward | live DRAM allocs (bwd) | |
|---|---|---|---|---|
| 128 | 1.696 GB | **3.930 GB** | 5910 | **PASS, trains end to end** |
| 256 | 2.930 GB | **14.755 GB** | 6596 | **PASS, trains end to end** |
| 384 | 4.795 GB | 32.370 GB | 5922 | FAIL |

Both units at every rung, and they disagree the other way from R16: 128 → 256 grows **3.75x in
bytes and 1.12x in allocations**, so the backward tracks the pair tensor at N² with a flat
count. **The 384 refusal is 1,207,959,552 B against a largest contiguous block of 1,204,698,624 B
with 2.371 GB still free — a FRAGMENTED ceiling, 3.26 MB short**, not a capacity one. That is a
different and far more tractable problem than "does not fit".

And the fits are honest: **172 of 172 weight gradients bit-identical (relative L2 exactly 0.0)**
against the composition immediately before the row, same set both sides; the one-bf16-ulp
negative control fails at 0.181; the three OF3 gradchecks reproduce their committed `grad_norm`
to every digit; inference byte-identical against a detached `origin/main` on the same card
across all three sha256 digests. Other four models unchanged, one measurement each.

### D13. `AdamW.check_displacement` raised on a correct short run. FIXED on `wk/of3t-gradients`.

A 20-step OF3 warmup runs at lr about `k x 1.8e-06`, putting **every step under bf16 spacing**,
so the device copy legitimately does not move and the cumulative ratio read **0.810 against a
(0.9, 1.1) band** — the fp32 master working as designed, reported as a failure.

`displacement()` now returns the displacement the device **cannot** show (half an ulp per
weight, `2**-8` for bfloat16) and the check reports instead of raising when the master has moved
less than that. Verified in three regimes: correct short run flagged `below_resolution` and not
raised; **a master that moved while the device did not still raises**; healthy long run reads
1.000.

Recorded as a defect rather than a tweak because a guard that fires on correct behaviour gets
disabled by the next caller, and then it is absent for the case it was built for.

### D12. `mse` drops upstream's per-entity loss weighting. FIXED and verified (`of3t-updaterule`, pass 39).

`objectives._TERMS['mse']` never passes `is_dna`, `is_rna` or `is_ligand` through to
`losses.mse`, so upstream's **5.0 / 5.0 / 10.0** entity weighting is silently dropped. Measured
on the frozen 5nw3 batch, which carries two ligand tokens: **the loss value moves 0.233
relative and the gradient seed 0.764**.

**The term fires either way, at the wrong weight.** A coverage check that reads the configured
weight passes this, and so does one that merely confirms the term contributed something — it is
visible only to a census that compares the gradient it seeds. Every nucleic-acid or ligand
training sample is affected.

**CLOSED.** The three keys are passed through and `losses.mse` — which had always implemented
the weighting — now receives them. Against upstream's own `mse_loss` at atom scope in float64
on the same batch: value **6.057e-16**, analytic gradient **3.662e-15** relative, worst element
2.159e-12. Their gradient was validated first by float64 central differences at the published
bundle's own h and sampling rule (median **5.657e-09**, worst 6.318e-07 over 18 entries against
the 1.1e-4 the reference itself was accepted at), with an h-sweep rising monotonically from
2.083e-09 at h=1e-3 to 1.092e-04 at h=1e-7 — the roundoff-dominated shape, not §3c-bis's
h-independence.

**The pass-through alone was not the fix.** Flags *present and identically zero* — a real
protein-only batch — give a **bit-identical value and seed** to flags *absent*. No numeric
check can separate "this batch has no ligand" from "this featuriser never produced the flags",
so on a protein-only batch the original defect is invisible to every number. `_OPTIONAL` now
names the labels a term silently computes a different loss without, and `af3_loss` records the
absent ones as `breakdown["mse"]["without"]`. That report is half the fix.

### D11. Our LR schedule is wired one step ahead of theirs. FIXED and verified (`of3t-updaterule`, pass 39).

`recipes.py:119` wires `af3_lr(s)` while `AdamW.step` increments before it reads, so **our step
1 runs at `lr(1)` where upstream's runs at `lr(0)`**. Measured at stack scope on the real model
with the update rule pinned to theirs: the shipped schedule phase reads **2.106 at k=2 against
0.515**, and **our `d_1` is 0.0139 where theirs is exactly 0**.

Every training step we take is therefore one rung further along the warmup than upstream's.
Found by the trajectory rung the protocol had written off as uninformative.

**CLOSED, and the fix was not where the defect was first described.** `af3_lr` was never
wrong: `AdamW.step` incremented its counter and then read the schedule. The fix reads at
`self.steps` **before** the increment (`tt_bio/train/optim.py`), verified against upstream's
real call order (`opt.step()` then `self.lr_schedulers().step()`,
`projects/of3_all_atom/runner.py:460-461`) rather than against a guessed `af3_lr(s-1)`, with
their scheduler loaded by path and hashed (`2ddcefaa…9bf3`, byte-identical in 0.4.3 and 0.5.0).

Three arms: the closed form over its whole domain, **1007 of 1007 points exact in float64**;
the rate each side's k-th optimizer step applies, **2005 of 2005 exact**, including k=1 at
**0.0 on both sides** and the warmup knee at k=1000/1001; and `d_k = w_k - w_0` over k=1..20 at
both the shipped warmup and §7a's scaled one, with **`d_1` exactly 0 on both sides in both
runs** and worst relative 2.052e-06 / 9.847e-07 against fp32 differencing floors of 3.087e-05
and 6.571e-06 derived from the dtype and the measured norms.

Negative control: restore the off-by-one and the rate arm fails at k=1 while `d_1` reads
1.440e-05 against their exact 0 (worst relative on `d_k` **2.170**) — and the closed-form arm
is **untouched and still passes**, which is what says the three arms measure separate things.

One fix for every model by construction, and censused rather than asserted: the package
contains exactly **one** `AdamW` construction passing a schedule (`recipes.py:118`) and exactly
one recipe.

### D2. AF2 receives no gradient at all. UNFIXED, out of this campaign's scope.

`of3t-leaves`, measuring each model's own pair stack from its own checkpoint on qb2 p300c:
AF2 reads **0 of 184** parameters with a gradient at 4 blocks, and **0 of 46** at 1 block.
Not a shortfall — a total absence. As things stand **AF2 cannot train**, and that needs its own
row before anyone claims AF2 fine-tuning works.

### D3. RFdiffusion3 cannot train. UNFIXED, out of scope, recorded so it is not lost.

`rfd3_bias`'s three entry points return a tensor rather than an optional and no fallback exists
(`of3t-tape`).

---

## Fixed on `wk/of3t`, unmerged

### D4. Weight discovery was blind to anything a module built for itself — on four models at once.

A parameter census hooked at the **loader** finds what the loader produced and nothing a module
derived in its own `__init__`. `TriangleMultiplication.__init__` concatenates `g_in`/`p_in` in
host torch, so the tensor its forward multiplies never passes `torch_to_tt`.

Measured by `of3t-equivalence` at its sharpest: on one triangle multiplication the taped backward
**completed with no error, the input received a gradient, and 0 of the module's 8 weights was an
autograd leaf.** It did not raise and it did not warn. Then by `of3t-tape` with a denominator:
**2119 weights found by the loader against 2531 reachable from the built model**, and
**2314 of 2531 carrying a gradient** once discovery walks the built model.

`of3t-leaves` then measured all five models rather than assuming: the loader is blind by the
**same 32-tensor shortfall** on OpenFold3, Protenix-v2, Boltz-2 and BoltzGen alike, because the
gap belongs to the shared classes. *"Protenix trains, so Protenix must be fine"* was refuted for
three models at once. Fixed in the shared path.

### D5. Our gradient clipping was not upstream's, in two independent ways.

Found by `of3t-orchestrator` against their real `compute_global_norm`, closed by `of3t-leaves`
against their real `grad_manager`:

- their global norm **excludes disabled parameters** and ours did not — clip **0.108 against
  0.662**, an 84 % difference in the factor applied to every gradient, and their runner disables
  the confidence head on **4 of the 5 datasets** in their own `initial_training` config.
  **8.333e-01 -> 1.989e-08.**
- their shipped default is **per-sample** clipping, which changes the *direction* of the
  accumulated update and not only its length — **1.957e-01 -> 0.000e+00.**

Honest scope note: per-sample clipping **has no caller** yet, because the shipped loop runs one
forward per batch. The rule is equivalent; the loop that would exercise it does not exist.

### D6. The tape had four silent blind spots.

All `of3t-tape`, all silent — no exception, no warning:

- `tape()` installs itself by rebinding the module-global `ttnn`, and a **function-local
  `import ttnn`** escapes it. 16 call sites in two OF3 modules ran untaped; the census then found
  **three more** (`train/mesh.py`, `train/tensors.py`, a kernel patcher), all verified outside
  any forward and allowlisted with reasons.
- `_Ttnn.__getattr__` decides "nested namespace" via `isinstance(attr, type(ttnn))` read off its
  own module global, so **a second proxy rebinding it** makes every `ttnn.experimental.*` verb
  come back raw. The symptom is a pybind `TypeError` several frames later, not a tape error.
- `triatt_qkv.py` declined taping at one entry point but not at the three fusions added to the
  same file later, one of which OF3's Pairformer reaches. Fixed **as a class**, not as three
  call sites.
- OF3's `fp32_softmax=True` path had **no backward at all** — a path Protenix never takes, which
  is why 331/331 coverage measured on Protenix did not transfer.

### D7. Two L1-residency gates `_l1_fits` does not reach, and a dangling provenance reference.

`_triangle_mul_memory_config` and `_pair_proj_config(out_l1=True)`: 214 MB held across six sites
in the MSA module, with the fourth block's QKV projection dying on a circular-buffer clash
(`of3t-tape`). And `NOTICE` cited `docs/openfold3-vendor.md`, which existed nowhere — found only
by composing the branches, since each looked fine alone, and closed by `of3t-data`.

---

### D8. The assembled pairformer block's pair-track gradients are an order of magnitude outside the bar. UNFIXED.

**PASS 90 RE-ATTRIBUTION: D8 IS LARGELY D23, and what survives is graded by DEPTH rather than by
attention involvement.** The `tb-off` arms already on the branch equalise our ending-node function
with the 0.5.0 reference's (the wrong side, but the mismatch is what is removed, so it is a valid
proxy). Recomputed with the campaign's own A14 rule and bars, not read from a summary
(`perf/of3t_orchestrator/revision/d8_vs_endnode.py`, `d8_vs_endnode.json`):

| arm | n | median | vs 2.0e-02 | over 5.0e-02 | over-bar norm share | worst |
|---|---|---|---|---|---|---|
| block 0, shipped | 52 | 0.0648 | FAIL | 36/52 | 23.7 % | 1.3208 `blocks.0.pair_stack.tri_att_end.layer_norm.weight` |
| block 0, tb-off | 52 | **0.0115** | **PASS** | **7/52** | **1.9 %** | 0.7918 `blocks.0.attn_pair_bias.linear_z.weight` |
| block 23, shipped | 52 | 0.0930 | FAIL | 46/52 | 64.1 % | 11.3914 `blocks.23.attn_pair_bias.layer_norm_a.weight` |
| block 23, tb-off | 52 | **0.0174** | **PASS** | 12/52 | 53.7 % | 11.1199 same tensor |
| block 47, shipped | 52 | 0.4270 | FAIL | 52/52 | 100 % | 2.9857 `blocks.47.attn_pair_bias.layer_norm_a.weight` |
| block 47, tb-off | 52 | 0.4004 | FAIL | 52/52 | 100 % | 1.4068 same tensor |

**The worst tensor in the shipped block-0 arm is `tri_att_end.layer_norm.weight`** — the ending
node, the one sub-module D23's trunk change modifies. The instrument pointed at the cause for
forty passes and the reading was "the pair track fails in composition".

**What this closes.** At blocks 0 and 23 the median goes FAIL to PASS once both sides compute the
same function: 0.0648 to 0.0115, and 0.0930 to 0.0174. D8's headline — every sub-module passes
alone while the assembled block reads 4.3e-01 to 1.4e+00 — is dominated by the reference
mismatch, not by composition.

**What survives, and it is real.** 7 of 52 at block 0 holding 1.9 % of the block's norm, 12 of 52
at block 23 holding **53.7 %**, and **block 47 barely moves** — 0.4270 to 0.4004, 52 of 52 over
bar under either arm. A median inside the bar with 53.7 % of the norm over it is not a pass, so
D8 is **reduced and re-localised, not closed**, and it stays UNFIXED.

**The grading changes, which redirects the hunt.** The standing lead was that the failure is
graded by attention and pair-track involvement, `single_transition` the only passer. With the
function mismatch removed the residual is graded by **depth** — block 0 nearly clean, block 23
partly, block 47 not at all — and the same tensor, `attn_pair_bias.layer_norm_a.weight`, is worst
at both 23 and 47. That is a different mechanism from the one D8/D9 were chasing, and the block
ladder rather than the sub-module ranking is where to cut next. D9's `fp32_softmax` 3.2x was
measured on the alone arm and R123 already showed the logit/value split does not survive
assembly, so nothing here revives it.

**Caveat, stated rather than buried:** the tb-off arm equalises on 0.5.0's side. The correct
configuration is our shipped flag against a 0.4.3 reference, which `of3t-rebase` produces. The
proxy answers "how much of D8 is the function mismatch" because equalising either way removes it;
it does not give the final numbers.


Every pair-track sub-module passes alone against a 0.05 bar — `tri_mul_out` **0.0172**,
`tri_mul_in` **0.0156**, `pair_transition` **0.0092** — and **the assembled block reads 4.3e-01
to 1.4e+00**, while the single track passes in the same run at 6.1e-03 to 6.4e-02
(`of3t-gradients`, block 2, 64 tokens, float64 reference, warmed probe).

Localised as far as it currently goes. **Triangle attention injects the error rather than
inheriting it**: the z-only cotangent moves the median from 8.630e-01 to 2.850e-01 with an
ordering that is *not* monotone in backward depth, which is what injection looks like and
amplification does not. The **in-place pair residual is refuted** — aliasing `ttnn.add_` to
`ttnn.add` process-wide leaves every figure bit-identical.

Until this closes, a model-scope gradient equivalence claim cannot pass for the right reason.

**PASS 47 — decomposed, by `of3t-orchestrator` from `of3t-gradients`' own artifact.** Block 0
against the bundle's boundary, 52 tensors (the one with ref_norm 1.4e-19 excluded per A14),
grouped by sub-module:

| group | n | median | worst | over the 0.05 bar |
|---|---|---|---|---|
| `tri_att_end` | 8 | **0.3838** | 0.952 | 8/8 |
| `attn_pair_bias` | 6 | **0.1470** | 0.831 | 6/6 |
| `tri_att_start` | 8 | **0.0865** | 0.3055 | 8/8 |
| `pair_stack`, the rest | 25 | **0.0680** | 0.101 | 17/25 |
| `single_transition` | 5 | **0.0212** | 0.0238 | **0/5** |

**The one sub-module with no attention and no pair-track coupling is the only one that passes**,
and the failure is graded by pair/attention involvement rather than by tensor size or depth.
That is mechanism-shaped, and it is the sharpest statement the campaign has about D8.

**PASS 85 — the start/end asymmetry is a property of COMPOSITION, not of the module.**
`tri_att_start` and `tri_att_end` are the same operation on different axes, so the ratio
between them is structural. Comparing **worst relative against worst relative** — the two
tables state different statistics and mixing them is how I first got this wrong:

| probe | what it measures | statistic | `tri_att_start` | `tri_att_end` | end/start |
|---|---|---|---|---|---|
| R36 (`bisect_grad.py`, block 2, 64 tokens) | each sub-module **ALONE** | median | 0.0950 | 0.0974 | **1.03** |
| R36, same run | | worst | 0.1389 | 0.1449 | **1.04** |
| pass 47 (block 0, crop 384, bundle boundary) | the same modules **INSIDE THE ASSEMBLED BLOCK** | median | 0.0865 | 0.3838 | **4.44** |
| pass 47, same run | | worst | 0.3055 | 0.952 | **3.12** |

The alone-arm medians are computed this pass from `perf/of3t_gradients/bisect_grad.json`, so
the comparison is now **apples-to-apples on both statistics**: ~1.0 alone on either statistic,
3.1x–4.4x assembled on either. The asymmetry is robust to the choice of statistic, which was
the one thing my first version of this entry got wrong.

**Alone the two are indistinguishable; assembled they are 3.1x apart.** That is not two probes
disagreeing — it is D8's own central finding showing up in a new place. Sub-modules that pass
alone fail assembled, and now we can add that composition does not degrade them *equally*: it
hits the ending-node axis about three times harder than the starting-node axis.

**Stated as an observation with a discriminator, not a mechanism** — the last four passes each
produced a plausible shape the data then refuted, and the two rows also differ in block (2 vs
0), crop (64 vs 384) and reference. So change one at a time:

  - **re-run the pass-47 decomposition at block 2, 64 tokens** — if the 3.1x survives, the
    asymmetry is a property of composition alone and is the sharpest handle D8 has had;
  - **if it collapses toward 1.0**, the asymmetry is depth- or length-dependent, which would
    connect D8 to **D19's near-linear accumulation through the stack** for the first time and
    would change the verdict's claim that they are two independent problems;
  - either way, **an axis-asymmetric error under a symmetric operation points at a reduction
    axis or a tile boundary**, not at arithmetic precision — which is testable against the
    shapes directly and is the one lead here that does not need the reference at all.

**Unowned**: `of3t-gradients` is concluded and `of3t-diffusion` is on the device arm.

**And D9's flag cannot explain it.** Projecting D9's own module-scope factors (tri_att_end
x0.362, tri_att_start x0.308) onto these tensors — generous, since it credits `fp32_softmax`
with fixing the whole triangle route to D9's measured level — moves the median **0.07813 →
0.07024** and the over-bar count **39 → 32 of 52**. Still a fail, and not close: tri_att is 16
of 52 tensors and the median is set by the other 36, which sit at 0.0687 on their own. A
first-order projection across two probes rather than a prediction of the flag-off run, but an
**upper bound**, and the bound fails.

### D9. `fp32_softmax` costs 3.2x on the triangle-attention weight gradient. UNFIXED; owner `of3t-gradients` (pass 47), and it is NOT the explanation for D8.

Same module, same probe, same reference, the flag alone: `tri_att_start`
**1.389e-01 -> 4.283e-02** and `tri_att_end` **1.449e-01 -> 5.239e-02** with it off, while the
forwards move only 2.610e-02 -> 2.923e-02 and 2.390e-02 -> 2.677e-02.

**A 3.2x gradient change under a 12 % forward change** — the shape of error a forward comparison
structurally cannot see. **Pass 47 bounds what it can buy**: projected onto block 0's
own tensors at D9's measured factors, the block median moves only 0.07813 → 0.07024 and 32 of
52 tensors stay over bar, so the flag is real and is **not** the cause of D8 (see D8). `openfold3_trunk.py:139` ships `fp32_softmax=True`. Turning it off is
an accuracy change to shipped inference and therefore release-gated, and the trade has not been
measured end to end: it buys gradient fidelity and costs forward fidelity, and nobody has priced
which matters more for a fold.

**PASS 85 — localised to the attention-LOGIT path, from the artifact, with no new run.**
Decomposing `bisect_grad.json`'s per-tensor values by where the tensor sits in the attention —
**logit path** (`linear_q`, `linear_k`, `linear_z`, `layer_norm`, n=5) against **value path**
(`linear_v`, `linear_o`, `linear_g`, n=3):

| case | logit path | value path | ratio |
|---|---|---|---|
| `tri_att_start` | **0.1098** | 0.0263 | **4.18x** |
| `tri_att_start_nofp32` | **0.0356** | 0.0294 | **1.21x** |
| `tri_att_end` | **0.1056** | 0.0246 | **4.29x** |
| `tri_att_end_nofp32` | **0.0460** | 0.0269 | **1.71x** |
| `tri_att_end_scaledbias` | **0.1125** | 0.0246 | **4.57x** |

**Three things this says that "3.2x on triangle attention" did not.** The error is not spread
across the module — the **value path is clean at ~0.025 and stays clean**, already well inside
the 0.05 bar, while the logit path is **4.2x–4.6x** dirtier. Turning `fp32_softmax` off moves
**only the logit path** (0.1098 → 0.0356, 0.1056 → 0.0460) and leaves the value path where it
was (0.0263 → 0.0294, 0.0246 → 0.0269, both marginally *worse*), which is what a
logit-path-localised cause looks like and what a module-wide precision story does not. And
`tri_att_end_scaledbias` at **4.57x** against `tri_att_end`'s 4.29x is R37's negative control
seen per-path: **scaling the bias does not touch it.**

**Caveats, because this is a re-analysis and not a new measurement.** Block 2, 64 tokens,
sub-modules **alone** — it says nothing about the assembled block, which is D8. The medians are
over 5 and 3 tensors. And the grouping is a judgement a reader may reject: `linear_z` is
counted on the logit side because it produces the pair bias that is **added to the logits**;
move it to the other column and the ratios soften but the direction does not change.

## Known and unfixed, lower severity

- **`ttnn.embedding`, 5 sites, no backward.** None is a table lookup; each replays a host gather
  on device so each backward is a scatter-add, and ttnn scatter/gather is rate-limited per
  element. A correctness gap and a perf risk in the same five sites.
- **9 `PairWeightedAveraging` weights carry no gradient** and are unexplained — deliberately not
  explained away by the row that found them.
- **111 of 176 confidence-head parameter gradients miss the §3d bars**, worst **1.0188e+00**,
  cause localised to bf16 backward arithmetic. Reported as the finding with no bar moved.

### D21. The device arm of instrument A runs, and its first reading is an instrument defect, not a result. UNFIXED — the forward discriminator has not been run.

**PASS 116 — D21 IS CLOSED AS AN INSTRUMENT DEFECT AND REPLACED BY A REAL MEASUREMENT, WHICH
FAILS.** `of3t-rebase` re-ran instrument A at diffusion scope against the rebuilt 0.4.3 reference
over **all 48 structures**, and the orchestrator verified every figure from
`perf/of3t_rebase/device_gradient_043all.json` rather than from prose:

| | |
|---|---|
| median relative L2 | **1.6588e-01** — **8.3x** over the 2.0e-02 median bar |
| worst | **1.8504e+01** on `diffusion_transformer.blocks.8.attention_pair_bias.layer_norm_a.layer_norm_s.weight` |
| over the 5.0e-02 bar | **474 of 547**, with **73 inside it** |
| weights with no gradient | **0** |
| compared set | 547 of 761 reference tensors, **51.14 %** of the model's squared gradient norm |
| zero-model baseline | 1.0 — so this is **6.0x better than a deleted model**, not at it |
| A18 discriminator | `forward_rel_median` **8.4748e-03**, inside the 5.0e-02 gate A19 fixed |
| reference floor | **bit-exact, 0.000e+00 over all 761 tensors** |
| structures | **48 of 48**, accumulation probe growing **26.68x** |

**The probe's growth is the check that the scope is now right.** 48 independent samples would grow
√48 = **6.93x** and perfectly correlated ones **48x**; the measured **26.68x** sits between, so
`backward` is accumulating across tape contexts rather than replacing, and the structures are
positively correlated as the same weights under different noise should be. Both conditions the
orchestrator named at pass 115 — full scope **and** a growing probe — are satisfied.

**So the verdict is FAIL against §3d, and it is a real failure rather than an artefact.** The
reference correction bought **4.62x** on the median and **4.75x** on the worst case. D21's
withheld 0.7672 and the 1-of-48 run's 0.9778 are both superseded: the first was a bijection
defect at 283 of 870, the second a scope defect at 1 of 48, and neither was a statement about the
port.

**One tensor is worst in all three measurements**, across two references and three scopes:
`diffusion_transformer.blocks.8.attention_pair_bias.layer_norm_a.layer_norm_s.weight` — **87.82**
under D21, **1.291** at 1-of-48, **18.50** now. A reproducible named worst case across independent
runs is the sharpest handle the diffusion gradient has, and no entry has pursued it.

**Minor record note:** the row's prose quotes A18 at 8.342e-03 where the artifact reads
**8.4748e-03**. The record follows the artifact. Neither changes the gate's verdict.


**PASS 91: the ceiling argument that closed this campaign is GONE for the diffusion half.** Pass
88 closed at a ceiling on A18's first clause — a disagreeing forward invalidates the gradient
comparison taken at it, our DiT forward disagreed at 2.07e-02 per block, therefore completing the
bijection and re-running instrument A was *void before it was taken*. Against a 0.4.3 reference
that forward reads **3.448e-03 per block and 2.168e-02 over all 24**, every rung inside the bar
(D23, `of3t-rebase` pass 1). **So the comparison is no longer void and the measurement is
available.** What still gates it is mechanical rather than conceptual: A18's discriminator was
scored against `S["xl_out"]` in a 3.2 GB `sub_boundary.pt` captured through a 0.5.0 forward, so it
must be re-captured at 0.4.3 before the discriminator can be re-read. The 0.7672 / worst 87.82 /
283-of-283 reading stays withheld until it is.


Found by `of3t-diffusion`, pass 79, on the first end-to-end device arm at the captured r=0
boundary: all 48 noised structures accumulated on one p300c, seeded with the reference's own
cotangent.

| quantity | value |
|---|---|
| tensors compared | **283** (of the reference's 738; of our 870 reachable) |
| share of the diffusion squared norm | **61.02 %** |
| median relative L2 | **0.7672** |
| worst | **87.82** on `diffusion_transformer.blocks.8.attention_pair_bias.layer_norm_a.layer_norm_s.weight` |
| over the 5.0e-02 bar | **283 of 283** |
| measured zero-model baseline | **1.0** |
| separation from a deleted model | **1.30x** |

**Two named defects, either of which produces this.** The **bijection reaches 283 of 870**: three
load sites were patched, and the diffusion transformer, the decoder and the noisy-position
embedder load by paths none of them cover. And **0.77 is the wrong shape** — an fp32-vs-float64
gap reads ~1e-2; a median at the zero-model baseline with a worst of 87.8 is a **mis-wired
operand**. Under **A18** this is therefore a broken instrument, not a ceiling, and it does not
publish as a result.

**The discriminator is cheap and was skipped.** `sub_boundary.pt` already carries their `xl_out`
at exactly these inputs. One short forward run separates wiring from arithmetic. Nothing further
on the gradient is worth doing before it.

**Two mechanical things came out right and should not be relitigated.** Accumulating 48 tapes is
**exact, and probed rather than assumed** — the leaf gradient norm grows 1.73e-4 → 3.84e-4 →
5.23e-4 over the first three structures, which a `backward` that replaced instead of accumulating
could not do. And the cost collapsed: first structure 39 s of compile, **1.2 s each** after, so
the matched-N run is ~2 minutes against the ~31 budgeted.

**A near-miss worth keeping.** An earlier single-structure run read median **0.985** — a
publishable-looking number that was pure **scope mismatch**, one noised structure compared against
a reference that is the sum over 48. For terms of similar magnitude and uncorrelated direction
that ratio must land near `sqrt(49/48)`, and it did. It is committed **labelled as a mismatch, not
a result**.

### D22. Our port targets openfold3 0.4.3; the reference bundle was built with 0.5.0. CONFIRMED and quantified — but it does NOT explain the diffusion gap. UNFIXED.

Found by `of3t-diffusion` (pass 86) as "revision skew, 50 files and 1,989 lines", and pinned to
a revision this pass by `of3t-orchestrator` from the two release trees already unpacked on pc.

**Which revision, measured.** Normalising away vendoring import rewrites and diffing all 108
vendored Python files against both releases:

| our `tt_bio/_vendor/openfold3` vs | differing lines | files differing |
|---|---|---|
| **0.4.3** | **537** | **19** |
| **0.5.0** | **2,028** | **50** |

**3.8x closer to 0.4.3.** The row's 50-files/1,989-lines figure against 0.5.0 reproduces here
at 50 files / 2,028 lines under a slightly different import filter, so the two measurements
agree and the new fact is the 0.4.3 column. The reference bundle is 0.5.0 (`c4771653`).

**It cannot be the cause of the diffusion transformer's 2.07e-02, for three independent
reasons.**

1. **The vendor contains no model layers.** All 108 files are `core/data/*` (pipelines, io,
   framework, primitives, tools, resources), `core/utils/geometry`, `core/config` and
   `projects/of3_all_atom/config`. The **only** vendored model file is
   `core/model/structure/augmentation.py`. There is no vendored diffusion transformer, no
   vendored attention, no vendored transition — the ttnn model is hand-written. Vendor skew is
   skew in the featurizer and config, not in the function under test.
2. **The row measured the operands exact.** Our `s`, `z` against their captured `dit_in` read
   **0.000e+00**, so whatever the featurizer divergence does, it does not reach this boundary.
3. **Every DiT-path layer file is functionally INERT between 0.4.3 and 0.5.0.** Checked file by
   file: `diffusion_transformer.py` splits `AttentionPairBias` into a dedicated
   `DiffusionAttentionPairBias` and drops the `use_ada_layer_norm` flag — and the two forward
   methods are **line for line identical** once 0.4.3's `use_ada_layer_norm=True` branch is
   taken, so the split is a pure refactor. `transition.py`'s 56 changed lines are the
   **deletion of `StructureModuleTransition`**, an unused AF2 class. `diffusion_conditioning.py`
   changes exactly one line, `linear_init_params.linear_z` to `.linear_s` — an
   **initialisation** parameter, inert when a checkpoint is loaded.

**So the DiT gap points back at the hand-written module, not at the revision.** That is the
opposite of the row's leading explanation and it is the more expensive answer, which is why it
needs saying: *"we are comparing two different models" is the comfortable reading, and the
evidence does not support it here.*

**Where the revision DOES bite is the trunk, and it names the campaign's worst sub-module.**
0.5.0 adds a `transpose_bias` argument to `triangular_attention.py` that 0.4.3 does not have:

```
# 0.4.3                 triangle_bias = permute_final_dims(self.linear_z(x), (2, 0, 1))
# 0.5.0   transpose_bias: permute_final_dims(self.linear_z(x), (2, 1, 0))  else (2, 0, 1)
```

Upstream's own docstring: *"In practice this is used for Triangle Attention from the **end
node**, where the input is transposed prior to calling this function."* **`tri_att_end` is the
worst group in D8's decomposition at 0.3838**, and pass 85 measured the start/end asymmetry at
**3.1x-4.4x assembled against ~1.0 alone**. It is the one sub-module in the block whose bias
ordering is revision-dependent. Our port **has** the flag — the stack-scope arms measure
**1.8986** shipped against **5.7014** with it off — so this is explained rather than open, but
it is why the end node is special and it was never written down.

**What is still open.** Whether the 537 lines that separate our vendor from 0.4.3 matter, and
whether the hand-written ttnn modules were written against 0.4.3 throughout. The vendor being
0.4.3-era is evidence about the vendor, not proof about hand-written code that was never
vendored at all.

**RECONCILIATION — the row concluded at 23:00:42 still calling revision skew the leading
explanation, and did not see the three checks above.** Its final DONE reads *"the leading
explanation is revision skew rather than a port defect"*; the amendment carrying the refutation
reached its brief minutes after it exited. **The campaign's position is the one recorded here**:
the skew is real, it is 0.4.3-against-0.5.0, and it is **not** the cause at this boundary. The
row is not wrong to have said it — it is the reading its own evidence supported, and it named
the gap in that evidence itself (*"the DiT is hand-written and unvendored, so the skew evidence
cannot exhibit the specific change"*). The missing piece was the release trees, which were on
the disk the whole time.

**And the row itself produced the strongest evidence that this is NOT one mechanism with D19 —
by measuring the composition law rather than matching shapes.** D19's trunk error grows
**near-linearly, at 0.74x of block count**. The DiT's grows **sub-linearly, at 0.32x**, and its
curve is flat and then jumps: **2.07e-02, 2.28e-02, 2.23e-02** through four blocks, **3.42e-02**
at eight, **1.42e-01** at sixteen, **1.59e-01** at 24. *A shared mechanism should compose the
same way.* This supersedes the "both are per-block forward errors compounding with depth"
resemblance that this record noted at pass 83 — resemblance of shape is not resemblance of law,
and the law is measurable.


---

### D23. The reference bundle runs the preview2 checkpoint on upstream 0.5.0, which upstream declares unsupported. CONFIRMED on CPU. It is the cause of D19 and of the DiT forward gap, and it clears our shipped inference. UNFIXED (the reference must be rebuilt at 0.4.3).

Found by `of3t-orchestrator` pass 89 from the release trees on pc. No card. Scripts and results:
`perf/of3t_orchestrator/revision/` (`dit_apb_identity.py`, `dit_apb_control.py`, `FINDING.md`).

**Upstream's own registry settles the compatibility question.** `entry_points/parameters.py` in
the 0.5.0 sdist gives `of3-p2-155k.pt` `version_compatibility=">=0.4,<0.4.4dev0"` and lists it in
`LEGACY_CHECKPOINTS`, commented *"not supported for download and use in the current version"*.
The same file in 0.4.3 gives it `">=0.4"` unbounded and makes it `DEFAULT_CHECKPOINT_NAME`.
v0.4.0 is the "OpenFold3 Preview2" release; v0.5.0 is the "OpenBind Model Release". The campaign's
bundle is 0.5.0 (D22, `c4771653`) loading `of3-p2-155k.pt`.

**Two code changes separate the revisions, one per track, and both are on the paths the campaign
measures.**

*Trunk.* `transpose_bias=True` on `PairFormerBlock.tri_att_end` occurs in `base_blocks.py` zero
times in 0.4.0, 0.4.3, 0.4.4 and 0.4.5, and once in 0.5.0.

*Diffusion transformer.* 0.5.0 splits `AttentionPairBias` into a plain class and a new
`DiffusionAttentionPairBias`, and the new class has **no `layer_norm_z`** — not constructed, not
applied. 0.4.3's single class constructs it (line 107) and applies it to the pair bias (line 156)
on both tracks. The p2 checkpoint carries the weights it needs: 24 tensors
`diffusion_module.diffusion_transformer.blocks.{0..23}.attention_pair_bias.layer_norm_z.weight`,
shape `(128,)`.

**Measured.** Loading p2's block-0 attention weights into each of upstream's own constructions:
0.4.3 reports `missing=[] unexpected=[]`; 0.5.0 reports `missing=[] unexpected=['layer_norm_z.weight']`
— it has nowhere to put the norm and drops it silently. Relative L2 between the two constructions
in float64 on the same weights: **3.995e-01 at N=64, 3.868e-01 at N=384**, flat in N, which is the
campaign's own DiT size ladder. **Sufficiency control: pre-normalising `z` with the checkpoint's
own `layer_norm_z` makes 0.5.0 reproduce 0.4.3 at 0.000000e+00, bit-identical.** `layer_norm_z` is
the entire difference, not one contribution among several.

**This corrects D22.** D22 concluded every DiT-path layer file is functionally inert between the
revisions. It compared the two `forward` methods, which are line-for-line identical; the change is
in `_prep_bias`, which `forward` calls. The wider primitive set D22 did not cover
(`normalization.py`, `activations.py`, `linear.py`) **is** inert under a float64 reference — those
diffs are bf16 autocast branches, an opt-in Triton kernel flag and an init-time bias guard.

**This clears our shipped inference, and that is the load-bearing consequence.** `of3t-reopen`
reported that our shipped OpenFold3 trunk computes the ending-node function 0.5.0's
`PairFormerBlock` does not call. True of 0.5.0, which does not support these weights. tt-bio sets
`tri_att_end_bias_follows_pair = not is_openbind(state_dict)` (`openfold3_trunk.py:133`) and
selects the per-block `layer_norm_z` on `shared_ln is None` (`openfold3_diffusion_transformer.py:265`)
— giving each checkpoint the convention of the release that produced it, which is upstream's own
binding reproduced. **No flag is to be flipped.** Flipping the end node would have put a real
regression into shipped OpenFold3 inference on the authority of a reference that cannot run these
weights.

**`of3t-reopen`'s own bisection corroborates the DiT half.** Inside one DiT block it measured
`d_attention_pair_bias` over bar at **5.522e-02** with the shared AdaLN conditioning clean at
**6.027e-03**. `layer_norm_z` is on the pair-bias path and not on the AdaLN path, so the one op
that differs between the revisions is the one op the bisection indicted.

**The composition-law refutation stands.** D19 composes near-linearly at 0.74x of block count and
the DiT sub-linearly at 0.32x, so they are not one mechanism — they are two different code changes
on two tracks. They have one *cause*.

**Remedy, and the predictions that falsify it.** Rebuild the bundle at **0.4.3**: the revision the
port targets and the last inside upstream's declared window. 0.4.4's model tree is byte-identical
to 0.4.3's; 0.4.5 changes 13 model files, so 0.4.5 and later are not substitutes. pc's installed
tree at `/home/moritz/.coworker/scratch/of3-upstream/repo` (0.4.6.dev12+g72fc3a953) still carries
the unified `AttentionPairBias` with `layer_norm_z` and no `transpose_bias`, so it behaves as
0.4.3 on both changes. Written before the rebuild exists:

1. The 48-block forward on the **shipped** arm falls from **2.792e-01** toward the bf16
   composition floor and the `tb-off` arm, better against 0.5.0 at **4.965e-02**, gets worse. Both
   arms moving the same way refutes this.
2. `d_attention_pair_bias` falls from **5.522e-02** toward **6.027e-03**, and `block_out` from
   **2.072e-02**. If it stays over bar the DiT has a second defect and this closes only part.
3. D19's near-linear 0.74x composition disappears, one differing sub-module per block being what
   produces a per-block constant that composes linearly.

**PASS 90 — the skew is completely bounded, and upstream's own loader refuses it.** The two
changes above were found by reading diffs, which is not a bound. Building the whole `OpenFold3`
model from each revision's own `model_config` and diffing parameter names against the checkpoint
(`full_model_keys.py`): **0.4.3 gives missing 1 (`version_tensor`), unexpected 0** against 4,936
model params and 4,935 checkpoint tensors; **0.5.0 gives missing 3, unexpected 48** against 4,890.
The 48 are exactly the `layer_norm_z` hoist counted across both paths the checkpoint carries, 24
under `diffusion_module.` and 24 under `sample_diffusion.`. **There is no other parameter-level
divergence at whole-model scope**, and p2 fits 0.4.3 exactly.

And upstream 0.5.0 **already ships this check**: `entry_points/experiment_runner.py:750-772`,
`_load_state_dict_with_version_validation`, computes the same two sets, warns and loads
`strict=False` only when `missing == {"model.version_tensor"}` with nothing unexpected, and
otherwise **raises ValueError**. At missing=3 / unexpected=48 the p2-on-0.5.0 case takes the raise
branch, so **upstream's supported entry point will not load this checkpoint on 0.5.0 at all** —
the bundle went around it with a direct `load_state_dict(..., strict=False)`. 0.5.0 also registers
`version_tensor = MODEL_VERSION = [2,0,0]` and raises on a mismatch, so upstream treats the
checkpoint-to-architecture binding as a correctness gate rather than a convention.

**Correction to the gate this campaign is installing: it is necessary and not sufficient.** A
key-set check catches the diffusion half and is structurally blind to the trunk half —
`transpose_bias` carries no parameter, so a model computing the wrong ending-node function loads
with missing 0 and unexpected 0. Pair it with an explicit assertion that the installed revision
sits inside the checkpoint's declared `version_compatibility` window, which catches both kinds.
`of3t-rebase`'s brief is amended accordingly and the live session was told directly.

**PASS 108 — D23's TRUNK HALF IS CONFIRMED TOO, and it survives its own pre-registered
falsifier.** `of3t-rebase` ran six crop-64 arms against the rebuilt 0.4.3 reference, scored with
`of3t-orchestrator`'s `revision/d8_vs_endnode.py` **unchanged** — same 5.0e-02 / 2.0e-02 bars,
same A14 floor, same norm-share definition, 53 compared and 52 kept on both sides so the columns
are the same tensors. **Recomputed independently by the orchestrator from the row's own artifacts
and reproducing exactly:**

| arm | median vs 0.5.0 | median vs 0.4.3 | over bar | norm share | |
|---|---|---|---|---|---|
| block 0 shipped | 6.481e-02 | **1.2136e-02** | 9/52 | 5.1 % | 5.34x better, **PASS** |
| block 0 tb-off | 1.148e-02 | 7.3978e-02 | 37/52 | 24.1 % | 6.4x worse, FAIL |
| block 23 shipped | 9.304e-02 | **1.9191e-02** | 15/52 | 25.0 % | 4.85x better, **PASS** |
| block 23 tb-off | 1.743e-02 | 1.008e-01 | 46/52 | 70.0 % | 5.8x worse, FAIL |
| block 47 shipped | 4.270e-01 | **2.0129e-01** | 52/52 | 100 % | 2.12x better, FAIL |
| block 47 tb-off | 4.004e-01 | 2.386e-01 | 52/52 | 100 % | 1.68x better, FAIL |

**The falsifier was "both arms moving the same direction refutes D23's trunk half."** At blocks 0
and 23 they move in **opposite** directions — shipped improves, `tb-off` degrades — which is what a
convention mismatch looks like and nothing else does. Both blocks cross **FAIL → PASS** on the
median bar. Against the prediction written before the rebuild existed (≈0.0115, ≈0.0174, ≈0.40),
the measurements land **within 6 % and 10 %** on the first two, and block 47 comes out twice as
good as predicted.

**Block 47 is a separate animal and D23 does not explain it.** Both arms improve *together*,
2.12x and 1.68x, and both still fail at 2.013e-01 and 2.386e-01 with 52 of 52 tensors and 100 % of
the compared norm over bar. **The depth-graded residual stands as its own open finding**, now with
the reference-side term removed from it.

**These arms are readable because the floor under them is bit-exact** — A13 on the rebuilt
reference reads worst **0.000e+00** on all 171 tensors — so what they measure is our port's error
with no reference-side term to share or subtract.

**A mis-pin the row caught and handled exactly right.** The ladder first ran with
`--scale-pair-bias on`; audited against the arms' own `shipped_config`, that is true of the three
48-block *stack* arms and **false** of all six crop-64 arms, so the first run moved the bias
convention and the revision together. The row **moved** those results to
`perf/of3t_rebase/mispinned_spb_on/` rather than labelling them in place, citing that a label
beside a wrong number does not stop it being quoted. It kept them as evidence about the flag, and
they say something new: **the bias scale is worth 4.7x at depth 47** (9.439e-01 mis-pinned against
2.013e-01 matched) **and nothing at depth 0** (1.207e-02 against 1.214e-02) — **depth-dependent**,
which no entry records. The mis-pin also dropped `attn_pair_bias.linear_z.weight` out of the
bijection, 52 instead of 53, which is how it was caught.

**Orchestrator correction:** pass 106 hypothesised from the logs that "D1's correction has an L1
cost". The arm that hit the circular-buffer clash was the **mis-pinned** `spb on` run, and crop-64
arms ship with `spb=false`, so the clash was on a configuration that is not shipped. The narrow
true statement is that `spb=on` clashed at crop 64 where `spb=off` ran; whether that is inherent
is a lower-priority open question, and the 4.7x-at-depth-47 result is the more interesting fact
about that flag.

**PASS 91 — D23's DIFFUSION HALF IS CONFIRMED AND CLOSED AT BLOCK 0, against the prediction
written before the rebuild existed.** `of3t-rebase` built the reference at 0.4.3 and re-ran the
one-block bisection on the same captured `dit_in`, block 0, n=384, device arm float32 HiFi4 on
one p300c, reference float64:

| boundary | against 0.5.0 | against 0.4.3 |
|---|---|---|
| `adaln_a_out` | 6.027017e-03 | **6.027017e-03**, unchanged to every printed digit |
| `adaln_t_out` | 6.850e-02 | **9.180e-03** |
| `a1_running` | 2.180e-02 | **4.245e-03** |
| **`d_attention_pair_bias`** | **5.522e-02** over bar | **1.076e-02** |
| `d_conditioned_transition` | 5.327e-02 over bar | **7.502e-03** |
| `block_out` | 2.072e-02 | **3.448e-03** |

**Every boundary is now inside the 5.0e-02 per-tensor bar**, so on the falsifier the brief set —
*if `d_attention_pair_bias` stays over bar the DiT carries a second defect* — **there is no second
defect at block 0**. D23 predicted `d_attention_pair_bias` would fall from 5.522e-02 toward
`adaln_a_out`'s 6.027e-03 and `block_out` from 2.072e-02; they read 1.076e-02 and 3.448e-03.

**`adaln_a_out` is the control that makes the rest readable.** It is the one boundary in the block
that `layer_norm_z` is not on, and it reproduces across the two references to every digit. A
reference swap that moved everything equally would have moved nothing in particular; this one
moves the pair-bias path and leaves the path beside it alone, which is exactly what D23 says the
change is.

**Depth ladder**, same instrument truncated to d blocks on both sides:

| blocks | 1 | 2 | 4 | 8 | 16 | 24 |
|---|---|---|---|---|---|---|
| vs 0.5.0 | 2.072e-02 | 2.284e-02 | 2.232e-02 | 3.423e-02 | 1.417e-01 | 1.592e-01 |
| vs 0.4.3 | **3.448e-03** | **5.272e-03** | **5.380e-03** | **7.793e-03** | **1.535e-02** | **2.168e-02** |

The full 24-block DiT reads **2.168e-02 against 1.592e-01, a 7.34x improvement**, and every rung
is inside the bar. The 0.32x composition law is **restated rather than retired** — 0.262x against
0.320x of block count, log-log slope 0.579 against 0.642, both consistent with independent
per-block perturbations accumulating as roughly the square root of depth. *The shape was never the
defect; the level was.*

**REFBUILD passed its own gate, and the gate was shown to fail.** 0.4.3 loads the checkpoint at
missing=1 (`version_tensor`) / unexpected=0 and upstream's own loader accepts it; the 0.5.0
control reads missing=3 / unexpected=48 and upstream's loader raises. The check is now baked into
`perf/of3t_reference/bundle_min.py`, which refuses to build on a non-empty unexpected set, and it
exits 1 on the 0.5.0 tree on demand. The row also kept the version-window assertion as the second
gate, correctly noting it is the one to keep if only one can be afforded.

**Still open on that row:** the trunk arms (waiting on the float64 gradient rebuild in flight),
the diffusion-scope gradient (A18's discriminator must be re-read at 0.4.3, which needs a 3.2 GB
boundary re-capture) and the inference digests. Its verdict is PARTIAL and it says so.

**NEW, AND IT REACHES EVERY BIJECTION FIGURE IN THIS CAMPAIGN: the denominator 4,147 is the 0.5.0
model's parameter count.** Measured independently by `of3t-orchestrator` and `of3t-rebase` and
agreeing: `OpenFold3(model_config).parameters()` is **4,170 at 0.4.3** and **4,147 at 0.5.0**, the
difference being 24 per-block DiT `layer_norm_z` weights minus the 1 hoisted shared norm. So every
figure the campaign quotes as `x of 4,147` — D17's **3,545 of 4,147 tensors / 98.69 % of the
squared gradient norm**, the reference's **4,147 of 4,147** A13 bit-identity — is computed against
a model that is **missing 23 parameters our device model carries**, and the norm shares are shares
of the wrong model's norm. The A13 self-consistency result is not thereby wrong, but it is about
the wrong model. Re-deriving the bijection and its norm shares against 4,170 is owed by
`of3t-rebase` alongside the trunk arms.

Owner: `of3t-rebase`, dispatched pass 89.

### D26. Instrument A's reach denominator excludes `absent` tensors, and the shipped `scale_pair_bias=True` default pushes its largest attn_pair_bias tensor into `absent` at every pairformer block. MEASURED by `of3t-orchestrator`, pass 118. UNFIXED.

`instrument_a_bundle_*.json` reports reach over the tensors it **compared**. Tensors the
value-bijection could not place land in `absent` and leave the denominator entirely, so the
reported share is a share of what the instrument happened to see.

At pairformer block 47 this is not a rounding matter.
`pairformer_stack.blocks.47.attn_pair_bias.linear_z.weight` carries `ref_norm` **1.684e-01**,
which is **26.12 %** of the block's full reference gradient mass (full block 1.0861e-01 squared
norm; the compared set only 8.0244e-02). Under `scale_pair_bias=False` it is compared and fails
at **0.811**. Under `scale_pair_bias=True` — the default the row landed — it is `absent`,
presumably because the device folds the scale into the weight and the value no longer matches.
The row's file therefore reports reach **12.336 %** where the honest full-block figure is
**9.114 %**.

The same +1 live-tensor asymmetry between the two arms appears at blocks 0 and 23, so on the
shipped default this is **48 tensors — one per block — invisible to instrument A**, and they are
the tensors belonging to the very module the lever acts on. A further four
(`attn_pair_bias.mha.linear_{q,k,v}.weight`, `linear_q.bias`) are absent in **both** arms,
consistent with a fused QKV on the device side and unquantified because the bundles do not carry
their reference norms; until that is settled the compared set is at most **73.88 %** of block 47.

Fix: carry `ref_norm` for `absent` tensors and report reach against the full reference mass, so
that a tensor the bijection loses counts as unreached rather than as not-there. A15 already says
reach is a squared-norm share; D26 is A15 applied to the denominator as well as the numerator.

Owner: `of3t-rebase`. Evidence: `perf/of3t_orchestrator/block47/{FINDING.md,block47_arms.json}`.

### D27. At pairformer block 47 the median and the norm-share reach rank the two `scale_pair_bias` arms in opposite directions, and `transpose_bias` — the lever that rescues blocks 0 and 23 — moves block 47 by exactly zero. MEASURED by `of3t-orchestrator`, pass 118. UNFIXED.

Four arms, one frozen reference (`bundle.sha256 = 1d4ea922...`, verified identical across all
four, with bit-identical `ref_norm` on every shared tensor), 51 tensors common to all four,
scored under A14 and A15:

| arm | median rel | over 5.0e-02 | reach (compared) | reach (full block) | worst |
|---|---|---|---|---|---|
| spb ON, tb shipped | 0.9439 | 45/51 | 12.336 % | 9.114 % | 10.84 |
| spb ON, tb off | 0.9684 | 45/51 | 12.336 % | 9.114 % | 13.12 |
| spb OFF, tb shipped | 0.2001 | **51/51** | 0.000 % | 0.000 % | 33.08 |
| spb OFF, tb off | 0.2369 | **51/51** | 0.000 % | 0.000 % | 33.27 |

Turning `scale_pair_bias` off improves the median **4.7x** and takes the reach to **zero** — every
tensor goes over the per-tensor bar, and the worst tensor degrades **3x** (10.84 -> 33.08). The
headline number and the scope statement disagree about which arm is better. Nor is the 9.114 % a
partial success: it is **one tensor**, `attn_pair_bias.layer_norm_z.weight` at 12.02 % of the
compared mass and rel 0.029, plus 0.31 % of crumbs. **No sub-module of block 47 passes in any arm.**

Orthogonality, same recompute at the three measured blocks (reach as share of compared mass):

| block | tb off | tb shipped | median off -> shipped |
|---|---|---|---|
| 0 | 75.900 % | 94.886 % | 0.0736 -> 0.0121 |
| 23 | 30.047 % | 74.956 % | 0.0998 -> 0.0192 |
| 47 | 12.336 % | **12.336 %** | 0.9684 -> 0.9439 |

`transpose_bias` moves block 23's reach by 45 points and block 47's by **exactly 0**. Conversely
`scale_pair_bias` moves block 47's median 4.7x and blocks 0 and 23 by under 2 %
(0.0121 -> 0.0121, 0.0192 -> 0.0192). **Two levers, two disjoint populations of blocks.** Block 47
does not have the defect the shipped `transpose_bias` fix addresses, so tuning that lever cannot
reach it. This retires the working assumption that block 47 is a "depth-graded residual" of the
same mechanism seen at 0 and 23.

**A mechanism with a shipped lever, added pass 119.** The bundles carry each block's own
boundary activation norms. The **single** track grows through the stack and the **pair** track
does not: `s_norm` 5.8502e+03 -> 1.5190e+06 -> 2.1493e+06 at blocks 0/23/47, a **367x** growth,
against `z_norm` 1.3204e+06 -> 1.3191e+06 -> 1.2937e+06, a **0.98x** change. Mean per-block
fractional growth of `s` is **27.3 %** over blocks 0-23 and **1.46 %** over blocks 23-47; bf16's
relative resolution is `2^-8 = 3.9e-03`, so the per-block update is **70x** the resolution early
and **3.7x** late.

`tt_bio/tenstorrent.py` already implements the remedy as **`s_fp32_residual`**, whose own comment
says it "is the difference between carrying an update and losing it: a track whose residual is
much larger than its per-block update quantises that update away". It is set `True` in exactly
one place, `openfold3_confidence.py:101` — the **confidence** Pairformer. `openfold3_trunk.py:152`
does not pass it, so the trunk runs at the default `False`, and the trunk is the stack whose
single track grows 367x.

Experiment: re-run the crop-64 instrument-A arms at blocks 0, 23 and 47 with the trunk Pairformer
constructed `s_fp32_residual=True`, nothing else changed. **Registered before the run:** block 47's
median falls more than 2x, block 0 moves less than 20 %, and the pair track moves less than the
single track.

Honest limits. `s_norm` is an L2 norm and not the absmax the flag's comment cites; the 27.3 % and
1.46 % are segment means, not local values; and **block 23 already sits at 70.7 % of block 47's
`s_norm` with a 49x better median**, so `s_norm` alone does not order the failures — block 23 is
the control, and if neither block moves the hypothesis is refuted. Turning the flag on in the
trunk is **release-gated** (accuracy change plus one `[B, L, c_s]` fp32 tensor): branch only.

Also refuted this pass: the comfortable reading that this is catastrophic cancellation on
small-gradient layer norms. Spearman(`ref_norm`, `rel_l2`) within each arm is **-0.50..-0.29** at
block 0 and **-0.33..-0.05** at block 23 — the precision signature — but **-0.11..+0.15** at
block 47. Block 47's failure hits large-mass and small-mass tensors alike, so "imprecise, not
wrong" is not available there.

Owner: unassigned — this is the campaign's largest unexplained trunk failure, and the
`s_fp32_residual` arm is the cheapest test anyone has proposed for it.

### D28. A18 fires at block 47 and, harder, at block 0: the gradients are taken at forwards that disagree above the bar, and the disagreement lives entirely in the crop padding. MEASURED by `of3t-orchestrator`, pass 118. UNFIXED — the backward has never been checked for mask-cleanliness.

`forward_rel` from the same four bundles, 64-token crop over 56 real tokens:

| block | `z` unmasked | `z` masked | `s` unmasked | `s` masked |
|---|---|---|---|---|
| 0 | 2.86e-02 | 3.48e-03 | **3.39e-01** | 8.12e-03 |
| 23 | 3.79e-02 | 4.12e-03 | 2.34e-03 | 2.34e-03 |
| 47 | **8.26e-02** | 8.82e-03 | 3.08e-02 | 3.66e-03 |

Block 47's `z` is **above the 5.0e-02 per-tensor bar**, so A18 applies directly: its gradient is
taken at a forward that disagrees. Block 0 is the sharper case — `s` disagrees **6.8x over the
bar** unmasked and by 8.1e-03 masked, a **42x** reduction. The device writes garbage into the pad
rows and the mask hides it in the forward.

Block 0's comfortable median of **0.0121**, one of the two trunk figures the campaign currently
quotes as passing, is therefore taken at the worst forward disagreement in the table. That the
forward is mask-clean does **not** establish that the backward is; nothing measured so far can
tell the difference, because every comparison is against a reference computed at the same crop.

Cheapest discriminator: re-run one block with the pad rows set to NaN and check whether any
parameter gradient becomes NaN. A NaN says the pad reaches the weights and every crop-64 figure in
the campaign inherits it; a clean run retires D28 in one shot.

**Scoped, same pass, from `device_gradient_043all.json`.** At full scope — 48 blocks, NP 448,
no 64-token crop — the per-block `forward_rel` is **flat**: min 2.05e-03, median 8.34e-03, max
1.57e-02, and the block-47 entry is an unremarkable **1.32e-02**, lower than block 25's. A
log-log fit against depth gives exponent **-0.071 at R² 0.0195**, Spearman **-0.166**: there is
no depth trend whatsoever. And the masked crop-64 figure at block 47, **8.82e-03**, agrees with
the full-scope figure to within a factor of 1.5. **So the 8.26e-02 is a property of the crop-64
harness's padding, not of the model**, and D28's exposure is confined to the crop-64 instrument
— which is nonetheless where blocks 0, 23 and 47 were all measured, so the NaN-pad discriminator
is still owed before any crop-64 figure is quoted as a pass.

Owner: `of3t-rebase` (same bundles, same harness).

### D29. WITHDRAWN AND REPLACED. The "accumulation probe growing 26.68x" is indexed by diffusion SAMPLE, not by depth, and it is not an error measure at all. CORRECTED by `of3t-orchestrator`, pass 120, against its own pass-119 entry.

**What the arrays actually are.** In `device_gradient_043*.json` the 48-long `accumulation_probe`
and `forward_rel` are indexed by **diffusion structure (sample)**, not by pairformer block:
`structures_done` reads `[0, 1, ..., 47]`, `structures_asked` has 48 entries, and
`perf/of3t_diffusion/device_gradient.py` loops over structures. The single-structure run
`device_gradient_043.json` carries exactly **one** entry in each array with `structures_asked: [0]`,
which settles it.

**And the probe is not an error.** The loop appends
`float(ttnn.to_torch(gv).double().norm())` for the first leaf it finds with a gradient, then
`break`s — it is the **norm of one arbitrary parameter's accumulated gradient** after k+1
structures. Its growth 1.2510e-04 -> 3.3378e-03 is the accumulating sum building up over 48
samples. It contains no comparison against the reference and therefore **no parity information
whatsoever**, and it should never have appeared in a parity summary.

**Pass 116 used it correctly and pass 119 misread pass 116.** Pass 116 reports the figure in a
row labelled `structures | 48 of 48, probe growing 26.68x` and reasons about it exactly right:
48 independent samples would grow `sqrt(48) = 6.93x`, perfectly correlated ones `48x`, and the
measured 26.68x sits between — so `backward` accumulates across tape contexts rather than
replacing, and the samples correlate as the same weights under different noise should. That is a
**scope certification**, it is sound, and it stands.

What pass 119 did was re-read that correctly-labelled per-structure figure as a per-**block**
depth profile, fit `k^0.765` to it, and announce that it had corrected pass 116's framing. There
was nothing to correct: pass 116 never claimed depth. The pass-119 arithmetic is right about a
quantity that carries no parity information, on an axis that was never depth, against a claim
nobody made. **The pass-119 entry is withdrawn in full; the pass-116 scope certification is not
affected.**

**The genuine depth evidence, which exists and says something.** `dit_depth_ladder_043.json` is a
real depth axis: the DiT truncated to `d` blocks on **both** sides, real captured activations,
device float32 against reference float64.

| DiT blocks `d` | 1 | 2 | 4 | 8 | 16 | 24 |
|---|---|---|---|---|---|---|
| forward rel (real tokens) | 3.448e-03 | 5.272e-03 | 5.380e-03 | 7.793e-03 | 1.535e-02 | **2.168e-02** |

Growth **6.29x over 24 blocks**, fitted `k^0.55` at R² 0.94 — barely above a random walk
(`k^0.5`, x4.90) and far below a coherent sum (`k^1.0`, x24.0). So the DiT forward error does grow
with depth, sub-linearly. That is the statement pass 119 wanted; it just was not in the file it
cited.

**What survives untouched.** D26, D27, D28 and the `s_fp32_residual` hypothesis all come from the
per-**block** instrument-A bundles (blocks 0, 23, 47) and from those bundles' own `probe.s_norm` /
`probe.z_norm`. None of them reads the 48-long arrays. Only D29 and one scoping paragraph in D28
did, and both are corrected. The D28 paragraph's substantive point — that the full-scope forward
figure and the masked crop-64 figure agree in magnitude — stands, but it is a comparison of two
forward error magnitudes and **not** a per-block depth profile.

### D30. The diffusion module's forward agrees to 0.85 % and its gradient is 16.6 % out: the backward costs a factor of 19.6x over the forward it is taken at. MEASURED by `of3t-orchestrator`, pass 120. UNFIXED, and it is the campaign's central number.

The diffusion module holds **89.2 %** of the model's gradient mass. From
`device_gradient_043all.json`, across all 48 structures against the rebuilt 0.4.3 reference:

| quantity | value |
|---|---|
| forward relative error, median per structure | **8.34e-03** |
| gradient relative error, median over tensors | **1.6588e-01** |
| ratio | **19.6x** |

Against the DiT truncation ladder's full-depth forward figure (2.168e-02, a different harness and
scope) the ratio is **7.7x**. Either denominator gives the same conclusion: **the diffusion
failure is backward-specific and is not inherited from a bad forward.** A forward at 0.85 % is
inside every bar this campaign uses; the gradient taken at it is 8.3x outside the 2.0e-02 median
bar.

The same decomposition at the three measured trunk blocks, forward against gradient:

| block | forward | gradient median | ratio |
|---|---|---|---|
| 0 | 1.10e-02 | 0.0121 | 1.1x |
| 23 | 3.69e-03 | 0.0192 | 5.2x |
| 47 | 1.32e-02 | 0.9439 | **71x** |

Where the port is healthy the backward costs about 1-5x. Where it fails — block 47, and the
diffusion module — it costs 8x to 71x. **That is the shape of the remaining gap, and it points at
the backward path rather than at the forward kernels.** The forward numbers here are per-structure
and per-block figures from two different harnesses and are not interchangeable with each other;
the ratios are order-of-magnitude statements, not calibrated quantities.

Owner: unassigned. The cheapest next discriminator is whichever backward stage the existing
`bisect` field was built to hold — it is `{}` in every 0.4.3 run on the record, so the bisection
the harness already supports has never been run at 0.4.3.


### D31. At every attention site the forward ships a fused SDPA carrying a measured 3.3e-02 deficit and the tape returns the gradient of a precise recompute. The function differentiated is not the function computed. RAISED by `of3t-orchestrator`, pass 121, from the code. UNFIXED and UNTESTED — the discriminator is one arm and needs no reference.

`taped_ttnn.py` calls `shipped(*ra, **rk)` for the forward value and hands it to
`ag.triangle_attention(..., value=out_v)`. `autograd.triangle_attention`'s own docstring states
what happens next:

> "The backward below reads q, k, v and bias and **recomputes the scores**; it never reads the
> forward output, so supplying the output changes nothing about the gradient"

and the recompute is `ttnn.matmul` + `ttnn.multiply` + `ttnn.add` + `ttnn.softmax` at
`precise_config()` (`autograd.py:801-807`). The same docstring records the fused kernel's cost,
measured on a p300c: *"the error sits flat at **3.3e-02** for every mask magnitude tested
(0, 0.05, 0.5, 2.0), which is the fused softmax's own known deficit."*

So the forward computes `fused(q,k,v,bias)` and the tape returns `d/dw precise(q,k,v,bias)`. The
gradient is neither the exact gradient of our forward nor the exact gradient of upstream's — it is
the gradient of the precise attention **evaluated at activations produced by the deficient one**.

**Which fused kernel, precisely — corrected pass 122.** Not tt-bio's own `triatt_sdpa`. That one
**declines** while a tape is open: `triatt_sdpa.py:340` reads `if ops.taping(): return None   #
generic_op has no backward; the stock fused SDPA verb is taped`. The object in the taped forward
is therefore the **stock `ttnn` fused SDPA** that `taped_ttnn.py`'s verb wraps — which is the same
object the 3.3e-02 figure was measured against, so D31's substance is unchanged and only its name
needed fixing. It matters because "the shipped fused SDPA" reads most naturally as the custom
kernel, and the custom kernel is exactly the one that is not there.

**Provenance note.** The docstring cites `tenstorrent.py:7205` for `self.scale = head_dim ** 0.5`.
The line has moved; it is now **`tenstorrent.py:7398`**, and the adjacent
`self._bias_scale = self.scale if scale_pair_bias else 1.0` at 7405 is what ties this path to D1.
The claim is correct, the line number is stale.

**Why it is a candidate for D30.** It is backward-specific by construction, so a forward-only
comparison cannot see it. And 3.3e-02 per site entering only the backward, across 24 DiT blocks,
is the right order for a 19.6x factor. Plausible, not demonstrated.

**A third leg stood here and is WITHDRAWN, pass 125.** It read: *"It sits on exactly the module
family that is worst in both stacks — the docstring says this one entry is what `AttentionPairBias`
in the pairformer and the denoiser's attention sites both reach, and the campaign's worst tensors
are `attn_pair_bias.layer_norm_a.weight` (trunk) and
`attention_pair_bias.layer_norm_a.layer_norm_s.weight` (diffusion)."* Classifying every compared
tensor by its **leaf op** shows that is a statement about the tail, not the locus: at every block
and arm the layer-norm median and the linear/matmul median agree to within 25 %, with layer norms
marginally **better**. Layer norms own only the extreme tail, because a layer-norm weight gradient
is a sum with cancellation over the normalised axis. **D31 stands on its other two legs.** See D34.

**What the existing checks miss.** `perf/hallgrad/gradcheck.py --cases triatt_chunked` differences
two chunkings of the **recompute** and establishes chunking-invariance. Nothing on the record
compares the recompute against the **fused kernel**.

**The discriminator, one arm, no reference needed.** On identical `q, k, v, bias`, compare the
shipped fused SDPA output against `autograd.triangle_attention`'s own recompute (call it with
`value=None` so it materialises `out_blocks` through exactly the code the backward
differentiates). Relative L2 per attention site. **~3.3e-02 confirms the mismatch at the
docstring's own magnitude and makes D31 the leading candidate for D30; ~1e-03 or below refutes it
for this model and it should be recorded refuted.**

**Two things this is not.** It is **not** a proposal to close the fused softmax's row-sum deficit
— that lever has been tried three times in this lineage and moved folds measurably worse each
time, because the deficit is entangled with a compensating accumulation order. The defect is the
**mismatch**, and the remedy if confirmed is to make the backward differentiate what the forward
computed. It is also **not** a claim about shipped inference; the forward is what production
ships and its accuracy is measured elsewhere. This is about the gradient.

**Refuted alongside it, so nobody spends a run on it.** `precise_config()` hardcodes
`ttnn.WormholeComputeKernelConfig` and is the only `ComputeKernelConfig(` construction in
`tt_bio/*.py` that does not branch on architecture, while `tenstorrent.py:11268` selects
`BlackholeComputeKernelConfig` on Blackhole. That looked like an exact fit for a backward-specific
gap. It is not: on the row's own environment, ttnn 0.68.0 `types.py:57` reads
`BlackholeComputeKernelConfig = WormholeComputeKernelConfig`, the `.so` exports only the Wormhole
name, and the arch branch is cosmetic in this version. The backward genuinely runs HiFi4 with
`fp32_dest_acc_en` and `packer_l1_acc`. (Worth a one-line cleanup note somewhere that the branch
is a no-op, but it is not a defect.)

Owner: `of3t-rebase` — it holds the card and the harness. Evidence:
`perf/of3t_orchestrator/backward/FINDING.md`.


### D32. Twenty-one sites across nine shipped modules route differently while a tape is open, so a training step is a materially different execution than an inference step — and no training throughput can be projected from an inference measurement. MEASURED by `of3t-orchestrator`, pass 122, from the code. UNFIXED as a method gap; the charter's second half has not been measured on either side.

`ops.taping()` is consulted at 21 branch points in 9 shipped modules, and each one routes the call
down a different, unfused path while the tape that training requires is open:

| module | sites | what stops happening |
|---|---|---|
| `triatt_qkv.py` | 74, 179, 276, 390 | fused QKV projection |
| `tenstorrent.py` | 1126, 4129, 4595, 8531 | trimul DRAM shape route; pair-proj L1 output; **L1 -> DRAM memory config** at 8531 |
| `eltwise_fusion.py` | 80, 104, 115 | `FUSE_MASK_ADD`, `FUSE_NORM_RESIDUAL` |
| `reblock_permute.py` | 373, 597, 877 | reblock/permute fusions |
| `softmax_generic.py` | 369, 514 | fused softmax |
| `triatt_sdpa.py` | 340, 486 | tt-bio's custom fused SDPA, and the QKV-from-pre-projection entry |
| `trimul_tail.py` | 233 | fused trimul tail |
| `mm_dualnoc.py` | 87 | dual-NoC matmul |
| `swiglu_fused.py` | 94 | fused SwiGLU |

The reason is uniform and stated in the code: *"generic_op has no backward"*. A `generic_op`-based
custom kernel cannot be differentiated, so the tape's price for a gradient is the composed path.

**Two consequences, and the second is the one the charter cares about.**

**(1) The gradient parity and the shipped inference path are measurements of different
executions.** Every instrument-A and device-gradient figure in this campaign is taken on the
unfused path. That is *self-consistent for training* — training would run taped too — but no
parity result here transfers to the inference path production ships, and no inference accuracy
result transfers here.

**(2) A training s/step cannot be projected from an inference s/step.** The campaign's own GAP
field records *"No s/step exists on either side: the GPU baseline's method is pre-registered and
nothing is measured, so the second half of Moritz's bar is untouched."* This defect sharpens that:
when it *is* measured, the device number must be taken **with the tape open**, because nine
modules' worth of fusion is off in that configuration. Any method that benchmarks the shipped
inference forward and calls the result a training throughput is measuring a different program.
Pre-registering that now, before anyone measures, is the cheapest it will ever be.

**Partially known, not previously drawn together.** `LEDGER.md:286` records `qkv_heads` declining
at `triatt_qkv.py:74`, and `tt_bio/train/lora.py:590` documents the discovery-ordering consequence
with a measurement — an untaped discovery forward reaches 196 device weights where the taped
forward that trains reaches 204. What was not on the record is the **scope** (21 sites, 9 modules,
including a memory-config change) or either consequence above.

Owner: unassigned for (1), which is a scope statement rather than work. For (2) it belongs with
whoever is given the perf half; it is a constraint on the method, to be fixed in the brief rather
than in code.

### D33. The campaign's own summary denied a measurement it also reported: GAP said no s/step existed on either side while PROVES carried 870.75 s against 7–8 s and a ~116x ratio. FOUND and FIXED by `of3t-orchestrator`, pass 123, with a guard and a negative control.

Two owed fields, both quoted by readers, directly contradictory:

- **PROVES**: *"Both sides now have a number at crop 384, and it is the campaign's first
  cross-stack timing statement: **870.75 s on a p300c against 7–8 s on an H200, ~116x**."*
- **GAP**: *"No s/step exists on either side: the GPU baseline's method is pre-registered and
  nothing is measured, so the second half of Moritz's bar is untouched."*

Both cannot be true. The denial is the wrong one, and it erased two real figures; I propagated it
into a report at pass 122 before noticing the numbers four hundred lines above it.

**What is actually true**, and the field now says this:

| side | figure | why it is not a step |
|---|---|---|
| GPU | **7–8 s** steady state, crop 384, batch 1, bf16-mixed, 1980 MHz sampled DURING | **NOT §4a**: recycle count drawn U{0..3} and not pinned, so it samples the draw. The 11 / 8 / 7 s spread across three steps is that draw showing itself. |
| TT | **870.75 s** on a p300c: forward 172.43 s + backward 698.32 s, AICLK median 1350 sampled DURING over 325 samples | **one taped trunk cycle at `num_recycles = 0`, not a step** — excludes the diffusion module (**89.2 %** of the gradient mass), every loss head, and the optimizer |

So: neither side has a §4a-satisfying s/step, the TT side has no step at all, and **~116x is a
lower bound on the gap that is generous to TT**. Closing it needs a pinned-recycle GPU rerun and a
TT number that includes what the trunk cycle omits — taken **with the tape open**, per D32. The
existing 870.75 s already was taped and is D32-compliant.

**The guard, and why it is narrow.** `audit_evidence.py` now fails if the summary asserts a
quantity has *not* been measured while the same summary carries a figure for it. It does not
attempt contradiction detection in general — only the shape that bit. Verified both ways:
injecting `No s/step exists on either side.` into GAP takes the audit to **148 confirmed / 1
drifted**; the corrected document reads **149 confirmed / 0 drifted**.

**And a second-order lesson, paid for immediately.** My first correction quoted the retired
sentence verbatim to show what had been fixed — and the guard fired on my own quotation. The
retired sentence is now paraphrased rather than quoted, which is the same rule as
`superseded-stamp-does-not-stop-the-number-being-read`: a denial left sitting in a live field gets
read as current, whatever label is next to it.

Owner: closed. The measurement gap it was hiding is real and belongs to whoever is given the perf
half.


### D34. The gradient failure is NOT localised to any op family — layer norms and linear/matmul fail at the same median at every block and arm — so "the worst tensor is always a layer norm" describes the tail, not the locus. MEASURED by `of3t-orchestrator`, pass 125. Closes a mis-reading; redirects the backward bisect.

Every compared tensor in the six block arms, classified by its **leaf op** (AdaLN nests
`linear_g`/`linear_b` under a `layer_norm_a` prefix, so a path-substring test bins those linears
as layer norms; this classifier keys on the last two path components). A14 applied.

| arm | layer_norm median / max | linear/matmul median / max |
|---|---|---|
| block 0, tb shipped | **0.0101** / 1.251 | **0.0121** / 0.080 |
| block 0, tb off | 0.0696 / 1.373 | 0.0753 / 1.048 |
| block 23, tb shipped | **0.0164** / 4.853 | **0.0206** / 0.075 |
| block 23, tb off | 0.0891 / 4.893 | 0.1035 / 1.330 |
| block 47, tb shipped | **0.9334** / 10.840 | **0.9450** / 1.014 |
| block 47, tb off | 0.9574 / 13.120 | 0.9705 / 1.816 |
| block 47, spb off, tb shipped | 0.1990 / **33.082** | 0.2025 / 1.016 |

At every block and arm the two medians agree to within 25 %, and **layer norms are marginally
better than linears**. A defect localised to one op's backward cannot produce that.

**What is true instead.** Layer norms own the extreme **tail**: max 10.84–33.27 against
linear/matmul's 1.0–1.8. A layer-norm weight gradient is a sum with cancellation over the
normalised axis, so a small absolute error reads as a large relative one — which is also why the
pass-119 test found relative error mildly anti-correlated with `ref_norm` at blocks 0 and 23. The
campaign's most-repeated pointer — *"one tensor is worst in all three measurements across two
references and three scopes"* — therefore describes the heaviest tail, not the source, and it has
been quoted as if it named a module.

**Where it redirects the bisect.** A failure whose median is uniform across op families *within* a
block while varying **78x between blocks** (0.0121 at block 0, 0.9450 at block 47, shipped arm) is
something that scales a whole block. That is what D27 showed from the other side — block 47
responds to `scale_pair_bias`, a whole-block input scaling, and to `transpose_bias` not at all —
and what the `s_fp32_residual` hypothesis proposes. **Cut the backward stage bisect by block
boundary first and by op only after.**

Owner: closed as a mis-reading. Evidence:
`perf/of3t_orchestrator/fd/op_family_is_not_the_locus.json`.

### D35. A single `rel_l2` cannot say whether our gradient is 18x too small, 2x too big, or uncorrelated — and at block 47 it is only 5.6 % better than emitting zeros. Instrument A holds both tensors when it computes `rel` and emits neither the magnitude ratio nor the cosine. MEASURED by `of3t-orchestrator`, pass 126. UNFIXED — two floats per tensor close it.

With `rel = ||g_dev − g_ref|| / ||g_ref||`, `r = ||g_dev|| / ||g_ref||` and `c = cos(g_dev, g_ref)`:

    rel² = 1 + r² − 2·r·c        and requiring |c| ≤ 1 gives exactly   r ∈ [1 − rel, 1 + rel]

A zero gradient gives `r = 0` and `rel = 1` **exactly**, so every `rel` is measured against a 1.0
ceiling. Applied to the arms (A14, common set):

| arm | n | median `rel` | IQR / median | `r` lower | `r` upper | better than zeros |
|---|---|---|---|---|---|---|
| block 0, tb shipped | 51 | 0.0121 | 1.486 | **0.988** | **1.012** | 98.8 % |
| block 23, tb shipped | 51 | 0.0192 | 1.851 | **0.981** | **1.019** | 98.1 % |
| block 47, tb shipped | 51 | 0.9439 | **0.142** | **0.056** | **1.944** | **5.6 %** |
| block 47, spb off | 52 | 0.2013 | 0.354 | 0.799 | 1.201 | 79.9 % |

**Three things fall out.**

**(1) Blocks 0 and 23 are genuinely close.** Their magnitude is pinned to within ~2 % and they are
nearly aligned. Those are real passes in a way the block-47 figure is not.

**(2) Block 47's magnitude is unidentified, and the shipped arm is 5.6 % better than emitting
zeros.** `rel = 0.9439` admits `r` anywhere in [0.056, 1.944]: *18x too small and correctly
aligned*, *2x too big and correctly aligned*, and *uncorrelated noise of about the right size* all
produce it. The instrument as built cannot tell them apart. Note also that the **spb-off arm is
the more informative one** — `rel = 0.2013` pins `r` to [0.799, 1.201] — even though D27 shows its
*reach* is worse. Informativeness and reach are different axes.

**(3) The tight clustering at block 47 is itself evidence.** IQR/median is **0.142** there against
**1.486** and **1.851** at blocks 0 and 23. At block 47 the relative errors of `tri_mul_out`,
`tri_mul_in`, `tri_att_start`, `tri_att_end`, `pair_transition` and the layer norms — completely
different ops — cluster inside ±14 % of one value. **Independent per-op errors do not do that; a
common multiplicative factor does.** This is the same conclusion D34 reached from the op-family
side and D27 from the lever side, now from the dispersion.

**Why it matters for the live hypotheses.** `s_fp32_residual` predicts a **shrunk** gradient —
per-block updates quantised away by a bf16 residual far larger than them, so `r ≪ 1` with `c ≈ 1`.
D27's whole-block scaling predicts the same shape. Both sit at the **low end** of [0.056, 1.944],
and nothing measured so far can confirm or refute that they are there.

**The fix, and it is two floats.** Instrument A already has `g_dev` and `g_ref` in memory when it
computes `rel`. Emit `device_norm` (or `r`) and `cos` per tensor — zero extra compute — and the
three cases separate outright: `r ≪ 1, c ≈ 1` is a shrunk copy; `r > 1, c ≈ 1` inflated; `c ≈ 0`
noise. Emit the **measured zero-model baseline** beside the median too: **PROTOCOL A16 already
requires it** and these block bundles do not carry it, which is why *"5.6 % better than zeros"*
had to be derived here rather than read off.

Owner: `of3t-rebase`. Evidence:
`perf/of3t_orchestrator/fd/magnitude_is_unidentified.json`. The intervals are exact consequences
of `|cos| ≤ 1`; nothing is fitted.

### D36. A second statistic already in the files confirms block 47's error is a common multiplicative factor: `max_abs_rel / rel_l2` sits on 1 with an IQR width of 0.064 there, against 0.29–0.45 at blocks 0 and 23. MEASURED by `of3t-orchestrator`, pass 127. No new run required.

Instrument A emits two error statistics per tensor:
`rel_l2 = ||m−r||/||r||` and `max_abs_rel = max|m−r| / max|r|`
(`instrument_a_bundle.py:535`). Their ratio `ρ = max_abs_rel / rel_l2` is, writing `m = r + e`,

    ρ = (||e||_inf / ||e||_2) · (||r||_2 / ||r||_inf)

— the ratio of the error's peakiness to the reference's. **For a pure scale `m = c·r`, the error
`e = (c−1)r` has exactly the reference's peakiness, so `ρ = 1` identically, for every tensor,
whatever `c` is.** For an error not proportional to the reference, `ρ = 1` is a coincidence.

| arm | n | ρ median | IQR width | within ±10 % of 1 |
|---|---|---|---|---|
| block 47, tb shipped | 51 | 1.031 | **0.064** | **73 %** |
| block 47, tb off | 51 | 1.010 | **0.078** | **71 %** |
| block 47, spb off, tb shipped | 52 | 1.000 | 0.241 | 50 % |
| block 23, tb shipped | 51 | 0.993 | 0.292 | 41 % |
| block 0, tb off | 51 | 0.956 | 0.301 | 39 % |
| block 0, tb shipped | 51 | 1.020 | **0.446** | 27 % |

Block 47 carries the pure-scale signature on **both** `transpose_bias` arms, with an IQR four to
seven times tighter than blocks 0 and 23. Blocks 0 and 23 do not: their error is spread
differently from the reference, which is what precision noise looks like.

**Third independent line, none of which needed a new run.**
**D34** — the median is uniform across op families *within* a block.
**D35** — `rel_l2` clusters at IQR/median **0.142** at block 47 against **1.486** and **1.851**.
**D36** — `ρ` sits on 1 at block 47 and is dispersed at blocks 0 and 23.
All three say block 47's error is a **common multiplicative factor**, not accumulated per-op error.

**Honest limits.** `ρ = 1` is *necessary* for a pure scale and not *sufficient* — any error whose
peakiness happens to match the reference's gives `ρ ≈ 1`. And `ρ` says nothing about the factor's
**sign or size**, which remains D35's unresolved `r ∈ [0.056, 1.944]`. The two floats D35 asks for
(`device_norm` and `cos`) are still what settles it. Noted and not explained: `ρ` median at block
47 shipped is **1.031**, slightly above the 1.0 a perfect scale gives, so the error is marginally
spikier than the reference.

Owner: none needed — this is evidence for D27/D35, not new work. Evidence:
`perf/of3t_orchestrator/fd/pure_scale_signature.json`.

### D37. The direction is MEASURED and it is the opposite of what I registered: block 47's gradient is INFLATED by 1.18x across essentially the whole block, not shrunk — and the block carries two separate defects. MEASURED by `of3t-rebase` (instrument) and `of3t-orchestrator` (reading), pass 128. Refutes the `s_fp32_residual` prediction as registered. UNFIXED — the 1.18x factor is unexplained and the 32x tensor is its own open defect.

`of3t-rebase` landed amendment 14 within the hour: `instrument_a_bundle.py:539-548` now emits
`device_norm`, `norm_ratio`, `cos` and `zero_model_rel`, with an `identifiability` summary block.
Six A16 bundles, all `scale_pair_bias=False` (the trunk default this branch holds), same frozen
reference `1d4ea9225f85`, crop 64. Their median `rel` reproduces my independent recompute exactly
at all four shared arms — 0.0121, 0.0192, 0.2013, 0.2386 — so the two sides agree before the new
fields are read.

| block | median `rel` | `norm_ratio` 10 / 50 / 90 | `cos` median | \|r−1\|<5 % | bulk `r` |
|---|---|---|---|---|---|
| 0 | 0.0121 | 0.988 / **0.998** / 1.002 | 0.9999 | **94 %** | 0.999 |
| 23 | 0.0192 | 0.986 / **0.999** / 1.007 | 0.9998 | **92 %** | 0.999 |
| 47 | 0.2013 | 0.887 / **1.180** / 1.219 | 0.9977 | **0 %** | 1.182 |

**Blocks 0 and 23 are right.** 94 % and 92 % of tensors have `|r−1| < 0.05` *and* `cos > 0.99`.
Their two-to-four outliers are all `attn_pair_bias` tensors at reference norms of 5e-06 to 3e-04 —
precision on near-zero gradients, which is what the bar exists to tolerate.

**Block 47 carries a block-wide 1.18x inflation.** Its `norm_ratio` deciles run
0.887 / 1.155 / 1.180 / 1.193 / 1.219: the **middle half of the block sits inside ±1.6 % of
1.18**, with `cos` 0.996–0.999, and **zero** tensors are within 5 % of unit norm ratio. A common
multiplicative factor, measured rather than inferred. The arithmetic closes:
`rel² = 1 + r² − 2rc` with `r = 1.180`, `c = 0.9977` gives **0.193** against the measured
**0.2013**. So block 47's failure is almost entirely **magnitude**, not direction.

**And one tensor is a separate, severe defect.** `47.attn_pair_bias.layer_norm_a.weight` has
`norm_ratio` **32.556** and `cos` **−0.5148** at a reference norm of 9.76e-03 — 32x too large
*and pointing the wrong way*. That is the campaign's long-quoted worst tensor, now characterised
rather than named. It is not the 1.18 factor; it is its own defect.

**MY REGISTERED PREDICTION IS REFUTED ON THE DIRECTION.** At pass 119 I registered
`s_fp32_residual` with the prediction that block 47's gradient is **shrunk** — updates quantised
away by a bf16 residual far larger than them, so `r ≪ 1` with `cos ≈ 1`. The `cos` half holds
(0.998). The direction does not: `r = 1.18`, inflated. **The hypothesis as registered is
refuted**, and the `s_fp32_residual` arm should be re-scoped or dropped rather than run to confirm
a direction already measured against it.

**What a scale fix would buy.** Correcting the 1.18 alone takes block 47 from `rel` 0.2013 to
`sqrt(2−2c) = 0.065` — a 3x improvement that **still fails** both the 2.0e-02 median bar and the
5.0e-02 per-tensor bar. Necessary, not sufficient.

**Correction to my own D36.** It read `ρ ≈ 1` as evidence of a *common* multiplicative factor.
`ρ` is computed per tensor, so strictly it shows only that the error is proportional to the
reference **within** each tensor; commonality **across** tensors needed the `norm_ratio` spread,
which D36 did not have. The measurement happens to support the stronger reading at block 47, but
D36 was entitled only to the weaker one.

**SHARPENED, AND MY OWN "WHOLE BLOCK" IS CORRECTED, pass 129.** Block 47 does not carry one
factor. It carries **two, one per track, in opposite directions**:

| sub-module | track | `r` median | `cos` median |
|---|---|---|---|
| `pair_stack.tri_att_start` | pair | **1.196** | 0.9976 |
| `pair_stack.pair_transition` | pair | **1.191** | 0.9982 |
| `pair_stack.tri_mul_in` | pair | **1.188** | 0.9979 |
| `pair_stack.tri_att_end` | pair | **1.181** | 0.9980 |
| `pair_stack.tri_mul_out` | pair | **1.171** | 0.9977 |
| `single_transition` | single | **0.887** | 0.9247 |
| `attn_pair_bias` | mixed | 0.796 (excl. its 32x outlier) | 0.7074 |

Pooled: **pair track 1.183 over 41 tensors, single track 0.887 over 5.** At blocks 0 and 23 both
tracks read **1.000 / 0.993** and **0.999 / 0.995**. So the pair gradients are uniformly ~18 %
high and the single gradients uniformly ~11 % low, at block 47 only. My pass-128 wording —
"a block-wide 1.18x inflation across essentially the whole block" — is wrong on the single track.

**Why a per-track constant is a strong clue.** A factor constant *within* a track and different
*between* tracks is what a **scaled cotangent** produces: every weight gradient downstream of that
track's cotangent inherits the same constant, and the forward is untouched. Both hold here — block
47's forward agrees to under 1 % masked on both tracks (s 3.66e-03, z 8.82e-03).

**Two candidates, and a cheap discriminator.**
**A — our block-47 backward scales each track.** Against it: blocks 0 and 23 run the same code and
config and read 1.00 on both tracks; only the weights and activation magnitudes differ.
**B — the harness injects a per-track-scaled cotangent at block 47 only.** For it: these arms are
isolated, each driven by the bundle's own boundary, so block 47's cotangent comes from a different
place in the capture than blocks 0's and 23's, and a per-track constant is exactly that signature.
Against it: `tt_bio/openfold3_trunk.py` returns `s, z` straight out of the Pairformer, so nothing
in *our* trunk after block 47 could rescale either track.
**Discriminator:** record **our** injected cotangent norm beside the reference's own, per track,
per block. The bundles already carry `probe.cot_s_norm` and `probe.cot_z_norm`; our side of those
two numbers is what is missing. If the ratio reads 1.18 on z and 0.89 on s at block 47 and 1.00 at
blocks 0 and 23, **it is an instrument defect at the campaign's worst trunk result**, not a port
defect.

**DISCRIMINATOR RUN, CANDIDATE B REFUTED, pass 130.** `of3t-rebase` implemented the
cotangent-norm check within the hour (`instrument_a_bundle.py:403-424`, emitting
`cotangent_on_device`) and ran it at all three blocks:

| block | `ratio_s` | `ratio_z` |
|---|---|---|
| 0 | 1.000117 | 0.999981 |
| 23 | 0.999998 | 0.999996 |
| 47 | **1.000060** | **1.000003** |

The injected cotangent matches the reference to **6e-05** on the single track and **3e-06** on the
pair track at block 47 — nowhere near the 1.18 and 0.89 the per-track factors would require. **The
harness does not rescale either track.** Candidate **A** by elimination: the per-track factors are
generated **inside our block-47 backward**, given a correct cotangent *and* correct inputs. So the
campaign's worst trunk result is a **real port defect**, D27/D34/D35/D36/D37 all survive, and the
1.18 hunt returns to the top of the queue.

**AND IT REVERSES MY OWN PASS-128 CALL.** I dropped the `s_fp32_residual` arm because I had
registered it predicting a **shrunk** gradient and the measured factor was 1.18, inflated. That
was the **block-pooled** median, dominated by the 41 pair-track tensors. `s_fp32_residual` touches
the **single track only** — and on the single track the measurement is **0.887**
(`single_transition`, n=5) and **0.796** (`attn_pair_bias`, which also writes `s`): **deflated by
11–20 %**, which is exactly what *"per-block updates quantised away by a bf16 residual far larger
than them"* predicts. **I refuted the hypothesis on a number that does not apply to it.** The arm
is **reinstated**, scoped to the single track.

**What is now boxed.** Block 47 receives a correct cotangent and correct inputs and produces
gradients scaled by a constant **per track** — pair ×1.183 over 41 tensors, single ×0.887 over 5 —
while blocks 0 and 23, same code and same config, produce 1.00 on both. The only differences are
the weights and the activation magnitudes: `s_norm` is **367x** block 0's at block 47 while
`z_norm` is flat at 0.98x. A per-track constant from correct inputs points at whatever is
**per-track** in the backward — the two residual streams and their dtypes.

**TWO EXPLANATIONS ELIMINATED, pass 131**, from the probe norms the D37 bundles already carry:

| blk | `s_norm` | `z_norm` | `cot_z` | ref grad sq total | single `r` | pair `r` |
|---|---|---|---|---|---|---|
| 0 | 5.8502e+03 | 1.3204e+06 | 7.8301e-04 | 1.2985e-02 | 0.993 | 1.000 |
| 23 | 1.5190e+06 | 1.3191e+06 | 4.8565e-04 | 7.7243e-03 | 0.995 | 0.999 |
| 47 | 2.1493e+06 | 1.2937e+06 | 7.6014e-04 | **1.0861e-01** | **0.887** | **1.183** |

**The pair activation magnitude is ELIMINATED.** `z_norm` is flat across all three blocks —
1.3204e+06 / 1.3191e+06 / 1.2937e+06, a **2 % range** — while the pair factor goes 1.000 / 0.999 /
**1.183**. A 2 % spread in the input cannot produce an 18 % spread in the output factor.

**The cotangent magnitude is ELIMINATED.** `cot_z` is not monotone in the factor: 7.8301e-04 at
block 0, 4.8565e-04 at block 23, 7.6014e-04 at block 47. **Block 0 carries the largest pair
cotangent and reads 1.000.**

**The single activation magnitude is WEAKENED, not eliminated.** Block 23 is already **70.7 %** of
block 47's `s_norm` and 260x block 0's, yet reads 0.995 against 0.887. If magnitude drives it
there is a **threshold** between 1.52e+06 and 2.15e+06, not a gradient — which is consistent with
a dtype-range effect and not with a smooth dependence.

**What survives is the gradient regime, not the activation one.** Block 47's reference gradients
total **1.0861e-01** in squared norm against **1.2985e-02** and **7.7243e-03** — **8.4x and 14.1x
larger** — with a per-tensor median of 8.4674e-03 against 4.0474e-03 and 3.5373e-03. That is what
being the *last* block buys: the cotangent has been attenuated least. Whether an 8–14x larger
gradient produces a clean 1.18 is **not** established here; it is the one regime variable left
standing, on n = 3, which is a ranking and not a fit.

**How the ladder should read it.** The block-boundary ladder now in flight should report, per
boundary, both the **cotangent norm ratio per track** and the **reference gradient squared-norm
total**. If the factor turns on where the gradient total crosses a threshold rather than where the
activations do, that is a dtype-range story and it names the dtype. If it turns on at one specific
boundary with no regime change, it is a code path at that boundary.

Owner: `of3t-rebase` — the single-track `s_fp32_residual` arm (reinstated), the pair-track 1.18
hunt, and `attn_pair_bias.layer_norm_a.weight` as its own defect. Evidence:
`perf/of3t_orchestrator/a16/{IDENTIFIED.json,PER_TRACK.json,COTANGENT_REFUTES_B.json,WHAT_IS_ELIMINATED.json}`
and the nine bundles beside them.

### D38. Three more mechanisms for block 47's per-track factors are eliminated by code reading, and one structural asymmetry is recorded: the pair track takes five residual adds per block and the single track two. `of3t-orchestrator`, pass 132. No card used; narrows D37 rather than resolving it.

**Eliminated, so no run should be spent on them.**

**The tape's in-place residual add is not it.** Both tracks use `ttnn.add_`, and `taped_ttnn._binary`
tapes every in-place verb **out of place** — with the exact trap already found and fixed, in its
own words: *"`ttnn.multiply_(a, b)` writes the product over a's buffer, and a is exactly what that
product's backward reads for db… Measured before this line existed: d(fc2) cosine −0.028 on the
shipped Transition."* A live in-place aliasing bug would show as an anti-correlated gradient, and
block 47's pair track is `cos` 0.998.

**A silently dropped fused scalar is not it.** `add_(x, y, input_tensor_a_activations=
[MUL_UNARY_SFPU(c)])` is `x·c + y`, and the trunk takes that path by default at `fp32_softmax=True`.
If the tape ignored the `c`, every gradient through that add would be off by exactly a constant —
the right shape for a per-track factor. It does not: `_FUSED_UNARY_PARAM` registers the builder
with derivative `c`, and `_activation(...)` **raises** on anything unmodelled rather than passing
it through. Scalar operands with a fused activation raise `NotImplementedError` explicitly.

**Recorded, not explanatory: the tracks are structurally asymmetric.** In
`PairformerLayer.__call__` (`tenstorrent.py:8825-9003`) the **pair** track takes **five**
`ttnn.add_(z, z_update)` residual adds per block — tri_mul_out, tri_mul_in, tri_att_start,
tri_att_end, pair_transition — and the **single** track takes **two** `self._s_residual(...)`.
That is a real per-track difference in a model where the two tracks carry different factors.

It does **not** explain the values, and the arithmetic says so: one shared per-add constant would
need `1.183^(1/5) = 1.0342` to equal `0.887^(1/2) = 0.9418`. It does not, so the factors are not a
single per-residual-add error compounding. The asymmetry is offered as a place to look, not as a
mechanism.

**Running tally of what is eliminated for D37**, all by reading or by measurements already on
disk: the backward kernel config (identical class on this ttnn, pass 122); the harness's cotangent
injection (bit-faithful to 6e-05, pass 130); the pair activation magnitude and the cotangent
magnitude (pass 131); the in-place residual add and the fused-scalar drop (here). What survives is
the **gradient regime** — block 47's reference gradients total 8.4x and 14.1x more squared norm
than blocks 0's and 23's — and the block-boundary ladder is the instrument that decides it.

Owner: unchanged. Evidence: `perf/of3t_orchestrator/a16/WHAT_IS_ELIMINATED.json` and this entry.


### D39. The block-boundary ladder lands: block 47 sits at the top of a sharp cotangent step, and cotangent magnitude is eliminated as the ordering variable — what is unique to block 47 is its POSITION, not a regime. MEASURED by `of3t-rebase` (capture) and `of3t-orchestrator` (reading), pass 135. The decisive arm is block 46.

`capture_trunk_boundary_043_ladder.json` — 7 boundaries from BUNDLE-MIN-043's own r = 0 step,
forward 684.6 s at loss rel **2.915e-05**, backward 1652.1 s, 45/45 draws replayed, **0** replay
mismatches, and `capture_vs_bundle` `worst_rel` **0.0 at every boundary**: the capture reproduces
the bundle bit-exactly.

| block | `cot_s` | `cot_z` | vs block 47 (s) | vs block 47 (z) | device factors |
|---|---|---|---|---|---|
| 0 | 3.53493e-05 | 7.83014e-04 | **0.631** | **1.030** | 0.993 / 1.000 |
| 8 | 2.16398e-05 | 5.64460e-04 | 0.386 | 0.743 | — |
| 16 | 2.16400e-05 | 5.59263e-04 | 0.386 | 0.736 | — |
| 23 | 2.14167e-05 | 4.85650e-04 | 0.382 | 0.639 | 0.995 / 0.999 |
| 32 | 2.14672e-05 | 4.29460e-04 | 0.383 | 0.565 | — |
| 40 | 1.94329e-05 | 3.04621e-04 | **0.347** | **0.401** | — |
| 47 | 5.60000e-05 | 7.60138e-04 | 1.000 | 1.000 | **0.887 / 1.183** |

**Block 47 sits at the top of a sharp step.** Going 47 → 40 the cotangent falls **2.88x** on the
single track and **2.50x** on the pair track. Nowhere else does it move more than **1.41x** over a
comparable span. Block 47 is the only block whose cotangent arrives **directly from the heads**;
every other block receives what has propagated back through at least one pairformer block.

**And cotangent magnitude is ELIMINATED as the ordering variable.** Block 0's single-track
cotangent is **0.631x** block 47's — only **1.58x** smaller — and block 0 is **clean**. Block 40's
is **0.347x**, the smallest in the stack. So a 1.58x difference in magnitude separates "clean"
from "factored" while a 2.9x difference produces no factor at all. That closes the last candidate
D37 had standing after pass 131, in the form it was left standing in.

**What is left is a position, not a regime.** What is unique to block 47 is that it is the **last**
block — the only one whose cotangent comes from the heads rather than through a pairformer block.
The defect should be sought at the trunk's **output boundary**, which is one block's worth of code.

**The decisive arm is block 46**, and the ladder has already made it cheap. Block 46 sits one block
below the step, in the same magnitude regime as 40 and 32, and is the first block whose cotangent
has passed through a pairformer block. **If 46 is clean and 47 is not, the defect is at the output
boundary and is localised to one block.** If 46 also carries factors, the step is not the boundary
and the search widens to the 47–40 span — for which 40 and 32 are already captured.

Honest limit: the factors are measured at three blocks and the cotangent profile at seven. The
elimination uses only measured points; the position claim is an inference from n = 3, and the
block-46 arm is what tests it.

Owner: `of3t-rebase`. Evidence: `perf/of3t_orchestrator/ladder/LADDER_READING.json` and the
captured ladder beside it.

### D40. Seven blocks measured: the per-track factor is UNIQUE to block 47, six controls are magnitude-correct — and only TWO of the seven pass the median bar, so "the trunk is fine except block 47" is wrong. MEASURED by `of3t-rebase` (arms) and `of3t-orchestrator` (reading), pass 136. Confirms D39's position claim and opens block 32.

Seven arms at one config — `scale_pair_bias=False`, `transpose_bias=True`, same frozen reference,
crop 64 — with `norm_ratio` and `cos` per tensor:

| blk | median `rel` | `r` median | `cos` median | pair `r` | single `r` | passes 2.0e-02? |
|---|---|---|---|---|---|---|
| 0 | **0.01214** | 0.9986 | 0.99993 | 0.9997 | 0.9932 | **yes** |
| 8 | 0.03025 | 0.9976 | 0.99955 | 0.9971 | 1.0019 | no |
| 16 | 0.02162 | 0.9989 | 0.99981 | 0.9990 | 0.9992 | no |
| 23 | **0.01919** | 0.9989 | 0.99984 | 0.9991 | 0.9948 | **yes** |
| 32 | 0.09679 | 1.0002 | **0.99563** | 0.9998 | 1.0026 | no |
| 40 | 0.02118 | 0.9991 | 0.99979 | 0.9988 | 1.0106 | no |
| 47 | **0.20129** | **1.1809** | 0.99768 | **1.1832** | **0.8867** | no |

**D39's position claim is CONFIRMED, with six controls instead of the one arm I asked for.** Pass
135 inferred from n = 3 that block 47 is distinguished by position rather than by a magnitude
regime, and proposed block 46 as the test. The factor is absent at **40** — only seven blocks
below 47 — and at every other depth sampled. **It is a property of the last block.**

**And the trunk is not otherwise fine.** By the 2.0e-02 median bar only **two of seven pass**:
block 0 at 0.01214 and block 23 at 0.01919. Blocks 16, 40, 8, 32 and 47 all exceed it. Any reading
of the form *"the trunk is fine except block 47"* is wrong, and the campaign should not adopt one.
What **is** true is that six of the seven are magnitude-correct and well-aligned, so their failures
are small residuals while block 47's is structural.

**Block 32 is a different shape and nobody has looked at it.** Median **0.09679**, the second
worst, with `norm_ratio` **1.0002** and `cos` **0.99563** — the lowest cosine of the seven. Its
magnitude is right; its **direction** is not. That is a directional error, not a scale, and it is
a different defect from block 47's.

**No depth trend among the clean six**: medians run 0.0121, 0.0303, 0.0216, 0.0192, 0.0968, 0.0212
at depths 0, 8, 16, 23, 32, 40 — not monotone. Depth does not order them; block 32 is an outlier
inside the clean set.

**Sharpening D39's elimination.** On the **pair** track — the one carrying the 1.183 — block 0's
cotangent is **1.0301x** block 47's, i.e. **3 % larger**, and block 0 is clean. So for the pair
factor the clean block carries *more* cotangent than the factored one: magnitude is eliminated
outright, not merely weakened.

Owner: `of3t-rebase` — block 47 (now known to be the only block with this shape) and block 32
(a directional error, uncharacterised). Evidence:
`perf/of3t_orchestrator/ladder/DEPTH_PROFILE.json` and the seven arms beside it.

### D41. Across seven blocks exactly ONE measured variable separates block 47 from the six clean ones — the reference gradient magnitude, at 6.97x the next highest with no overlap — and it has a decisive one-arm test. MEASURED by `of3t-orchestrator`, pass 137. Four rivals eliminated by measurement.

| blk | `s_norm` | `z_norm` | `cot_s` | `cot_z` | ref grad sq tot | single `r` | pair `r` |
|---|---|---|---|---|---|---|---|
| 0 | 5.8502e+03 | 1.3204e+06 | 3.5349e-05 | 7.8301e-04 | 1.2985e-02 | 0.9932 | 0.9997 |
| 8 | 1.3495e+06 | 1.3216e+06 | 2.1640e-05 | 5.6446e-04 | 9.0924e-03 | 1.0019 | 0.9971 |
| 16 | 1.4769e+06 | 1.3206e+06 | 2.1640e-05 | 5.5926e-04 | 7.7101e-03 | 0.9992 | 0.9990 |
| 23 | 1.5190e+06 | 1.3191e+06 | 2.1417e-05 | 4.8565e-04 | 7.7243e-03 | 0.9948 | 0.9991 |
| 32 | 1.5368e+06 | 1.3144e+06 | 2.1467e-05 | 4.2946e-04 | 6.2936e-03 | 1.0026 | 0.9998 |
| 40 | 1.5483e+06 | 1.3067e+06 | 1.9433e-05 | 3.0462e-04 | 1.5586e-02 | 1.0106 | 0.9988 |
| **47** | 2.1493e+06 | 1.2937e+06 | 5.6000e-05 | 7.6014e-04 | **1.0861e-01** | **0.8867** | **1.1832** |

| variable | block 47 / max(others) | separates? |
|---|---|---|
| `s_norm` | 1.39x | no — block 40 is at 1.5483e+06 |
| `z_norm` | **0.98x** | no — block 47 has the **smallest** pair activation of the seven |
| `cot_s` | 1.58x | no — block 0 is at 3.5349e-05 and is clean |
| `cot_z` | **0.97x** | no — block 47's pair cotangent is **below** block 0's, and block 0 is clean |
| **ref grad sq total** | **6.97x** | **YES — no overlap** |

The six clean blocks spread only **2.5x** among themselves on gradient total (6.2936e-03 to
1.5586e-02); block 47 sits at **1.0861e-01**. Block 47 has the largest gradient because it is the
**last** block — its weight gradients receive the full head cotangent without attenuation through
any pairformer block. That is expected model structure. What is a defect is that our backward
stops matching there.

**Honest limit.** n = 7 with one point that is the outlier on *both* axes is a correlation, not a
mechanism — any variable singling out block 47 would look like this. The value is that four rivals
are now eliminated **by measurement at n = 7** rather than argued at n = 3, and that the survivor
has a decisive test.

**The decisive test, one arm, no new capture.** In **exact** arithmetic `norm_ratio` and `cos` are
**invariant** under scaling the injected cotangent: scale it by `k`, the device gradient scales by
`k`, and if the reference is scaled by the same `k` analytically — a multiply on a stored tensor,
free — then `r` and `cos` are unchanged exactly. **So any movement at all is finite-precision
magnitude dependence, isolated.** Run block 47 at `k = 1/8`, which puts its gradient inside the
other six blocks' range:

- factors move toward 1.00 → **the defect is magnitude-dependent**, and which dtype carries it is
  the next question;
- factors stay at 1.183 / 0.887 → **magnitude is dead too**, and what remains is something about
  the last block that is not its gradient size.

Owner: `of3t-rebase`. Evidence: `perf/of3t_orchestrator/ladder/DISCRIMINATING_VARIABLE.json`.

### D42. Both reference manifests name the WRONG upstream revision — "0.5.0 (git checkout)" — because it is a hardcoded `getattr` fallback, and 0.5.0 is the revision D23 disqualified. FOUND by `of3t-orchestrator`, pass 140. UNFIXED. The reference itself is fine; its provenance field is not.

`perf/of3t_reference/bundle_min.py:643`:

    "openfold3": getattr(openfold3, "__version__", "0.5.0 (git checkout)"),

The 0.4.3 checkout does **not** define `__version__` — verified: `openfold3.__file__` resolves to
`of3pkg043/openfold3/__init__.py` and `__version__` is absent — so the fallback fires **every
time**, and the fallback **asserts a specific wrong version** rather than admitting ignorance.

**The tree really is 0.4.3.** Its `PKG-INFO` reads `Name: openfold3` / `Version: 0.4.3`. The model
built from it has **4,170** parameters (0.5.0 gives 4,147) and the checkpoint loads
missing=1 / unexpected=0 (0.5.0 gives missing=3 / unexpected=48 and upstream's own loader raises,
which is D23). So the campaign's claim that the reference was rebuilt at 0.4.3 is **true** and is
supported by the parameter count and the load signature — not by the manifest.

**But the manifest is the artifact.** The string is in **both** of them:
`bundle_min_043/MANIFEST.json` and `run/out_043_fd/manifest.json`. An auditor reading the
provenance field of the file that certifies this campaign's reference would conclude it was built
on **the exact revision D23 disqualified**, and would be right to stop there. The truth is one
file away in `PKG-INFO` and the manifest does not look.

**Fix, and the shape of it matters.** Read the version from the checkout — `PKG-INFO`, or the
tree's git SHA if it has one — and make the fallback **`"unknown"`**, never a guess. A default
that names a version is indistinguishable from a measurement of that version, and this one names
the disqualified revision. Re-emit both manifests; the payload bytes are unaffected
(`grads_f64_043.pt` keeps sha256 `1d4ea922…`), so it is a metadata re-write, not a rebuild.

Owner: `of3t-rebase`. Evidence: this entry; `bundle_min.py:643`; `of3pkg043/PKG-INFO`.

### D43. The ramp chain aborted correctly on a missing script — the failure-stop asked for at pass 136 fired on its first use — and the homogeneity ladder is now blocked on `capramp.sh`. `of3t-orchestrator`, pass 140. Blocked, not broken.

`rampchain.sh` waits for the float64 FD job, then captures blocks 42–46 and runs the arms. It
reached its capture step and found `perf/of3t_rebase/capramp.sh` missing:

    bash: .../capramp.sh: No such file or directory
    === capramp rc=127 ===
    RAMPCHAIN_ABORT capture failed rc=127

**That is the right behaviour and it is new.** At pass 136 I flagged `ladderarms.log` printing
`LADDERARMS_ALLDONE` over a died report step. `rampchain.sh` now carries the opposite discipline
in its own comment — *"FAILURE STOPS THE CHAIN — a capture that dies must not hand a missing
boundary to the arm step, which would then 'skip' it and report a short table as if it were
complete"* — plus a per-block existence check before the arms run. It aborted, loudly, on first
use.

So the homogeneity ladder (A21, amendments 23–24) is **blocked on one missing script**, not on a
result. Owner: `of3t-rebase`.

### D44. Block 47 is worth about ONE PER CENT of the model's gradient mass, and it has held the campaign's attention for a dozen passes while the diffusion module — 89.211 % of the mass at 19.6x its own forward — has had nothing aimed at it. MEASURED by `of3t-orchestrator`, pass 141. A prioritisation defect, not a technical one.

Exact, over the seven sampled trunk blocks:

| blk | ref grad sq | reach (norm share ≤ 5.0e-02) | median |
|---|---|---|---|
| 0 | 1.2985e-02 | 94.88 % | 0.01214 |
| 8 | 9.0924e-03 | 84.76 % | 0.03025 |
| 16 | 7.7101e-03 | 83.38 % | 0.02162 |
| 23 | 7.7243e-03 | 74.96 % | 0.01919 |
| 32 | 6.2936e-03 | 61.58 % | 0.09679 |
| 40 | 1.5586e-02 | 82.94 % | 0.02118 |
| **47** | **1.0861e-01** | **0.00 %** | 0.20129 |

Over those seven the trunk reaches **29.20 %** of squared gradient norm. **Block 47 is 64.65 % of
the sampled mass and contributes 0.00 % of the reach** — not one of its tensors passes the
per-tensor bar. Excluding it, the six others reach **82.58 %**.

**Extrapolated — and this is an estimate, labelled as one.** Assuming the 41 unsampled blocks
resemble the six clean ones (mean ref grad sq 9.90e-03), block 47 is **≈18.9 %** of the whole
trunk and, since the trunk is **5.828 %** of the model, **≈1.10 %** of the model's gradient mass.
The campaign independently recorded block 47 at about 1.06 % of model mass, so the extrapolation
reproduces a number derived another way.

**The judgement that forces.** Block 47 has been the right place to learn *how* to localise — it
is the sharpest signal the campaign has ever had, a clean per-track factor with six controls, and
the method built on it (per-track norm ratio, cosine, the block ladder, the homogeneity test)
transfers. But **fixing it completely moves the model-scope number by about one per cent of the
mass.** The **diffusion module is 89.211 %** and sits at **19.6x** its own forward (D30), and
nothing has been aimed at it since the ladder work began.

**What should change.** The two block-47 arms still open are nearly free and should finish — the
k-ladder (A21) and the blocks 42–46 bracket capture now in flight, which is the cleanest
localisation available. **But the next dispatch after them belongs to the diffusion module**, and
the instrument already exists: the DiT has 24 blocks and the same per-block ladder that just
resolved the trunk applies unchanged.

Owner: `of3t-orchestrator` — this is a queue decision, and it is mine. Evidence:
`perf/of3t_orchestrator/ladder/WHERE_THE_MASS_IS.json`.

### D45. I dispatched a test that could not fail: PROTOCOL A21 told the row to scale by powers of two, which makes a scale-invariance arm vacuous. CAUGHT by `of3t-rebase`, confirmed by `of3t-orchestrator`, pass 142. The conclusion survives on the arms the row added; my evidence for it was empty.

A21 (pass 138) said: *"choose `k` a power of two and the test has no noise floor… any movement in
the last digits is a real finding."* **Backwards.** Scaling every input of a linear
floating-point computation by `2^n` shifts exponents only — every mantissa and every rounding
decision untouched — so the result is invariant **by the arithmetic**, whatever defect is present.
Ordinary rounding error is *relative*, hence exactly scale-invariant under `2^n`, so a
power-of-two ladder **cannot see** the magnitude-dependent rounding the test was written to find.

**Measured at block 47** (`instrument_a_bundle_COTSCALE_*.json`):

| `k` | power of two | median `rel` | pair `r` | single `r` | `cos` |
|---|---|---|---|---|---|
| 1.0 | yes | 0.201289 | 1.183174 | 0.886707 | 0.997679 |
| 0.125 | yes | 0.201289 | 1.183174 | 0.886707 | 0.997679 |
| 0.015625 | yes | 0.201289 | 1.183174 | 0.886707 | 0.997679 |
| 0.1 | **no** | 0.202049 | 1.183496 | 0.887574 | 0.997702 |
| 0.0137 | **no** | 0.201675 | 1.183135 | 0.885709 | 0.997708 |

The three powers of two agree to **all six digits across a 64x range**. Reproduced independently
here on a toy rounded-matmul chain: `k = 1/8` and `k = 8` give **rel move 0.000e+00** and
bit-identical results after unscaling, while `k = 1/3` and `k = 1.37` give **6.0e-03**.

**The conclusion survives, and now has evidence.** The row added the non-power arms on its own.
Across a **73x** range (1.0 → 0.0137) the pair factor moves **0.03 %** and the single **0.2 %**,
against factors sitting **18 %** and **11 %** from unity. **Gradient magnitude does not drive the
block-47 factors** — D41's surviving candidate is now refuted, by a test that could actually fail.

**What the powers of two are good for.** Not nothing: they are a valid **arithmetic control**.
They *must* read bit-identical, and if they do not, the computation is not linear in that input
and nothing else in the experiment is readable. Here they did, so linearity is established and
the non-power arms are interpretable.

**The failure mode.** Choosing the scale for exactness selects exactly the case that cannot fail —
a negative control that does not break what the check reads, arriving from the opposite direction:
the *positive* arm was made incapable of moving. A21 is corrected in place (the wrong sentence
removed, not annotated) and replaced by **A22**.

Owner: closed. Filed against `of3t-orchestrator`: I wrote the instruction, put it in the protocol,
and the row caught it.

### D46. "51.14 % of the model's gradient norm compared" is not sampling — it is 89.211 % × 57.32 %, and the missing 38.07 % is diffusion tensors our port does not build. MEASURED by `of3t-orchestrator`, pass 143. UNFIXED, and it is the ceiling on reach.

`full_model_of3_full_043.json`'s presence block: of the reference's **4,170** gradient-carrying
tensors, **3,569 (85.6 %)** are carried by a device tensor and **601 (14.4 %) are not**. By
section:

| section | uncarried tensors |
|---|---|
| `pairformer_stack` | 240 |
| **`diffusion_module`** | **188** |
| `input_embedder` | 93 |
| `msa_module` | 32 |
| `template_embedder` | 28 |
| `aux_heads.pairformer_embedding` | 20 |

**The arithmetic that matters.** The diffusion module is **89.211 %** of the model's squared
gradient norm, and the all-48-structure run's own `share_of_diffusion_sq_norm` is **0.5732** — so
the compared set is `89.211 % × 57.32 % = ` **51.14 %**, which is exactly the coverage the campaign
reports. The reported figure *is* the diffusion module times its internal coverage.

**Therefore 38.07 % of the model's gradient mass is in diffusion tensors the instrument cannot
currently compare.** It dwarfs everything the campaign has been chasing: block 47 is ~1.10 %
(D44), the whole trunk is 5.828 %.

**CORRECTED ONE PASS LATER — I called this "a port gap, missing parameters" and that is wrong for
the larger, regular half.** The 96 `diffusion_transformer` entries are **exactly four leaf names
repeated once per block across all 24 blocks**: `attention_pair_bias.mha.linear_q.weight`,
`.linear_q.bias`, `.linear_k.weight`, `.linear_v.weight`. Those are Q, K and V — and
`openfold3_diffusion_transformer.py:122-131` reads all four and **concatenates them into one
padded device tensor**: *"Fused padded qkv: cat([linear_q, linear_k, linear_v]) with head_dim
48->64"*. `tenstorrent.py:7422-7436` does the same for the shared `AttentionPairBias`. **The
parameters are on device.** The bijection cannot place them because one device tensor carries
three plus zero padding.

**So for those 96 the remedy is an instrument change, not a port rewrite:** unfuse the device
`qkv` gradient into its q / k / v slices and compare each against its own reference tensor. The
head packing is already derived from the device rather than assumed (`perf/ptx_fastpath/heads.py`),
and the 48→64 zero padding has to be dropped on the way out.

**The other 92 are open.** `atom_attn_enc` 50 and `atom_attn_dec` 42 span **eight** leaf names
over three blocks each — `linear_ada_out.{weight,bias}`, `linear_z.weight`, `mha.linear_{g,k,o,q}`
— which is more than QKV. Whether the atom transformers are also fused or genuinely absent is
**not established**.

**MEASURED AT PASS 146, AND IT REFUTES THIS DEFECT'S CENTRAL CLAIM.** One pass over
`grads_f64_043.pt` summing squared reference gradient norms per group:

| group | n | % of diffusion | % of model |
|---|---|---|---|
| carried | 573 | **98.735** | **88.082** |
| uncarried, atom transformers | 92 | 0.961 | 0.857 |
| uncarried, fused QKV | 96 | 0.304 | 0.272 |

**The 188 uncarried diffusion tensors hold 1.265 % of the diffusion module's gradient mass and
1.13 % of the model's — not 38 %.** The attribution in this defect was wrong *and* so was the
magnitude. Unfusing the QKV is a correctness fix worth **0.272 %** of the model, not a coverage
lever. (The diffusion module's 89.211 % share is confirmed here directly from the reference file
rather than from a derived table, which is the one thing this defect got right.)

**So where the 38.07 % comes from is now OPEN.** `device_gradient_043all.json` reports
`share_of_diffusion_sq_norm = 0.5732` while **98.735 %** of the diffusion mass is carriable. That
run compared **547 of 761** reference tensors and named **547 of 870** reachable device weights —
so **323 reachable device weights went unnamed** and 214 reference tensors unmatched. The ~41-point
gap between *compared* and *carriable* is a limitation of **that run's naming**, not of what the
port builds, and what it actually is has **not been determined**. That is the next measurement,
and it needs no card.

Honest limit: the group sums are exact over the reference file; the claim about *why* the all-48
run compared only 57.32 % is an inference from its own counters and is not established.

**Why this needs saying in exactly these words.** *"51.14 % of the squared gradient norm
compared"* reads like sampling — as though the other half were more of the same, unexamined. It is
not. It is a **hole with a known shape**, in the one section that holds nine tenths of the mass,
and its cause is named: tensors our `DiffusionModule` does not build.

**Honest limits.** The 601 uncarried tensors' mass share is computed **only** for the diffusion
part; the other five sections' contributions are bounded by their section shares (pairformer
5.828 %, aux_heads 2.843 %, msa 1.240 %, input_embedder 0.801 %) and are not separately measured
here. And "uncarried" (188) and "uncompared" (214 of 761 in the all-48 run) are close but not
identical sets — the difference is bijection failures on tensors that do exist. The 57.32 % is
the run's own field and is the authoritative coverage number.

Owner: unassigned, and it is the largest unowned item on the record. Evidence:
`perf/of3t_orchestrator/diffusion/COVERAGE_CEILING.json`.

### D47. The contiguous 40–47 bracket splits the two tracks: the pair factor IS unique to block 47, the single track breaks sharply at block 44, and the famous 32.556 outlier is not special to 47 at all — block 42's is larger. MEASURED by `of3t-rebase` (arms) and `of3t-orchestrator` (reading), pass 145. Corrects D39/D40 and retires a pointer.

| blk | median `rel` | pair `r` | pair cos min | single `r` | **single cos min** | single rel max | apb `r` max |
|---|---|---|---|---|---|---|---|
| 40 | 0.02118 | 0.9988 | 0.9988 | 1.0106 | **0.9976** | 0.0702 | 13.123 |
| 42 | 0.02546 | 0.9988 | 0.9976 | 0.9884 | **0.9995** | 0.0461 | **38.951** |
| 43 | 0.01935 | 0.9963 | 0.9960 | 1.0040 | **0.9998** | 0.0209 | 13.513 |
| **44** | 0.08278 | 1.0026 | 0.9930 | 1.0071 | **0.8438** | **0.5382** | 1.425 |
| 45 | 0.07928 | 1.0153 | 0.9772 | 0.7010 | 0.6812 | 0.7322 | 1.227 |
| 46 | 0.09326 | 1.0206 | 0.9884 | 0.8195 | **0.2888** | 1.1792 | 10.534 |
| 47 | 0.20129 | **1.1832** | 0.9937 | 0.8867 | 0.8172 | 0.5796 | 32.556 |

**The pair track: D39/D40 hold.** `r` sits **0.996–1.021 through block 46** and steps to
**1.1832** only at 47. That onset is a step, and it is at the last block.

**The single track: D39/D40 do not hold — it breaks at block 44.** The clean signal is the
**cosine**: minimum single `cos` runs **0.9976 / 0.9995 / 0.9998** at blocks 40, 42, 43 and then
**0.8438** at 44, 0.6812 at 45, **0.2888** at 46, 0.8172 at 47. Maximum single `rel_l2` confirms
it — 0.0702 / 0.0461 / 0.0209, then **0.5382** at block 44, a **25x jump**. The single-track
*median norm ratio* hides this (1.0071 at block 44) because the deviations straddle 1.0. **On a
five-tensor track the median is the wrong statistic; the cosine is the right one.**

**And the 32.556 outlier is not special to block 47.** `attn_pair_bias`'s maximum norm ratio reads
**13.123 / 38.951 / 13.513 / 1.425 / 1.227 / 10.534 / 32.556** across blocks 40–47 — no depth
pattern, and **block 42's 38.951 is larger than block 47's 32.556.** The tensor the campaign has
named for dozens of passes is one of many; `attn_pair_bias` carries large norm-ratio outliers at
every block measured. Third instance of a worst-case naming a tail rather than a locus.

**What this corrects.** D39 and D40 concluded "unique to block 47" from six controls at 0, 8, 16,
23, 32, 40 — **all far from the end**. Filling in 42–46 keeps the claim for one track of three:
the pair factor, yes; the single-track break, no (block 44); the outlier, never.

Honest limit: the single track is **five tensors per block** (`single_transition`), so its median
is weak — which is exactly why the reading above rests on the per-tensor cosines and the maximum
`rel_l2` instead. The pair track has 41 per block and its step at 47 is solid.

Owner: `of3t-rebase`. Evidence: `perf/of3t_orchestrator/ladder/BRACKET_40_47.json` and the five
RAMP arms beside it.

### D48. The 41-point coverage gap is `diffusion_conditioning` — 26 tensors, none of them uncarried, holding 41.415 % of the diffusion module's gradient mass. Including them takes the campaign's measured coverage from 51.14 % to ~88.08 % of the model. MEASURED by `of3t-orchestrator`, pass 147. UNFIXED, and it is the cheapest large lever on the record.

Sub-module mass, summed directly from `grads_f64_043.pt`:

| sub-module | n | uncarried | % of diffusion |
|---|---|---|---|
| `diffusion_transformer` | 552 | 96 | 49.202 |
| **`diffusion_conditioning`** | **26** | **0** | **41.415** |
| `atom_attn_enc` | 98 | 50 | 6.231 |
| `atom_attn_dec` | 82 | 42 | 1.477 |
| `layer_norm_s` / `layer_norm_a` / `linear_s` | 1 / 1 / 1 | 0 | 1.102 / 0.299 / 0.274 |

**The arithmetic closes exactly**, which is what makes this an identification and not a guess:

- carried diffusion tensors = 761 − 188 = **573**; the all-48 run compared **547**; 573 − 547 = **26**
- `diffusion_conditioning` has exactly **26** tensors and exactly **0** uncarried
- 98.735 % carriable − **41.415 %** = **57.320 %**, which *is* the run's own
  `share_of_diffusion_sq_norm` = 0.5732

**What it is worth.** `diffusion_conditioning` is **36.95 %** of the model's squared gradient norm.
Current measured coverage is **51.14 %**; including these 26 takes it to about **88.08 %** —
cross-checked against the independently measured carried-diffusion share of the model, 88.082 %.
**Twenty-six tensors roughly double the campaign's measured gradient coverage, without touching
the port.** They are carried — our code builds them — and the finite-difference run already
validated the reference on two of them (`transition_s.1.layer_norm.bias` at **5.088e-03**,
`transition_s.0.layer_norm.weight` at **1.501e-05**).

**CORRECTED ONE PASS LATER — it is not an omission, it is a documented scope boundary, and I
should have read the script before saying otherwise.** `perf/of3t_diffusion/device_gradient.py`'s
own docstring, line 9: *"Scope: our module is their `diffusion_module` minus
`diffusion_conditioning`, which on our side is a separate class that feeds it… The conditioned
`(si, zij)` come from THEIR conditioning via `sub_boundary.pt`, so nothing our own conditioning
does can flatter this number."* That is a **sound methodological choice** for what the arm is
for — isolating our diffusion module's own gradient, fed from the reference so our conditioning
cannot flatter it. The 26 tensors are not unmeasurable; they are outside **that** arm by design.

**So the ask changes shape.** `diffusion_conditioning` is a separate class on our side, so it
needs **its own instrument-A arm with its own captured boundary** — not a widened scope on the
existing one. That is a bigger piece of work than "include 26 tensors", and it is the right piece.

**What survives, and it is the part that matters.** The campaign's headline — *"51.14 % of the
model's squared gradient norm compared"* — does not disclose that **36.95 % of the model's mass
is excluded by a deliberate boundary**. A reader takes 51.14 % for a measurement limit. It is a
scope choice, and scope choices belong next to the number they shape.

**And the docstring's own figures are stale**: *"712 of 738 tensors, 66.39 % of the diffusion
squared norm"* is on the 0.5.0 4,147-parameter basis. On the correct 4,170 basis the arm's scope
is **735 of 761** tensors and **58.585 %** of the diffusion norm, and the run's own measured
`share_of_diffusion_sq_norm` is **57.320 %**.

**What is NOT claimed: that they will pass.** Coverage is not parity. Measuring the most
concentrated block of a section that already fails at median **1.6588e-01** will quite possibly
make the model-scope number worse.

**History.** The campaign recorded around pass 59 that `diffusion_conditioning`'s 26 tensors carry
*"33.61 % of the diffusion magnitude"* and sit outside our `DiffusionModule` boundary. That figure
was computed on the 0.5.0 model; on the correct 4,170-parameter 0.4.3 basis it is **41.415 %**.
The observation was right, the number was on the wrong basis, and **nobody followed it up for
ninety passes** — including me, through the whole of D46's three wrong versions.

Owner: `of3t-rebase`. Evidence: `perf/of3t_orchestrator/diffusion/WHY_57_PERCENT.json`.

### D49. `fp32_softmax=False` takes five of seven measured trunk blocks across the median bar, against two with it on — the campaign's first lever that improves gradient parity — though **by mass it is a 2.5 % improvement, not a 2-6x one**, because block 47 holds 64.86 % of the measured mass and does not move (restated under A23, pass 151). FOUND by `of3t-rebase`, INDEPENDENTLY VERIFIED by `of3t-orchestrator`, pass 148. RELEASE-GATED and deliberately not flipped.

Recomputed from the row's own `SMOFF` and `LADDER` bundles, config fields checked (`fp32_softmax`
**False** in every SMOFF, **True** in every LADDER):

| blk | ON | OFF | change | ON passes 2.0e-02? | OFF passes? |
|---|---|---|---|---|---|
| 0 | 0.01214 | 0.00949 | 0.782x | yes | yes |
| 8 | 0.03025 | **0.01297** | 0.429x | no | **yes** |
| 16 | 0.02162 | **0.01399** | 0.647x | no | **yes** |
| 23 | 0.01919 | 0.01513 | 0.788x | yes | yes |
| 32 | 0.09679 | **0.01679** | **0.173x** | no | **yes** |
| 40 | 0.02118 | 0.02127 | 1.004x | no | no |
| 47 | 0.20129 | 0.20311 | 1.009x | no | no |

**Five of seven better; three cross the bar.** The row's claim reproduces exactly.

**Block 32 is explained.** The "magnitude right, direction wrong" block I flagged at pass 136 —
median 0.09679 with `norm_ratio` 1.0002 and `cos` 0.99563 — improves **5.8x** to 0.01679 and
crosses. `fp32_softmax` accounts for it, and D47's open question about it is closed.

**And it does nothing for blocks 40 and 47** (1.004x, 1.009x). So the lever acts on blocks 0–32
and not on the end of the stack — independent evidence that block 47's per-track factor is a
different mechanism from everything the lever touches.

**What it does not settle.** `fp32_softmax=True` was set for **forward** accuracy; this arm is
trunk backward only, one crop, one boundary, seven blocks of forty-eight. It is a **gradient-
versus-forward tradeoff, not a free win**, and it is release-gated: the row reported it, did not
flip it, and the shipped default stays `True`. That is correct handling.

**What it licenses**: a pre-registered A/B on the forward. If `fp32_softmax=False` costs nothing
measurable on fold accuracy at shipped sizes, it is a candidate default change worth three of
seven blocks crossing the bar. If it does cost forward accuracy, the campaign has found a real
tension between forward and gradient parity — **a finding either way**, and the first one in this
campaign that points at a decision rather than at another measurement.

**RESTATED UNDER A23, pass 151 — the heading above overstates this by roughly 20x and is
corrected here rather than annotated.** The seven blocks are not equal: block 47 alone holds
**64.86 %** of the measured set's gradient mass, and its ratio is **1.009** — very slightly
worse. Weighting each block by the share of the model it actually holds:

| | mass-weighted | unweighted mean | median-of-blocks |
|---|---|---|---|
| `fp32_softmax=True` | **0.14053** | 0.05749 | 0.02162 |
| `fp32_softmax=False` | **0.13707** | 0.04182 | 0.01513 |
| off / on | **0.9754** | | 0.6998 |

So by mass the lever is a **2.5 %** improvement, not the 2–6x the per-block ratios suggest.
**Excluding block 47 it is a genuine halving — mass-weighted 0.02840 → 0.01521, ratio 0.5353.**
The lever is real and it is not small; what it does not do is touch the block that dominates the
mass, which is also the block already known to be the worst.

Two further things this restatement shows. **Before any lever**, the trunk's mass-weighted rel is
**0.14053** against the median-of-blocks **0.02162** the campaign has been quoting — the
statistic understates its own arm by **6.5x**, with the lever on or off. And the whole
restatement still covers **1.64401 %** of the model (28.21 % of the trunk); the other 41 blocks
are unmeasured at this config.

The measurement and its independent verification stand exactly as taken. What changes is the
sentence attached to them. The honest statement: **`fp32_softmax=False` halves the mass-weighted
gradient error across six of seven measured trunk blocks and leaves the seventh — which holds two
thirds of the measured mass — unchanged.** The forward A/B below is still the right next step and
the decision it feeds is now better specified.

Owner: unassigned — it needs a forward-accuracy arm, which is a different kind of work from
everything the gradient rows have been doing. Evidence:
`perf/of3t_orchestrator/smoff/FP32_SOFTMAX_LEVER.json` and
`perf/of3t_orchestrator/smoff/FP32_SOFTMAX_UNDER_A23.json`. Default remains `True`; nothing is
flipped.

### D50. A live row is running on an inverted premise I wrote: `of3t-auxheads`' brief says aux_heads is larger than the whole pairformer stack. Measured, aux_heads is 2.8431 % and pairformer is 5.8282 % — pairformer is 2.05x LARGER. FOUND by `of3t-orchestrator`, pass 149. Brief amended while the row holds the card.

Every section's share of the model's squared gradient norm, summed **directly** from
`grads_f64_043.pt` over all 4,170 tensors — measured, not derived from a table:

| section | n | % of model |
|---|---|---|
| `diffusion_module.diffusion_transformer` | 552 | **43.8936** |
| `diffusion_module.diffusion_conditioning` | **26** | **36.9462** |
| `pairformer_stack` | 2736 | 5.8282 |
| `diffusion_module.atom_attn_enc` | 98 | **5.5589** |
| `aux_heads` | 244 | **2.8431** |
| `diffusion_module.atom_attn_dec` | 82 | 1.3173 |
| `msa_module` | 227 | 1.2400 |
| `diffusion_module.layer_norm_s` | 1 | 0.9835 |
| `input_embedder` | 98 | 0.8007 |

**The brief's opening paragraph** — *"aux_heads is 4.27 % of the squared gradient norm — LARGER
than the whole 48-block pairformer stack at 3.16 % … a misallocation the norm shares make
visible"* — carries **both** 0.5.0 / 4,147-basis figures, which pass 91 established are the wrong
model. The row is on `qb2-card0` now, executing against a rationale that says the opposite of what
is true. **I wrote that brief.**

**Amended rather than cancelled.** aux_heads at 2.8431 % genuinely has never been measured at
instrument-A scope, and the §6 `bond` coverage hole is untouched by the correction — so the work
is worth finishing. What must change is the **claim**: it is a section never measured, now
measured, at 2.84 % of the mass, not "the campaign's misallocation".

**And a better target was named in the same amendment.**
`diffusion_module.atom_attn_enc` is **5.5589 %** — nearly **twice** aux_heads — and
`of3t-rebase`'s concluding measurement puts it at median **7.11e-01**, `cos` **0.7757**: the
worst-performing named thing in the model, with no dedicated arm.

**The number that reframes the campaign.** **Twenty-six tensors** — `diffusion_conditioning` —
are **36.9462 %** of the model's gradient mass, **more than everything outside the diffusion
module combined (10.79 %) by a factor of 3.4.**

Honest limit: this is the reference gradient's mass at one batch (`batch_step003`, 5nw3, 56 real
tokens) at r = 0. Nothing here establishes the shares are stable across batches; it is the right
denominator for comparisons against *this* reference, which is what every comparison in this
campaign uses.

Owner: closed as a brief defect (amended). The measured table is
`perf/of3t_orchestrator/SECTION_MASS_MEASURED.json`.

---

### D51. The campaign has been allocating arms by section name while 84.6 % of the gradient's mass sits in 1-D LayerNorm vectors holding 0.095 % of the parameters — and the single heaviest thing in the model had never been named. FOUND by `of3t-orchestrator`, pass 151. Measured, exhaustive, **UNFIXED**.

**What was measured.** Every one of the 4,170 tensors of the 0.4.3 reference
(`bundle_min_043/grads_f64_043.pt`, model squared norm 10.279642678524985), summed in float64
on the host, no device opened. The section table sums to **100.0 %** over all 4,170 tensors —
the earlier `SECTION_MASS_MEASURED.json` listed 14 sections and silently dropped 4 holding
0.002 %, which is negligible mass but a table that does not say it is partial reads as complete.
Both are corrected.

**The finding.**

| cut | tensors | parameters | % of parameters | % of gradient mass |
|---|---|---|---|---|
| all 1-D tensors | 1,514 | 350,588 | **0.0952 %** | **84.6253 %** |
| all `.bias` | 817 | 220,992 | 0.0600 % | 23.0692 % |
| `layer_norm` name family | 1,509 | 29,467,644 | 8.0011 % | 85.3118 % |

**Ten of the twelve heaviest tensors in the model are LayerNorm gain/bias vectors on the single
(`s`) track inside `diffusion_module`.** Only two matrices appear in the top twelve at all, and
the larger is 3.39 %. The heaviest single tensor in OpenFold3's gradient is a **384-entry
vector**: `diffusion_module.diffusion_conditioning.transition_s.0.layer_norm.bias`, **10.38237 %**.

**Why this is a defect and not just a fact.** Every dispatch this campaign has made was argued
from a *section* share — "diffusion is 89 %", "aux_heads is 2.8 %", "the trunk is 5.8 %". Those
shares are correct and they are the wrong granularity to allocate on, because the mass inside a
section is not spread across it. Two consequences already on the record:

- **`diffusion_conditioning`, the largest open item, is four tensors.** The section is 36.9462 %
  over 26 tensors; **four** of them are **33.9354 %** — 91.85 % of the section — over **1,985
  scalars**, 0.000539 % of the model's parameters. The other 22 tensors are 3.0107 % of the
  model between them. This item has been blocked for passes on moving `device_gradient.py`'s
  walked boundary to cover the whole module. The dominant third of it is four short vectors.
- **DiT block 8 is 9.84053 % of the model** — 1.69x the *entire* 48-block pairformer stack and
  9.2x pairformer block 47, the block this campaign has investigated in the most depth. The
  sixteen highest-mass blocks in the model are all DiT blocks. No block-scope arm has ever been
  run on DiT block 8. `perf/of3t_orchestrator/BLOCK_MASS_PROFILE.json`.

**A third consequence, in the other direction, which is good news.** The reference's
finite-difference axis was reported by count (8 of 4,170) and by section (4 sections hit). By
**mass** it directly validates **22.9369 %** of the gradient, and it happens to include the #2
and #3 heaviest tensors in the model (9.29095 % and 8.80266 %). Count-based coverage of a
mass-concentrated gradient is the wrong denominator in *both* directions — here it understated a
good result; applied to a uniform sample it would overstate a weak one. Recorded in
`fd/REFERENCE_VALIDATED_FINAL.json`, together with the caveat that the set's **worst** agreement,
rel **5.088e-03**, falls on the **#2 heaviest tensor** — the reference's own validation is
loosest exactly at the mass peak, still ~10x inside the 5.0e-02 bar.

**Is the concentration an artifact of dimension?** Partly, not mostly. Per-entry rms is
**5.272e-02** for the heaviest LayerNorm bias (384 entries) against **2.661e-03** for the
heaviest matrix in the top twelve (49,152 entries): the entries are individually ~20x larger
*and* there are ~128x fewer of them, and the two effects compound. Mechanically this is what a
LayerNorm gain/bias gradient is — a sum over every token and every sample of the normalized
activation, accumulated into a short vector — so it is expected, which is not the same as
harmless.

**And the denominator caveat that must travel with it.** The squared-norm share is the right
denominator for *this campaign's* question, which is whether our gradient **is** their gradient
as a vector, and it is what a global grad-norm clip sees. It is **not** the right denominator for
the optimiser step: Adam divides each coordinate by its own second moment, which removes most of
this concentration. A parity failure concentrated in these vectors would read loudly in a
gradient-norm comparison and quietly in an Adam trajectory. Quote both.

**What this does not establish.** Mass is not error. No arm has been run at this cut, so nothing
here says the LayerNorm gradients are where our port is wrong. It says where a given amount of
error costs most, and therefore which arm is worth building next. One batch (`batch_step003`,
5nw3, 56 real tokens), one r = 0 step; the stability of these shares across batches is unmeasured.

Owner: `of3t-orchestrator`. **UNFIXED** — this changes what should be dispatched next and no
dispatch has been made on it yet. Artifacts:
`perf/of3t_orchestrator/WHERE_THE_GRADIENT_MASS_LIVES.json`,
`perf/of3t_orchestrator/BLOCK_MASS_PROFILE.json`,
`perf/of3t_orchestrator/SECTION_MASS_MEASURED.json` (now exhaustive).

---

### D52. The campaign's largest open item has been recorded as blocked for eleven passes. It was unblocked at 01:43 the same day, by a capture one of my own rows made and nobody read the keys of. FOUND by `of3t-orchestrator`, pass 151. Now dispatched.

**The claim on the record.** `diffusion_conditioning` — 36.9462 % of the model's squared
gradient norm, the largest single unmeasured scope in the campaign — "needs the
`device_gradient.py` boundary moved", which `of3t-rebase` called *a precondition, not a
follow-up*. I repeated it in the state doc, in `of3t-auxheads`' amendment 3, and in the
pending-work list of every pass since. `perf/of3t_diffusion/device_gradient.py:9` says it in
the source: *"our module is their `diffusion_module` minus `diffusion_conditioning`, which on
our side is a separate class that feeds it"*.

**What is actually on qb2.** `/home/ttuser/of3t_rebase/diffcap043/sub_boundary.pt`,
3,170,912,516 bytes, written **2026-09-20 01:43**. Its keys, read this pass:

```
cond_out       tuple: si (1, 48, 384, 384), zij (1, 1, 384, 384, 128)
cond_out_cot   list : the REFERENCE's cotangent at exactly that boundary, same shapes
grad_f64       dict : 761 float64 reference gradients, including all 26 diffusion_conditioning.*
enc_in, dit_in, dit_out, dec_in, xl_out, cot
```

Inputs, the cotangent at the conditioning's output, and the float64 answer for all 26 tensors.
**The reference side of the arm is complete.** What is missing is our side — run our conditioning
class forward on `cond_out`'s inputs, backward on `cond_out_cot`, compare 26 tensors. That is a
script, not a boundary move.

**How it stayed hidden.** The capture was made to serve the *sub-module* bisection of the
diffusion path, and it was described by its purpose rather than its contents. `cond_out_cot` was
written because capturing a boundary means capturing both sides of it — it was a free by-product
of the thing the row was asked for, and no one listed the file's keys afterwards. Everything
downstream then quoted the scope sentence in `device_gradient.py`'s docstring, which was true
when written and describes a *tool's* scope, not the *evidence's* availability. Eleven passes of
"blocked on a precondition" rest on reading a tool's docstring as a statement about what data
exists.

**The lesson, which is not "read the docstring".** A scope sentence in a tool describes what the
tool walks. It is not a statement about what has been captured, and it silently becomes stale the
moment a different row captures more. **When a scope is recorded as blocked, the blocker is
re-checked against the artifacts on disk, not against the prose that first declared it** — list
the keys of the capture, do not quote the module that does not read them.

**Cost.** Eleven passes during which the largest item on the record was described as needing work
that had already been done, while smaller scopes were dispatched. Compounding D50 and D51: the
campaign's allocation has been argued from prose about section sizes and prose about blockers,
and both were checkable in minutes against files that were already present.

Owner: `of3t-orchestrator`. **FIXED as of this pass** — row `of3t-conditioning` dispatched
(`workstreams/of3t-conditioning.txt`, `TASKS.md`, `_of3t_donecheck.py` entry added), carrying the
verified key list so the next row does not have to rediscover it.

---

### D53. The campaign's errors are concentrated on exactly the tensors that carry the gradient — the ten worst hold 15.7140 % of the model, the ten best 1.1650 % — and the worst disagreement on the record, rel 18.504, is on the model's fourth-heaviest tensor. FOUND by `of3t-orchestrator`, pass 151, from two artifacts that already existed. **UNFIXED.**

**No new run.** `device_gradient_043all.json` (547 tensors compared, all 48 structures,
`median_rel` **0.16588**, `worst_rel` **18.504**, **474 of 547 over** the 5.0e-02 bar) has been
on qb2 since the 0.4.3 rebuild. The per-tensor reference mass was measured this pass (D51). They
had never been read together.

| | share of the model's squared gradient norm |
|---|---|
| the **ten worst** tensors by `rel` | **15.7140 %** |
| the **ten best** tensors by `rel` | **1.1650 %** |

A **13.5x concentration of error on mass.** The single worst point in the campaign —
`diffusion_transformer.blocks.8.attention_pair_bias.layer_norm_a.layer_norm_s.weight` at rel
**18.504** — is **8.05416 %** of the model, the **fourth-heaviest tensor in OpenFold3**, sitting
inside the heaviest block in the model (DiT block 8, 9.84053 %).

**Seven of the ten worst are `attention_pair_bias.layer_norm_a.*` inside the diffusion
transformer**, six of them the same leaf, `layer_norm_a.layer_norm_s.weight`. That is the same
leaf family D51 independently identified as carrying the model's mass. **The campaign's mass peak
and its worst errors are the same tensors.** Nothing connected them before because the mass table
did not exist until this pass.

**Why this is a defect about our reporting and not only about our port.** A23 was raised earlier
this pass on the general argument that a median over tensors describes the tensors that carry no
mass. This is that argument meeting data, and it lands in the **unflattering** direction: the
headline `median_rel` **0.16588** *understates* the damage, because the tensors failing worst are
the heavy ones. The mass-weighted figure — the one a parity claim actually owes — is still
unmeasured and will be worse than the median. Every place this campaign has quoted a diffusion
median is quoting the flattering half of a distribution whose failures sit on the mass.

**A hypothesis written down so it is falsifiable, and which this does NOT establish.** Within the
six `layer_norm_a.layer_norm_s.weight` points, `rel` broadly rises with mass — blocks 0/6/5/7/12/8
at rel 2.73/2.84/4.54/5.31/5.65/18.50 against mass 0.18/1.39/2.43/2.86/0.76/8.05 %. `rel` is
already a *relative* measure, so a fixed relative error would be **independent** of magnitude;
`rel` growing with magnitude would point at something super-linear — saturation, overflow, a
magnitude-dependent precision loss — rather than a uniform rounding difference. **But these are
six points, block 12 already breaks the ordering, and they are the six worst of a family of 24 —
a selected tail, not a sample.** The family's full `rel` set is not in the artifact, so the
hypothesis is **still open** — neither confirmed nor refuted, and it may not be quoted as
either. **What would settle it:** the per-tensor dump below, then `rel` plotted against
`‖g_ref‖` for all 24 blocks of that one leaf. **It is refuted if the full family shows no
trend** — which is the likely outcome, because six worst-of-24 points will manufacture this
trend whether or not it exists.

**What blocks all three follow-ups, and what it costs.** `device_gradient_043all.json` stores
aggregates plus `worst10`/`best10` and **no per-tensor array**. So the mass-weighted headline
A23 requires, the per-block DiT profile, and the mass-vs-`rel` test above are *all* underivable
from what exists — and all three come free from one re-run that writes the per-tensor rel beside
the tensor name. **A result file that keeps only the extremes cannot be re-analysed under a
denominator discovered later**, and this campaign has now changed its denominator twice.

**LOCALISED BY SOURCE AUDIT, same pass, no card.** Every one of the ten worst tensors is a
`layer_norm*.weight` gain or a `linear_g.{weight,bias}` — the **sigmoid-gated branch of AdaLN**.
**Not one is a `linear_s`.** Six are the same leaf,
`attention_pair_bias.layer_norm_a.layer_norm_s.weight`, across DiT blocks 0/5/6/7/8/12; two more
are `linear_g` inside `atom_attn_enc`'s atom transformer. **One sub-module, reached from two
different stacks.**

The forward is not the problem: upstream 0.4.3's `AdaLN`
(`core/model/primitives/normalization.py:88`) is `a = LN_a(a); s = LN_s(s); g = σ(linear_g(s));
a = g*a + linear_s(s)` with LN_a scale- and offset-free and LN_s scale-only at eps 1e-5, and ours
(`tenstorrent.AdaLN` via `remap_of3_adaln`) is the same transform. `forward_rel_median` is
**8.4748e-03**. The defect is in the **backward**.

Three hypotheses read against the source and settled:

- **The fused-sigmoid derivative — REFUTED.** Our forward fuses σ into `ttnn.multiply_` as an
  operand-B activation; had the tape treated it as a plain multiply, the cotangent to `s_scale`
  would miss σ(1−σ) — a factor ≥ 4 and unbounded, corrupting `linear_g` and the s-norm gain,
  sparing `linear_s`, leaving the forward untouched. It fits the pattern and the 4.5–18.5 range
  exactly. It is wrong: `taped_ttnn.py:444-515` evaluates the product rule on the **activated**
  operands and then applies σ(1−σ), and `d/da` correctly uses the activated `eb`.
- **The LayerNorm gain formula — REFUTED.** `autograd.py:1566-1622` computes `g · x̂` summed over
  the leading dims with a two-pass variance under `precise_config()`. Textbook correct.
- **A bf16 reduction in the gain and bias sums — WEAKENED, not eliminated.** `_taped_linear`'s
  dW rule documents that `fp32_dest_acc_en` is *not* enough — `packer_l1_acc` accumulates
  per-K-block partials at the **output** dtype — and passes `dtype=ttnn.float32`. The gain and
  bias gradients both go through `_sum_leading`, which passes `precise_config()` but **no output
  dtype**. That would hit exactly the two rules whose tensors fail and spare bias-free
  `linear_s`. But the class measured **6.5e-02 at K=4096** and scales as √K, projecting to
  ~1.4e-01 at the DiT's 18,432 terms.

**And the magnitude says none of these is the whole story.** `‖g_ref‖` for the worst tensor is
`sqrt(0.0805416 × 10.279642678524985)` = **0.9099**, so at rel 18.504 the error vector has norm
**~16.8**. That is not a precision signature — it is a wrong scale, a wrong transform, or double
counting. A single structural factor shared by the code path would also make all 24 blocks
equally wrong *relatively*; they are not (2.73 at block 0, 18.50 at block 8), so whatever it is
**scales with something that varies by block**.

**What settles it in one run:** the norm ratio `r = ‖g_dev‖/‖g_ref‖` and the cosine beside each
rel. `r ≈ 19.5` with `cos ≈ 1` is a scale and the search is over a factor; `r ≈ 1` with `cos ≈ 0`
is a wrong transform. rel alone cannot distinguish them.

Owner: `of3t-orchestrator`. **UNFIXED.** The per-tensor dump is requested of the live
`of3t-conditioning` row (brief amendments 1 and 2), now specifying `r` and `cos` for every
tensor rather than rel alone. Artifacts:
`perf/of3t_orchestrator/ERRORS_CONCENTRATE_ON_MASS.json`,
`perf/of3t_orchestrator/ADALN_BACKWARD_ELIMINATIONS.json`.
