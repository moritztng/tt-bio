{
  "what": "D193's owed fact table: the padded width each shared capture was taken at, read off the capture itself rather than off prose. A crop label in a brief, a defect entry or a summary field is checkable against this.",
  "why": "of3t-apbback's block-47 result was labelled 'at crop 64' in my brief and in DEFECTS.md. It is a crop-384 object. The crop is recorded in the capture as a tensor shape, so the label never had to be remembered -- D180 says quote the crop or do not quote the number, and D193 is the case where it was quoted and was wrong, which is worse because it survives review.",
  "measured_on": "tt-quietbox2 (qb2), pass 342, torch load + shape read, no device",
  "rule": "padded_width = kwargs.single_mask.shape[-1]; real_tokens = int(single_mask.sum())",
  "captures": {
    "/home/ttuser/of3t_gradients/cap/block0_boundary.pt": {
      "single_mask": [
        1,
        384
      ],
      "pair_mask": [
        1,
        384,
        384
      ],
      "padded_width": 384,
      "real_tokens": 56
    },
    "/home/ttuser/of3t_gradients/cap/block23_boundary.pt": {
      "single_mask": [
        1,
        384
      ],
      "pair_mask": [
        1,
        384,
        384
      ],
      "padded_width": 384,
      "real_tokens": 56
    },
    "/home/ttuser/of3t_gradients/cap/block47_boundary.pt": {
      "single_mask": [
        1,
        384
      ],
      "pair_mask": [
        1,
        384,
        384
      ],
      "padded_width": 384,
      "real_tokens": 56
    },
    "/home/ttuser/of3t_softgrad/diffcap043/diffusion_boundary.pt": {
      "token_mask": [
        1,
        1,
        384
      ],
      "atom_mask": [
        1,
        1,
        422
      ],
      "zij_trunk": [
        1,
        1,
        384,
        384,
        128
      ],
      "padded_width": 384,
      "real_tokens": 56,
      "no_samples": 48,
      "loss": 1.2675874205688995,
      "mask_key_read": "token_mask (this capture has no `single_mask`; the diffusion module takes `token_mask`, so the rule's key is module-dependent and the key read is recorded beside the number)",
      "used_by": "the `diffusion=` leg of every model-scope arm in perf/of3t_modelboundary/MODEL_withtrunk_n384.json, so every diffusion_module.* section figure this campaign publishes -- 43.89 % of the model's squared gradient norm -- is a padded-width-384 reading",
      "measured_on": "tt-quietbox2 (qb2), of3t-ditgap, torch load + shape read, no device"
    }
  },
  "consequences": [
    "ALL THREE shared block boundaries are padded width 384 with 56 real tokens. Every per-block result this campaign took on perf/refpath.py's CAP is a 384 reading, including of3t-apbback's block-47 softmax bisect (51.55 % of the block's error). The figure is unaffected and is a reading at the width the campaign reports.",
    "A 'crop 64' reading therefore cannot come from CAP. of3t-frame384's and of3t-widthattr's c64 arms are built from their own n64 boundary, not from these files -- so a crop label also identifies WHICH capture, not only a number.",
    "56 real tokens in 384 means 85.4 % of the width is pad at the campaign's reported width. That is the fact D175 was filed about and is recorded here beside the widths rather than argued from memory.",
    "of3t-ditgap added the DIFFUSION capture `diffcap043/diffusion_boundary.pt`: padded width 384, 56 real tokens, 48 diffusion samples. It is the same width and the same real-token count as the three CAP block boundaries, so the campaign's diffusion-side and trunk-side figures are at one width. Its mask key is `token_mask`, not `single_mask` -- the rule's key is module-dependent, and a row that reads `single_mask` on this file gets a KeyError rather than a wrong number, which is the safe direction."
  ],
  "not_covered": "the n64 and n384 boundaries built by of3t-frame384 and of3t-trunk043ref are not in this table; a row that adds one should extend it rather than start a second table."
}
