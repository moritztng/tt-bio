{
 "instrument": "perf/of3t_orchestrator/lnladder/bit_deficit_sites.py",
 "host": "pc",
 "unit": "mantissa bits short of IEEE fp32, = log2 of the ratio between our reading and a better-precision counterpart of the SAME quantity. The cancellation factor cancels out of such a ratio, which is why the two sites are comparable at all.",
 "unit_validated_by": "LADDER_SHAPE.json -- a 2-bit-short accumulator reads 3.928 against a predicted 4.000, flat in K over a 2,300x span",
 "sites": [
  {
   "site": "softmax/shipped",
   "source": "QUOTED from of3t-softmax's cost table",
   "factor_over_better_precision": 202900.0,
   "bits_short_of_ieee_fp32": 17.630409339531383
  },
  {
   "site": "softmax/precise_config()",
   "source": "QUOTED from of3t-softmax's cost table",
   "factor_over_better_precision": 16460.0,
   "bits_short_of_ieee_fp32": 14.006676715308988
  },
  {
   "site": "softmax/_accurate_softmax",
   "source": "QUOTED from of3t-softmax's cost table",
   "factor_over_better_precision": 5156.0,
   "bits_short_of_ieee_fp32": 12.332036548361652
  },
  {
   "site": "trunk LayerNorm affine reduction (low bound)",
   "source": "of3t-modelframe ATTRIBUTION.json, frame-matched",
   "factor_over_better_precision": 3.0,
   "bits_short_of_ieee_fp32": 1.584962500721156
  },
  {
   "site": "trunk LayerNorm affine reduction (high bound)",
   "source": "of3t-modelframe ATTRIBUTION.json, frame-matched",
   "factor_over_better_precision": 4.6,
   "bits_short_of_ieee_fp32": 2.2016338611696504
  }
 ],
 "softmax_bits_short_range": [
  12.332036548361652,
  17.630409339531383
 ],
 "trunk_bits_short_range": [
  1.584962500721156,
  2.2016338611696504
 ],
 "gap_in_bits_between_the_two_sites": 10.130402687192001,
 "gap_as_a_factor": 1120.869565217392,
 "verdict": "SUPERSEDED -- see the SUPERSEDED field. Do not quote the verdict this file originally carried.",
 "consequence": "The trunk's deficit is SMALL -- about two bits. That is the opposite of the softmax finding, where nothing on-device came within four orders of magnitude of real fp32 and a host round trip was the only route. A two-bit deficit is the size a higher-precision on-device accumulate could plausibly close, so of3t-lnreduce should price the on-device rung BEFORE assuming it needs a host reduction. Testable, and cheaper if it holds.",
 "caveat": "The two readings are different computations -- a softmax output against float64, and a reduction's dW against upstream's own bf16 step -- so this is an order-of-magnitude argument in a common unit, not a proof. It is decisive only because the gap is 10.1 bits, far beyond what the shape difference plausibly explains.",
 "SUPERSEDED": "Pass 379, by LEDGER R133 and of3t-lnreduce's measurement. The arithmetic in this file is correct; its verdict is not. It compares the softmax sites' deficit against TRUE FP32 with the trunk's 3-4.6x, which is a ratio against upstream's own BF16 step -- two different references, so the 10.1-bit gap is an artifact of the comparison and not a fact about the hardware. Measured at the worst affine site, ours against upstream's own fp32 configuration is 6,171x = 12.6 bits, inside the softmax's 12.3-17.6 band. The on-device deficit against true fp32 is UNIFORM across both sites. A 'bits short' figure is only comparable when both readings name the same reference."
}
