Pushed the corrected fix as `bf304d1`. The reason I did rather than wait: as the branch stands it
would merge with the NVIDIA path behaving differently from `301efdd`, which is the one thing the PR
body promises it does not do. One commit on top of `68b853d`, nothing rebased or force-pushed,
+14/-4 in `design_workers.py`, no test file. Drop it if you would rather take it yourself.

Re-measured today. Control is `68b853d` unpatched, result is `bf304d1`, same tree both runs, jax
0.11.2 / jaxlib 0.11.2, python 3.12.12, CPU backend, `design_devices()` stubbed:

```
                                                                  68b853d   bf304d1
A  third platform, 2 devices -> a 1-worker plan, not a raise        FAIL     pass
B  CUDA_VISIBLE_DEVICES=2,3 -> the cards the job was given          FAIL     pass
B  workers pinned to 2 and 3, not 0 and 1                           FAIL     pass
B  free memory keyed by the pin name, not the jax id                FAIL     pass
C  rocm, no variable set -> the whole box                           pass     pass
D  HIP_VISIBLE_DEVICES=2,3 -> the cards the job was given           FAIL     pass
D  variable does not describe the devices -> jax ids, no mixed list pass     pass
E  no accelerator -> no fan-out, no raise                           pass     pass
E  no accelerator -> an empty plan, no raise                        pass     pass
```

Unstubbed on the real CPU backend, both revisions: `selected_design_gpus() -> []` and
`plan_design_workers({}, 300, 20) -> []`. A box with no accelerator sees no change.

Where it still stops. Cases B and D rest on jax ids being a dense enumeration of the devices a
process can see. I measured that on CPU, `XLA_FLAGS=--xla_force_host_platform_device_count=4`
giving `cpu:0` through `cpu:3`, and not on an NVIDIA card, because there is none on this machine.
One line on a real box settles it:

```bash
CUDA_VISIBLE_DEVICES=2,3 python -c "import jax; print(jax.devices())"
```

If that prints ids 0 and 1, B and the first D are real. If it prints 2 and 3, only A and the two
dead `None` handlers are, and the rest of `bf304d1` is a no-op you can drop.
