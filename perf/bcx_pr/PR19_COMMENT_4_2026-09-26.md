Pushed `d00dd6f`, which makes this branch fix #23.

A DGX Spark GB10 shares one memory pool with the host, so `nvidia-smi --query-gpu=memory.free,memory.total` prints `[N/A]` in both fields and exits 0, and `float('[N/A]')` raises out of `nvidia_smi_memory_gb` through `plan_design_workers` into `campaign refused`. This branch carried that line unchanged from `main`, so it did not fix the report even though the report lands in the function it rewrites. It now drops a row whose memory does not parse instead of the whole reading, which leaves `design_gpu_memory_gb` to fall through to the plugin's own `memory_stats()`, exactly as it already does where `nvidia-smi` is absent. A board that reports numbers is read as before.

Measured on three trees with 11 checks, `design_devices()` stubbed and an `nvidia-smi` stub that prints what the reporter's does, jax 0.11.2 on the CPU backend, no accelerator anywhere:

| tree | pass / fail |
| --- | --- |
| `main` at `3e3563894` | 4 / 7 |
| this branch merged into it, before `d00dd6f` | 9 / 2 |
| the same merge, after | 11 / 0 |

The two that failed in the middle row are the GB10 pair. `git merge origin/main` still resolves clean, and the four files this branch touches appear in no other open PR.

I have no GB10, so what the CUDA plugin reports for the unified pool is untested. If it reports nothing, the outcome there is one worker per device rather than a refused campaign.
