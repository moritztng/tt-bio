diff --git a/CMakeLists.txt b/CMakeLists.txt
index 8b8cd50..97bc14e 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -86,6 +86,7 @@ set(CMAKE_CXX_EXTENSIONS OFF)
 option(HIP "Enable HIP GPU acceleration" OFF)
 option(SYCL "Enable SYCL acceleration" OFF)
 option(ALTCPU "Enable Accelerated CPU version" OFF)
+option(TT "Enable Tenstorrent acceleration (requires ALTCPU for the un-offloaded acc kernels)" OFF)
 if(HIP)
 	set(HIP_ARCH "" CACHE STRING "HIP architecture to use")
 	if (HIP_ARCH STREQUAL "")
@@ -137,6 +138,16 @@ option(DoublePrec_ACC "Accelerated Code use double-precision" OFF)
 option(MKLFFT "Use MKL rather than FFTW for FFT" OFF)
 option(DeviceTexture "Texture Memory on the Device" ON)
 
+if(TT)
+    if(NOT ALTCPU)
+        message(FATAL_ERROR "TT=ON needs ALTCPU=ON: the un-offloaded acc kernels compile from src/acc/cpu")
+    endif()
+    add_definitions(-D_TT_ENABLED=1)
+    find_package(Python3 COMPONENTS Development REQUIRED)
+    include_directories(${Python3_INCLUDE_DIRS})
+    message(STATUS "TT enabled - Tenstorrent acceleration through embedded CPython")
+endif()
+
 if(ALTCPU)
     message(STATUS "ALTCPU enabled - Building CPU-accelerated version of RELION")
 elseif(SYCL)
diff --git a/src/acc/acc_helper_functions_impl.h b/src/acc/acc_helper_functions_impl.h
index c06a122..7437eed 100644
--- a/src/acc/acc_helper_functions_impl.h
+++ b/src/acc/acc_helper_functions_impl.h
@@ -12,6 +12,10 @@
 #include "src/macros.h"
 #include "src/error.h"
 */
+#ifdef _TT_ENABLED
+#include "src/acc/tt/tt_bridge.h"
+#endif
+
 #ifdef _CUDA_ENABLED
 	using deviceStream_t = cudaStream_t;
 #elif _HIP_ENABLED
@@ -366,7 +370,7 @@ void runWavgKernel(
 				stream
 				);
 		else if (projector.mdlZ!=0)
-#if defined ALTCPU || defined _SYCL_ENABLED
+#if defined ALTCPU || defined _SYCL_ENABLED || defined _TT_ENABLED
 			AccUtilities::kernel_wavg<true,true,false,WAVG_BLOCK_SIZE_REF3D>(
 #else
 			AccUtilities::kernel_wavg<true,true,false,WAVG_BLOCK_SIZE>(
@@ -392,7 +396,7 @@ void runWavgKernel(
 				stream
 				);
 		else
-#if defined ALTCPU || defined _SYCL_ENABLED
+#if defined ALTCPU || defined _SYCL_ENABLED || defined _TT_ENABLED
 			AccUtilities::kernel_wavg<true,false,false,WAVG_BLOCK_SIZE_2D>(
 #else
 			AccUtilities::kernel_wavg<true,false,false,WAVG_BLOCK_SIZE>(
@@ -443,7 +447,7 @@ void runWavgKernel(
 				stream
 				);
 		else if (projector.mdlZ!=0)
-#if defined ALTCPU || defined _SYCL_ENABLED
+#if defined ALTCPU || defined _SYCL_ENABLED || defined _TT_ENABLED
 			AccUtilities::kernel_wavg<false,true,false,WAVG_BLOCK_SIZE_REF3D>(
 #else
 			AccUtilities::kernel_wavg<false,true,false,WAVG_BLOCK_SIZE>(
@@ -469,7 +473,7 @@ void runWavgKernel(
 				stream
 				);
 		else
-#if defined ALTCPU || defined _SYCL_ENABLED
+#if defined ALTCPU || defined _SYCL_ENABLED || defined _TT_ENABLED
 			AccUtilities::kernel_wavg<false,false,false,WAVG_BLOCK_SIZE_2D>(
 #else
 			AccUtilities::kernel_wavg<false,false,false,WAVG_BLOCK_SIZE>(
@@ -1154,6 +1158,24 @@ void runDiff2KernelCoarse(
 		bool do_CC,
 		bool data_is_3D)
 {
+#ifdef _TT_ENABLED
+	// Whole-tensor altitude: the device gets orientation_num x translation_num x image_size in one
+	// dispatch. Any shape the bridge declines falls straight through to the code below.
+	if (!do_CC && projector.mdlZ != 0 && !data_is_3D)
+	{
+		if (TTBridge::diff2Coarse(
+				(const float*)projector.mdlComplex,
+				projector.mdlX, projector.mdlXY / projector.mdlX, projector.mdlZ,
+				projector.mdlInitY, projector.mdlInitZ,
+				projector.maxR, projector.maxR2_padded, projector.padding_factor,
+				projector.imgX, projector.imgY,
+				d_eulers, trans_x, trans_y,
+				Fimg_real, Fimg_imag, corr_img,
+				diff2s,
+				(long)orientation_num, (long)translation_num, (long)image_size))
+			return;
+	}
+#endif
 	const long unsigned blocks3D = (data_is_3D? D2C_BLOCK_SIZE_DATA3D : D2C_BLOCK_SIZE_REF3D);
 
 	if(!do_CC)
@@ -1739,7 +1761,7 @@ void runDiff2KernelCoarse(
 // a single call to diff2_CC_course?
 		// dim3 CCblocks(orientation_num,translation_num);
 		if(data_is_3D)
-#if defined ALTCPU || defined _SYCL_ENABLED
+#if defined ALTCPU || defined _SYCL_ENABLED || defined _TT_ENABLED
 			AccUtilities::diff2_CC_coarse<true,true,D2C_CC_BLOCK_SIZE_DATA3D>(
 #else
 			AccUtilities::diff2_CC_coarse<true,true,D2C_BLOCK_SIZE_DATA3D>(
@@ -1760,7 +1782,7 @@ void runDiff2KernelCoarse(
 				local_sqrtXi2,
 				stream);
 		else if(projector.mdlZ!=0)
-#if defined ALTCPU || defined _SYCL_ENABLED
+#if defined ALTCPU || defined _SYCL_ENABLED || defined _TT_ENABLED
 			AccUtilities::diff2_CC_coarse<true,false,D2C_CC_BLOCK_SIZE_REF3D>(
 #else
 			AccUtilities::diff2_CC_coarse<true,false,D2C_BLOCK_SIZE_REF3D>(
@@ -1781,7 +1803,7 @@ void runDiff2KernelCoarse(
 				local_sqrtXi2,
 				stream);
 		else
-#if defined ALTCPU || defined _SYCL_ENABLED
+#if defined ALTCPU || defined _SYCL_ENABLED || defined _TT_ENABLED
 			AccUtilities::diff2_CC_coarse<false,false,D2C_CC_BLOCK_SIZE_2D>(
 #else
 			AccUtilities::diff2_CC_coarse<false,false,D2C_BLOCK_SIZE_2D>(
@@ -1838,6 +1860,26 @@ void runDiff2KernelFine(
 		bool do_CC,
 		bool data_is_3D)
 {
+#ifdef _TT_ENABLED
+	// The sibling of the coarse hook. RELION names significant_num entries of the
+	// orientation x translation matrix; the bridge computes them whole-tensor and returns.
+	if (!do_CC && projector.mdlZ != 0 && !data_is_3D)
+	{
+		if (TTBridge::diff2Fine(
+				(const float*)projector.mdlComplex,
+				projector.mdlX, projector.mdlXY / projector.mdlX, projector.mdlZ,
+				projector.mdlInitY, projector.mdlInitZ,
+				projector.maxR, projector.maxR2_padded, projector.padding_factor,
+				projector.imgX, projector.imgY,
+				eulers, trans_x, trans_y,
+				Fimgs_real, Fimgs_imag, corr_img,
+				rot_idx, trans_idx,
+				diff2s, (float)(op.highres_Xi2_img[img_id] / 2.),
+				(long)orientation_num, (long)translation_num, (long)significant_num,
+				(long)image_size, (long)job_num_count))
+			return;
+	}
+#endif
     long unsigned block_dim = job_num_count;
 
     if(!do_CC)
@@ -2002,6 +2044,11 @@ void runDiff2KernelFine(
 		#endif
     }
 
+#ifdef _TT_ENABLED
+	// TT_RELION_CHECK=1 only: grade the private-buffer result against what RELION just wrote.
+	if (!do_CC && projector.mdlZ != 0 && !data_is_3D)
+		TTBridge::diff2FineCheck(diff2s, (long)significant_num);
+#endif
 }
 
 void runCollect2jobs(	int grid_dim,
diff --git a/src/acc/utilities.h b/src/acc/utilities.h
index f407bdb..fc67a30 100644
--- a/src/acc/utilities.h
+++ b/src/acc/utilities.h
@@ -297,7 +297,7 @@ else
 #endif
 }
 
-#if defined ALTCPU || defined _SYCL_ENABLED
+#if defined ALTCPU || defined _SYCL_ENABLED || defined _TT_ENABLED
 template <typename T>
 static size_t filterGreaterZeroOnHost(T *in, const size_t sz, T *out)
 {
diff --git a/src/apps/CMakeLists.txt b/src/apps/CMakeLists.txt
index 24f2c3e..ea449d5 100644
--- a/src/apps/CMakeLists.txt
+++ b/src/apps/CMakeLists.txt
@@ -30,7 +30,7 @@ endif()
 configure_file("${CMAKE_SOURCE_DIR}/src/macros.cpp.in" "${CMAKE_BINARY_DIR}/macros.cpp" @ONLY)
 
 if (ALTCPU)
-	file(GLOB REL_SRC "${CMAKE_SOURCE_DIR}/src/*.cpp" "${CMAKE_BINARY_DIR}/macros.cpp" "${CMAKE_SOURCE_DIR}/src/*.c" "${CMAKE_SOURCE_DIR}/src/acc/cpu/*.cpp" "${CMAKE_SOURCE_DIR}/src/acc/cpu/cpu_kernels/*.cpp" )
+	file(GLOB REL_SRC "${CMAKE_SOURCE_DIR}/src/*.cpp" "${CMAKE_BINARY_DIR}/macros.cpp" "${CMAKE_SOURCE_DIR}/src/*.c" "${CMAKE_SOURCE_DIR}/src/acc/cpu/*.cpp" "${CMAKE_SOURCE_DIR}/src/acc/cpu/cpu_kernels/*.cpp" "${CMAKE_SOURCE_DIR}/src/acc/tt/*.cpp" )
 	file(GLOB REL_SRC_H "${CMAKE_SOURCE_DIR}/src/*.h" "${CMAKE_SOURCE_DIR}/src/acc/*.h" "${CMAKE_SOURCE_DIR}/src/acc/cpu/*.h" "${CMAKE_SOURCE_DIR}/src/acc/cpu/cpu_kernels/*.h" )
 else()
 	file(GLOB REL_SRC "${CMAKE_SOURCE_DIR}/src/*.cpp" "${CMAKE_BINARY_DIR}/macros.cpp" "${CMAKE_SOURCE_DIR}/src/*.c" "${CMAKE_SOURCE_DIR}/src/acc/*.cpp" )
@@ -224,6 +224,9 @@ endif()
 # This is empty for ICC.
 # See https://github.com/3dem/relion/issues/958#issuecomment-1498521352.
 target_link_libraries(relion_lib ${FFTW_LIBRARIES})
+if(TT)
+	target_link_libraries(relion_lib ${Python3_LIBRARIES})
+endif()
 if(NOT MKLFFT)
 	if(BUILD_OWN_FFTW)
 		add_dependencies(relion_lib own_fftw_lib)
